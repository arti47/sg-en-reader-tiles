# CLAUDE.md — Adaptive English Tutor for Singapore Primary (P1–P6) — "PrimaryEnglish"

> **Purpose of this file.** Single source of truth for an AI coding agent (or human dev) building a personal, Core5-style adaptive English tutor for a small group of **Singapore primary school children (P1–P6)**, aligned to the **MOE English Language Syllabus 2020 / STELLAR 2.0** and preparing toward **PSLE English**. Read top to bottom before coding. Build in the phase order in §15. When ambiguous, prefer the choice that keeps the app **pedagogically sound, PSLE-aligned, self-contained, and simple to run at home**.

---

## 1. Project Overview

**What we are building.** A self-adapting English tutor for **Singapore Primary 1–6 (ages ~7–12)**, modeled on the adaptive/blended engine of Lexia Core5 but **localised to the Singapore MOE curriculum and PSLE**, for **personal/home use with a small group of children**.

**Primary audience: students with literacy difficulties (incl. dyslexia).** Confirmed profile: **decoding + spelling difficulties (reading and spelling)**. This is the central design driver. Consequences (enforced throughout): Structured Literacy / Orton-Gillingham is **mandatory, not optional**; **every phonics pattern is taught for BOTH reading (decoding) and spelling (encoding)**; **reading level is decoupled from P-level** (a P5 child may need P1–2 decoding); pacing is **slower with smaller steps and more review**; content is **decodable/controlled** (§6a); UI is **dyslexia-friendly by default** (§14); tone foregrounds **growth over exam pressure** (§10).

**Add-Student is a first-class in-app feature** (not hardcoded profiles): the parent adds a child, sets **chronological P-level**, and the app then runs an **independent reading placement** (§7). Optional difficulty flags captured at add-time tune starting focus.

**Two things Core5's model gives us, kept:** (a) an adaptive engine with continuous embedded assessment ("Assessment Without Testing"), and (b) a blended loop that branches to explicit re-teaching when a child struggles. **What we change:** the *content and assessment* are rebuilt around the MOE EL Syllabus 2020 strands and PSLE Paper components, in **British/Singapore English**, for a **largely bilingual (English + Mother Tongue) learner population**.

**The core loop.**
1. Each child has a profile and a placement on a fixed **P1→P6 scope-and-sequence**.
2. The app presents short, adaptive **activities** using **PSLE-style item types**.
3. Every answer is **continuously scored (deterministic)** → per-skill **mastery estimate** (no separate test).
4. On mastery → advance; on struggle → branch to an explicit **Lesson**, then extra **Practice**.
5. A **parent dashboard** shows progress, an **action plan**, and a **PSLE-readiness indicator** framed on the AL1–AL8 scale.

**The key architectural bet: FULLY OFFLINE, ZERO RUNTIME AI.**
- **No network access and no AI calls at runtime.** All content ships as **static, pre-authored content packs** (JSON + audio assets) bundled with the app.
- Content packs are **authored/pre-generated at BUILD time** (curated word lists for phonics/spelling; AI may assist authoring offline before shipping, but the shipped app is static).
- **All scoring is deterministic** (exact-match / rule-based). No LLM scoring. Free-text comprehension is deferred or MCQ-only.
- **Lessons are pre-written** per skill and shipped in the pack.
- Rationale: the MVP is decoding+spelling, whose item space is finite and enumerable — hardcoded curated lists give guaranteed decodability, correct keys, zero cost, and permanent offline operation on iPad/iPhone.

---

## 2. Non-Goals (scope discipline)

Do **not** build unless later asked:
- **Any runtime network dependency or AI/LLM call.** The app must function fully in airplane mode.
- Multi-tenant accounts, school/teacher admin hierarchies, roles/permissions.
- User authentication / cloud accounts (Phase 1 is local-device only).
- Payment, licensing, or data-compliance infrastructure — personal use.
- **Composition / Continuous & Situational Writing marking** (PSLE Paper 1) — hard to auto-mark reliably; spec'd as a **later, optional** module.
- **Oral (Reading Aloud + Stimulus-based Conversation)** and **Listening Comprehension audio** — require speech I/O; **Phase 3, optional**.
- A pixel clone of Core5. We target a **PSLE-aligned viable core** and expand.

---

## 3. Pedagogical Foundation

Two frameworks combine: **MOE EL Syllabus 2020 / STELLAR 2.0** (the *what* — content, strands, values) and **Structured Literacy** (the *how* — the adaptive/explicit engine, especially for lower-primary foundations).

**Structured Literacy principles → features:**

| Principle | Meaning | Where in app |
|---|---|---|
| **Explicit** | Rules taught directly, not guessed | A skill's lesson fires on FIRST exposure (before any test item), then again on struggle (§8) |
| **Systematic** | Simple → complex, valid order | Fixed P1→P6 scope-and-sequence (§4) |
| **Cumulative** | New builds on prior; constant review | Interleaved review / spaced repetition (§7) |
| **Diagnostic** | Continuously find each child's gaps | Assessment Without Testing (§9) |
| **Responsive** | Adjust in real time | Adaptive engine (§7) |

**Strands (localised).** The MOE syllabus organises learning into Listening & Viewing, Reading & Viewing, Speaking & Representing, Writing & Representing, and **Grammar & Vocabulary** as knowledge about language. For an auto-adaptive app we implement these **six teachable strands**, weighted toward what PSLE Paper 2 actually tests:

1. **Phonics & Word Recognition (decoding)** — sound–letter correspondences, decodable words, high-frequency words. **MVP core.** *Does NOT taper by grade — serve by reading placement, not P-level.*
2. **Spelling (encoding)** — the reverse operation of every phonics pattern: hear word → build/type it. Taught in lockstep with strand 1 (OG: read it AND spell it before mastery). Includes dictation of decodable words/sentences. **MVP core.**
3. **Grammar** — tenses, subject–verb agreement, prepositions, connectors, pronouns, articles, reported speech, verbs of perception, gerunds. *Major PSLE weight; post-MVP emphasis.*
4. **Vocabulary** — word meaning, nuance, phrasal verbs, collocations, idioms, context clues.
5. **Cloze** — grammar cloze (word-bank) and comprehension cloze (open). *Own strand; upper-primary.*
6. **Comprehension** — literal → inferential → evaluative; visual text; open-ended technique.
7. **Sentence Manipulation** — Synthesis & Transformation; Editing (spelling & grammar).

**EAL / bilingual reality.** English is the medium of instruction but often **not the home language** (many children speak Mandarin, Malay, or Tamil at home). Do **not** assume English L1: gloss harder vocabulary, keep instructions simple, and let vocabulary support scaffold meaning.

**Motivation as a feature.** Learner agency + growth mindset; celebrate mastery with certificates carrying **"I can…" statements**; encouraging, non-punitive feedback; short sessions.

---

## 4. Localisation & PSLE Alignment (READ — drives content generation and scoring)

**Language conventions (mandatory in every generated item and in scoring):**
- **British/Singapore English spelling** — colour, favourite, realise, centre, travelled, neighbour. **Never** mark US spellings as the *only* correct form; never generate US spellings as targets.
- **Singapore context** — local names (e.g., Wei Ming, Siti, Kavitha, Mr Tan), settings (hawker centre, MRT, void deck, HDB, wet market, CCA), currency in dollars/cents. Keep culturally neutral and inclusive across races.
- **Register** — "internationally acceptable English that is grammatical and appropriate"; avoid Singlish as a *target answer* (may appear only as an editing/error example to correct).

**PSLE English structure the app builds toward** (from the SEAB PSLE English syllabus; use for component coverage and the readiness indicator, not for exact mark replication):

| Paper | Component | Format | In-app coverage |
|---|---|---|---|
| **P1 Writing (25%)** | Situational + Continuous Writing | open | **Later/optional** (§2) |
| **P2 Language Use & Comprehension (45%)** | Grammar MCQ | MCQ | Grammar strand |
| | Vocabulary MCQ | MCQ | Vocabulary strand |
| | Vocabulary Cloze | MCQ | Vocabulary strand |
| | Visual Text Comprehension | MCQ | Comprehension strand |
| | Grammar Cloze (word bank) | open | Cloze strand |
| | Editing (spelling & grammar) | open | Sentence-manipulation strand |
| | Comprehension Cloze (no word bank) | open | Cloze strand — acceptable-list scored |
| | Synthesis & Transformation | open | Sentence-manipulation — pattern-scored |
| | Comprehension (open-ended) | open | Comprehension — MCQ-adapted |
| **P3 Listening (10%)** | Listening Comprehension | MCQ | **Phase 3** (audio) |
| **P4 Oral (20%)** | Reading Aloud + Conversation | open | **Phase 3** (speech) |

Grading is reported as **Achievement Levels AL1 (best) → AL8**. The readiness indicator (§10) is framed on this scale.

**Support-programme analogy (validates the tiered model).** MOE runs Learning Support Programme (P1–2), Empowered to Read Too (P1–2), and School-based Dyslexia Remediation (P3–4). Our struggle-branch (Lesson → Practice) mirrors this "extra explicit support when stuck" idea.

---

## 5. The Scope & Sequence (fixed backbone)

Hand-authored spine stored as **JSON**, edited without touching logic. Content packs supply items *within* a skill; nothing sets skill order but this file. Levels map to **P1–P6**; skills within a level are ordered and gated by `prereqs`. *(M5 §19: **Learn mode** walks the dual-gated phonics/spelling **patterns** of this sequence in order via `nextToLearn`; this file remains the single source of pattern order for both Learn and Test.)*

```jsonc
// scopeAndSequence.json  (excerpt — expand to full P1–P6)
{
  "levels": [
    {
      "id": "P1", "name": "Primary 1", "ageBand": "6-7",
      "skills": [
        {
          "id": "PH-short-vowels",
          "strand": "phonics",
          "objective": "Decode CVC words with short vowels.",
          "iCanStatement": "I can read short-vowel words.",
          "prereqs": [],
          "itemType": "decode_choice",
          "itemPool": "phonics-L01-cvc",
          "mastery": { "window": 12, "accuracyToPass": 0.85, "minItems": 10 }
        },
        {
          "id": "GR-articles",
          "strand": "grammar",
          "objective": "Choose a / an / the correctly.",
          "iCanStatement": "I can use a, an and the correctly.",
          "prereqs": [],
          "itemType": "grammar_mcq",
          "itemPool": "grammar-L01-articles",
          "mastery": { "window": 12, "accuracyToPass": 0.8, "minItems": 10 }
        }
      ]
    }
    // P2 ... P6 (increasing complexity; PSLE-component skills concentrate in P4–P6)
  ]
}
```

**Suggested progression by strand (compress/expand as needed):**
- **Phonics/decoding (MVP):** letter–sounds → short vowels (CVC) → digraphs (sh, ch, th, ck) → blends (initial → final) → FLOSS/doubling → long vowels (silent-e) → vowel teams → r-controlled → diphthongs → 2-syllable words (syllable division) → prefixes/suffixes. High-frequency ("tricky") word sets threaded throughout.
- **Spelling/encoding (MVP):** mirrors phonics 1:1 — each pattern gets `build_word` (tiles) → `spell_tiles` → `dictation` (tiles) items. Mastery of a pattern requires BOTH decode and encode gates (§7).
- **Grammar (P1→P6):** articles → nouns/pronouns → subject–verb agreement (simple → neither/nor, collective) → tenses → prepositions → connectors → reported speech → perception verbs, gerunds, modals.
- **Vocabulary:** picture–word → synonyms/antonyms → shades of meaning → phrasal verbs → collocations → idioms → contextual choice.
- **Cloze (P3→P6):** grammar cloze (word bank) → comprehension cloze (short) → PSLE-length.
- **Comprehension:** sentence meaning → literal → inference → cause/effect → evaluative → visual text → open-ended technique. *Passages decodability-constrained (§6a).*
- **Sentence manipulation (P3→P6):** editing (spelling) → editing (grammar) → synthesis & transformation.

**Authoring rule:** ship a complete sequence for **Phonics + Spelling** first (the Phase-1 MVP core), with a starter Grammar/Vocab track for variety. PSLE Paper-2 strands (cloze, comprehension, synthesis) follow in M3 — they are unreachable for a child who cannot yet decode them.

---

## 6. Content Model (item types)

Each skill declares an `itemType`. Renderers to implement:

| itemType | Renders | Answer | Scoring |
|---|---|---|---|
| `decode_choice` | Word + audio "read this" + choices, or match word to picture | tap | exact |
| `sight_word_flash` | Word shown briefly; pick from choices | tap, timed | accuracy + latency |
| `grammar_mcq` | Sentence with 4 options (the PSLE grammar MCQ) | tap | exact |
| `vocab_mcq` | Sentence; pick best-meaning word / closest synonym of an underlined word | tap | exact |
| `vocab_cloze_mcq` | Short passage, blanks, MCQ options | tap per blank | exact |
| `grammar_cloze` | Passage with blanks + a **lettered word bank**, each used once | assign words | exact |
| `editing` | Passage with underlined errors; supply correction | short text | exact vs authored key |
| `comprehension_cloze` | Passage, blanks, **no word bank** | short text per blank | matches authored `acceptable[]` lists |
| `synthesis_transform` | Rewrite/combine sentence(s) in required form | text | matches authored `acceptable[]` patterns (deferred if too brittle) |
| `visual_text` | Generated poster/advert description + question | MCQ | exact |
| `passage_question` | Short passage + question | **MCQ only** (free-text deferred — no AI scorer) | exact |
| `build_word` | Hear word (audio) → drag letter/grapheme tiles to spell it | ordered tiles | exact + **error analysis** (which grapheme missed → missedConcept) |
| `spell_tiles` | Hear word → build from letter/grapheme tile tray | ordered tiles | exact + error analysis |
| `dictation` | Hear short decodable sentence → build word-by-word from tile trays | ordered tiles per word | per-word scoring; rule-based error analysis |
| `pa_blend` | Hear separated phonemes in sequence (🔊) → pick the blended word | tap | exact (phonemic awareness, §3; Learn-only) |
| `pa_count` | Hear a word → pick how many sounds it has | tap | exact (phonemic awareness, §3; Learn-only) |

### 6a. Decodability constraint (mandatory, enforced at AUTHORING time)
Every word/sentence in a skill's item pool must be decodable using only patterns taught at or before that skill, plus that skill's allowed high-frequency word list. Enforced by a **build-time lint script** that parses each word against the taught-pattern set and fails the build on violations. Each item carries `decodableWithin: skillId` metadata.

### 6b. Content pack QA (build-time, replaces runtime validator)
A build script validates every pack: (1) answer keys resolve, (2) exactly one correct answer per item, (3) en-SG spelling (dictionary check), (4) decodability lint (§6a), (5) no duplicate item IDs, (6) minimum pool size per skill (≥20 items/difficulty where feasible). CI-style: build fails on any violation.

### 6c. Audio strategy (fully offline)
iOS `speechSynthesis` with an **on-device en-GB voice** for words/sentences — works offline. Isolated phonemes (/sh/, /a/) cannot be TTS'd reliably: bundle a **pre-recorded phoneme set (~44 clips)** as static assets. `audio.phoneme(id)` plays clips; `audio.speak(text)` uses TTS. One service interface; assets cached by the service worker.

### 6d. Item pools & variety (replaces infinite generation)
Each skill ships a **pool of 20–40 hand-curated items per difficulty**. Runtime variety via: shuffle, distractor rotation (distractor sets stored per item, sampled), and never repeating an item within its pool cycle. Pool exhaustion recycles with reshuffle — acceptable given spaced repetition spreads exposure.

> **Owner directive (2026-07-20) — MAX OUT EVERY PHONICS PACK.** For all phonics/spelling packs (T01–T12: CVC, digraphs, **blends**, FLOSS, silent-e, vowel teams, r-controlled, diphthongs, two-syllable, HF sets), author to the **quality ceiling** of that pattern (as many decodable, en-SG, child-safe words as the pattern cleanly yields — typically ~60 items/skill), not merely the §6d floor. Applies to every future pack **when building blends and beyond** — always go to max before requesting owner sign-off. Precedent: CVC 60, digraphs 60.

**Multisensory (OG requirement):** pair sound + visual + motor (tile drag, typing) for every phonics/spelling pattern.

---

## 7. Adaptive Engine

> **M5 cross-ref (§19).** This engine now powers **Test mode only**. Teaching (the explicit-first intro lesson and the struggle→lesson re-teach) has moved to **Learn mode**; in Test, `eligibleSkills` is **learned-gated** (only patterns taught in Learn are assessed) and struggle **flags the pattern needs-review** (for Learn to resurface) instead of re-teaching. Everything below — mastery, difficulty, dual gate, retention/SRS, interleave/fluency, down-shift — is unchanged and still applies within Test.

**Mastery model.** Rolling-window accuracy per (child, skill), gated by `minItems`. (Interface kept clean so a Bayesian/BKT upgrade can drop in later.)

**Difficulty (1–3) within a skill.** Start at 1. Correct streak of 3 → +1 (cap 3); 2 wrong in recent window → −1 (floor 1). Difficulty flows into generation (harder distractors, subtler nuance, longer/lower-frequency vocabulary, PSLE-length passages at level 3).

**Dual mastery gates (phonics patterns).** A pattern is mastered only when BOTH its decode skill and its encode (spelling) skill pass. Encode unlocks after decode reaches ~70% over **≥6 real items** (`ENCODE_UNLOCK_MIN`, so a 3/3 fluke doesn't open spelling early), but advancement requires both. **Massed practice first:** threading + interleave are suppressed until the child's first pattern is acquired. `minItems` for foundational decode/encode is **12** (overlearning for struggling readers; 85%×12 ≈ 11/12).

**Mastery = RETENTION, not acquisition (OG, 2026-07-21).** Passing the dual gate in a session is **PROVISIONAL mastery**: it advances the ladder (keeps momentum) but the **certificate is WITHHELD** until the **first +2d spaced-review pass** confirms retention. So progress `status:'mastered'` = acquisition (drives advancement + the dashboard growth count); a **certificate = retained** (minted on the confirming review). Prevents the "certify now, maybe demote later" false-positive.

**Spaced repetition (replaces vague "review %").** On acquisition, schedule reviews at **+2d, +7d, +21d** for **BOTH the decode and encode skills** of the pattern. Session composition: due reviews first (cap ~4/session), then current-skill items. The certificate is minted only once **both channels have retained** (each review stage ≥1 — so spelling retention is verified, not assumed). A **failed review truly demotes**: it re-books the near review AND fires an immediate guided re-practice block (3 difficulty-1 items on the forgotten skill), not just a re-test in 2 days. Post-M4: tune intervals.

**Error taxonomy (missedConcept).** Fixed enum per strand — e.g. phonics: `digraph-sh`, `vowel-short-a`, `consonant-b` (b/d reversal), `r-controlled-ar`; spelling: `silent-e-a`, `vowel-team-choice`; grammar: `sva-neither-nor`. Drives struggle detection, Lesson targeting, and dashboard "stuck on" reporting. **Grapheme-level (2026-07-21):** for encode items, `scoring.tileMissedConcept` derives the tag from the EXPECTED grapheme at the first same-length SUBSTITUTION via `src/data/graphemeConcepts.json` (`gen-grapheme-concepts.mjs`), so a vowel confusion / digraph swap / b-d reversal is diagnosed precisely; length-mismatch errors (omission/insertion, e.g. a dropped silent-e) fall back to the item's authored `missedConceptOnFail`. MCQ/decode items keep their authored tag.

**Progression pseudocode:**
```
onAnswer(child, skill, item, result):
  record(result)                              # feeds AWT + mastery
  updateDifficulty(child, skill, result)
  m = rollingAccuracy(child, skill, window)
  n = itemsAnswered(child, skill)
  if n >= skill.mastery.minItems and m >= skill.mastery.accuracyToPass:
      markProvisionalMastery(child, skill)    # advances the ladder now…
      scheduleReview(child, skill, +2d)       # …cert WITHHELD until this review passes (§ retention)
      advance(child, nextUnlockedSkill(child))   # respects prereqs
  elif strugglingSignal(child, skill):        # m < 0.6 over >= 5 items, or 2 same-concept misses
      branchToLesson(child, skill)            # may re-fire once more (cap 2/skill/session) if still failing
  else:
      continuePractice(child, skill)
```

**Struggle branching (blended-model equivalent):** re-teach EARLY to minimise error exposure — Lesson (explicit rule + worked examples + guided items) → Practice (easier extra items) → resume. The lesson may re-fire **once more** (cap 2/skill/session, after ≥3 further failed attempts) if the child is still struggling.

**Explicit-first teaching (§3, 2026-07-22).** The FIRST time the child meets a new pattern (no prior attempts on that skill, not placement-credited), its lesson is shown **before** any test item — teach the rule, don't make them discover it. The shared lesson→guided-block(we-do)→normal-items(you-do) flow then gives the full gradual release. A per-skill `introRef` guards it (fires once, and never consumes the struggle re-teach budget); the reactive struggle→lesson branch still fires later if needed.

**Immediate error correction (OG, 2026-07-21).** At the item level, a WRONG answer is scored (assessment) but does NOT end the item: every renderer reveals + **voices** the correct answer and requires the child to **reproduce it correctly** (tap the right choice / rebuild the modelled word / re-place the right cloze word) before continuing — so the last production is correct, not the error (avoids consolidating the wrong form). `onAnswer` is deferred until the fix, carrying the first attempt, so score + correct-only fluency latency are unaffected; the placement warm-up (`quiet`) stays assessment-only.

**Placement (dual — critical for this audience).** Capture **chronological P-level** at add-time (sets PSLE-component expectations), then run an **independent reading placement** that finds true instructional level per strand. For struggling readers this diverges from chronological P-level — place content by **reading level, not P-level**. **Checkpoint + refine (2026-07-22):** the decode ladder is now 20 dual-gated rungs, too many for a linear every-rung walk within a short warm-up — so placement runs a **coarse** pass that probes rungs `STRIDE=4` apart (`PER_COARSE=2` each) to find the child's BAND, then a **fine** pass that walks that band rung-by-rung from the low end (`PER_SKILL=3`, the conservative decision-maker) to pin the entry. This reaches a fluent reader's true level in ~12 items yet stays start-low for a weak one (`MAX_ITEMS=22`). Passing **every** checkpoint doesn't shortcut to the top — the **top band** (2nd-last checkpoint → top) is still refined rung-by-rung so the top rungs are directly confirmed before being credited (no crediting of untested rungs on an all-pass; lower between-checkpoint crediting is intrinsic to checkpointing and caught by the Test struggle→needs-review→Learn net). Both PERs are **flag-tuned** (§1): `PER_SKILL` 3→4 and `PER_COARSE` 2→3 for dyslexia/decoding, so a weaker reader can't fluke a checkpoint and jump a band. Low-pressure: **frame it as a warm-up game, show no right/wrong feedback during placement**, end on an achievable item (`MIN_WARMUP=6`). **Spelling is NOT free-placed (2026-07-21):** the warm-up only tests decoding, so a high placement credits the lower rungs' decode+encode but **holds back the top TWO read rungs' spelling** — the child must earn those in-session before advancing (`encodeUnlocked` treats a placement-confirmed decoder as ≥70%). Spelling lags decoding in dyslexia, so a decode-only placement never assumes more than two rungs of spelling. The fine pass needs **3/3 per rung** so lucky guesses can't over-place. Because a high placement can unlock many skills at once, a Test session concentrates on the lowest **`FOCUS_WIDTH=3`** eligible skills rather than scattering across the whole unlocked curriculum.

**Interleaving:** ~15–20% of session items are quick reviews of mastered skills (cumulative principle).

---

## 8. Lessons (explicit re-teaching)

**Pre-written per skill, shipped in the content pack** (finite: ~40–60 skills). Served on struggle. Contract:
```ts
type Lesson = {
  iCanStatement: string;
  explanation: string;              // plain, kid-friendly rule; en-SG
  workedExamples: { text: string; note: string }[];
  guidedItems: GeneratedItem[];     // 2–3 scaffolded, easier items
};
```
Example (subject–verb agreement, "neither…nor"): rule stated simply → worked examples showing the verb agrees with the nearer noun → guided items.

---

## 9. Assessment Without Testing (AWT)

No separate test — every attempt is an assessment event:
```ts
type Attempt = {
  childId: string; skillId: string; itemId: string;
  correct: boolean; partial: boolean; latencyMs: number;
  difficulty: 1|2|3; missedConcept?: string; ts: number;
  review?: boolean; // true = non-assessment rep (interleave/thread/fluency/SRS-review/guided/
                    // down-shift). EXCLUDED from headline accuracy/readiness so easy reps don't
                    // inflate them; per-skill mastery still counts every attempt. Legacy = assessment.
};
```
The `Attempt` stream feeds mastery (§7), the readiness indicator (§10), the action plan (§11), and struggle detection. Persist every attempt.

---

## 10. PSLE-Readiness Indicator

Transparent, non-alarming, framed on **AL1–AL8** (not a verdict on the child):
```
inputs: pace (skills mastered / active week),
        recentAccuracy (last ~30 ASSESSMENT attempts — excludes non-assessment reps:
                        interleave/thread/fluency/SRS-review/guided/down-shift, tagged
                        Attempt.review; null when no assessment data yet → shown as "—"),
        coverage (% of current-level PSLE-component skills mastered)

status = On-Target (green) | Some-Risk (amber) | High-Risk (red)   # traffic-light, mirrors Core5
projectedBand = coarse AL estimate from coverage + accuracy across P2 components
```
Show status + **one recommended action** (e.g. "focus grammar cloze — 3 sessions this week"). **For literacy-difficulty learners, foreground GROWTH (skills mastered, accuracy trend, weeks of progress) over the projected AL band** — the band is secondary context for the parent, never shown to the child. Keep honest and supportive.

---

## 11. Data Model & Persistence

**Phase 1:** **IndexedDB** (idb wrapper), per-device. Content packs are static bundle assets (not stored in DB).
```
schemaVersion                 → number                 (migration stub runs on load if < current)
profiles:index                → string[] childIds
profile:{childId}             → Child
progress:{childId}            → { [skillId]: SkillProgress }
attempts:{childId}            → Attempt[]              (raw, capped; roll off oldest, <5MB)
aggregates:{childId}          → per-skill/per-ISO-week rollups {items,correct,minutes} — NEVER rolled off; feeds trend charts
daily:{childId}               → per-calendar-day rollups {items,correct,minutes} (DB v5) — feeds the daily trend view
learn:{childId}               → per-pattern LearnState {patternId,learned,needsReview,…} (DB v6, §19.4) — M5 Learn/Test
usage:{childId}               → { weeklySessionTarget:number, sessionsThisWeek:number, streakWeeks:number }
reviews:{childId}             → { [skillId]: nextReviewTs }
itemState:{childId}           → per-item exposure history (for no-repeat cycling, §6d)
certificates:{childId}        → Certificate[]
settings                      → { ttsRate, englishVariant:"en-SG", sessionLength }
```
```ts
type Child = { id:string; name:string; pLevel:1|2|3|4|5|6;   // chronological
  entrySkillId:string;                                       // from reading placement (may be << pLevel)
  difficultyFlags?:("decoding"|"fluency"|"vocab"|"comprehension"|"dyslexia")[];
  createdAt:number };
type SkillProgress = { skillId:string; status:"locked"|"active"|"mastered";
  itemsAnswered:number; rollingAccuracy:number; difficulty:1|2|3; lastSeen:number; masteredAt?:number };
type Certificate = { skillId:string; iCanStatement:string; awardedAt:number };
```
Provide **export/import** (JSON) since Phase-1 storage is device-bound.

---

## 12. Content Pack Spec (replaces runtime LLM)

**Packs are static JSON shipped with the app.** One pack per strand-level (e.g. `phonics-L03-digraphs.json`).

```ts
type ContentPack = {
  packId:string; strand:Strand; skillIds:string[]; version:number;
  items: PackItem[];                 // pre-authored, QA'd (§6b)
  lessons: { [skillId:string]: Lesson };  // pre-written explicit lessons
};
type PackItem = {
  id:string; skillId:string; itemType:ItemType; difficulty:1|2|3;
  stem:string; passage?:string; displayWord?:string;
  audioText?:string;                 // what TTS speaks (or phonemeId)
  choices?:{id:string;label:string}[];
  distractorPool?:string[];          // rotated at runtime for variety
  wordBank?:string[]; blanks?:{id:string;acceptable:string[]}[];
  correctChoiceId?:string; acceptableAnswers?:string[];
  missedConceptOnFail:string;        // from error taxonomy enum
  decodableWithin:string;            // skillId envelope (§6a)
  rationale:string;                  // shown as feedback/teaching note
};
```

**Authoring pipeline (build time, not runtime):**
1. Curated word lists per phonics/spelling pattern (source: standard OG/decodable lists, adapted to en-SG spelling).
2. Items assembled by script from word lists (decode_choice, build_word, spell_typed, dictation are template-driven — cheap to mass-produce deterministically).
3. Grammar/vocab MCQ and later-strand passages: authored with AI assistance **offline during development**, then human-reviewed, then frozen into packs.
4. §6b QA lint gates every pack into the build.
5. **Owner review gate:** every pack is delivered to the owner as readable JSON (+ a human-readable word-list summary) for review/edit BEFORE freezing into the app bundle. No pack ships unreviewed.

**Scoring (all deterministic):** exact match for MCQ/tiles; tile-sequence exact match for spelling; per-word tile match for dictation; cloze blanks match `acceptable[]` lists (author multiple acceptable answers where genuinely valid). Error analysis = rule-based: compare wrong grapheme/word against `missedConceptOnFail` + positional diff.


---

## 13. Architecture

**Distribution & updates (resolved).**
- **Hosting:** GitHub repo **`sg-en-reader-tiles`** → **GitHub Pages** (deploy from `main` via GitHub Actions on every push; Vite `base:'/sg-en-reader-tiles/'`; URL `https://<user>.github.io/sg-en-reader-tiles/`). Repo not yet created — README documents setup: create repo → push → Settings→Pages→Source: GitHub Actions. Workflow: all work merges to `main`; push = deploy.
- **Netlify (or any root-domain host) — supported alongside Pages.** The **`base` is environment-driven** in `vite.config.ts`: `process.env.BASE_PATH ?? (NETLIFY ? '/' : '/sg-en-reader-tiles/')` (start_url/scope follow it). GitHub Pages serves under the repo-name subpath, so base must be `/sg-en-reader-tiles/`; Netlify serves from **root `/`**, so a Pages build there loads assets from `/sg-en-reader-tiles/assets/…` → **blank screen**. Netlify sets `NETLIFY=true` in its build env, so the same repo auto-builds with `base:'/'` there (override anywhere with `BASE_PATH`). `netlify.toml` pins `command="npm run build"`, `publish="dist"`, `NODE_VERSION=20`, and an SPA `/* → /index.html 200` fallback. **To deploy on Netlify:** link the repo → it reads `netlify.toml` → done (no dashboard build-setting changes needed).
- **Install:** open Pages URL in Safari on iPad/iPhone → **Add to Home Screen** → runs standalone, fully offline thereafter (service worker precaches shell + packs + audio).
- **Updates:** service-worker update flow — on load, `registration.update()`; when a new SW reaches `waiting`, show a **toast: "Update available — tap to refresh"**; tap → `postMessage({type:'SKIP_WAITING'})` → SW `skipWaiting()` → `controllerchange` → `location.reload()`. Never auto-reload mid-session (could lose an active session); toast persists until acted on. Also check for updates on `visibilitychange` (app foregrounded). Version string (from package.json) shown in parent dashboard footer for verification.
- **Content pack updates ride the same flow** (packs are precached assets; new SW = new packs).
- iOS note: if the PWA is unused for extended periods Safari may evict some storage — IndexedDB data survives in practice for installed PWAs, but the **export/import backup (§11) is the safety net**; remind parents to export periodically via a dashboard nudge.

**Phase 1 — offline-first PWA (BUILD FIRST).** Vite + React PWA. **Targets iPad/iPhone Safari**: add-to-home-screen, service worker precaches app shell + all content packs + phoneme audio → fully functional in airplane mode. Storage: **IndexedDB** (via idb wrapper). TTS: on-device `speechSynthesis` en-GB. No backend, no auth, no network calls ever. Touch-first UI (large tiles/targets); **ALL spelling input via on-screen tiles — no system keyboard anywhere** (uniform iPad/iPhone UX, no autocorrect interference, larger dyslexia-friendly targets). Tile tray = target graphemes + distractor tiles scaled by difficulty. **Granularity: GRAPHEME tiles** — digraphs/vowel teams/r-controlled units are single tiles ("sh", "ck", "ea", "ar"), reinforcing OG chunking. Each PackItem stores `graphemes:string[]` (the tile segmentation, e.g. "ship"→["sh","i","p"]); distractor tiles drawn from confusable graphemes ("ch" vs "sh", "ee" vs "ea") defined in the error taxonomy.

```
/domain   scopeAndSequence.json · mastery.ts · predictor.ts · placement.ts
/content  llm.ts · prompts/ · itemCache.ts · audio.ts
/state    storage.ts · profiles.ts · progress.ts · attempts.ts
/ui       ChildPicker · Placement · SessionRunner ·
          items/ (one renderer per itemType, §6) · LessonView · Certificate · ParentDashboard
/app      App.tsx (state machine / routing)
```

**Phase 2 — content expansion.** More packs (PSLE Paper-2 strands), better audio (recorded words for dictation where TTS quality falls short), multi-device export/import.

**Phase 3 — optional enrichments (may reintroduce connectivity as OPT-IN only).** Listening comprehension audio packs; oral reading via on-device speech recognition if feasible; nothing may break core offline operation.

---

## 14. UI / UX

> **M5 cross-ref (§19), M6-updated (§20.2).** Tapping a child on the picker (**🚀 Play**) opens THEIR **galaxy hub** (`GalaxyMap`) — planets = patterns; tap a planet → **Learn** if new/needs-review, **Test/mission** if learned; Trophies + Sound wall live in the galaxy header. (Pre-M6 this was per-child 📘 Learn / 🎮 Test / 🏆 Trophies buttons + a tappable Learn map; the galaxy replaced that map.) Planet status = `patternStatus` (not-started/learning/learned/mastered/needs-review); the active planet (needs-review first, else the frontier) and every earlier one are tappable, later ones are locked. **Learn** then walks a read+spell teaching unit for the tapped pattern; **Test** is the assessment runner (learned-gated, no teaching — routes to Learn via a "learn first" empty state when nothing is eligible).

**Child-facing:** big, low-clutter, audio-assisted for lower primary. Child picker (avatar tiles; each card has **📘 Learn / 🎮 Test / 🏆 Trophies**) → Home (Play, strand chooser of *unlocked* strands, XP/streak, nearest certificate) → **Trophy room** (child-facing: level + XP bar, 6 badges earned/locked, earned "I can…" certificates — reachable from the picker and from the session summary; the session summary also highlights awards earned *that* session) → Session runner (one item at a time, large tap targets, immediate feedback, session progress dots, prompts read aloud) → Lesson view (visually distinct "we're learning something new") → Certificate (celebratory, savable/printable).

**Parent dashboard (PIN-gated):** per-child card (current reading level vs P-level, readiness traffic-light, **"N of M sessions this week" + weekly streak** — fidelity mechanic, mirrors Core5 usage targets; per-child `weeklySessionTarget`, default 4); **action plan** (one recommended action, stuck-on error tags, recent certificates); **trend chart with a Daily / Weekly / Monthly / Yearly toggle** (`aggregate.summarise`; weekly/monthly/yearly from the never-truncated `aggregates`, daily from the `daily` store); a **student dropdown** (when >1 child) to focus the dashboard on one student; per-child **Reset** + **Remove** (two-tap confirm); export/import backup.

**Add-Student flow:** from Child picker → "Add student" → enter name + chronological P-level + **optional difficulty flags (decoding/fluency/vocab/comprehension/dyslexia — built, stored on `Child.difficultyFlags`, shown as chips on the teacher dashboard, and NOW driving pacing via `src/lib/support.ts` — dyslexia/decoding = more review + gentler difficulty + more conservative placement; fluency = earlier automaticity practice)** → run reading placement → profile created. **Manage (Teacher area):** student management lives in the PIN-gated Teacher dashboard (NOT the child-facing home screen) — per-child **Reset** (wipe progress/attempts/certs/reviews, clear entry skill, re-run placement) and **Remove** (delete profile + all data, inline two-tap confirm, no native dialogs). The home screen (ChildPicker) only launches Learn / Test / Trophies + Add student.

**Constraints — dyslexia-friendly by DEFAULT (not opt-in):** OpenDyslexic/sans-serif font, generous letter/line spacing, off-white background, high contrast, large tap targets, short lines, audio-first prompts, **no timing pressure by default** (sight-word timing is opt-in only), reduced-motion, adjustable TTS rate/session length. No ads, no external links, nothing unsuitable for children. Feedback is encouraging and never failure-framed; celebrate small wins.

---

## 15. Roadmap (build in order)

**M0 — Skeleton.** Shell, **add-student flow + profiles** + storage, TTS wrapper, one `grammar_mcq` item end-to-end. *Accept:* add a student with a P-level, answer one item, feedback shown, attempt persisted.

**M1 — MVP (decoding + spelling engine).** Full phonics + spelling scope-and-sequence with dual mastery gates; phoneme audio assets; `decode_choice`/`build_word`/`spell_tiles`/`dictation` tile renderers; content packs (curated word lists, §6b build-lint); placement (warm-up framing); struggle→Lesson→Practice; spaced repetition; certificates. *Accept:* a struggling reader is placed below P-level, works adaptively through patterns needing BOTH decode+encode to advance, gets re-taught on struggle, earns a certificate — fully offline.

**M2 — Dashboard + readiness. ✅ MET (2026-07-20).** AWT analytics, aggregates, usage targets/streak, action plan with error-tag reporting, AL-framed readiness (parent-only), export/import, schemaVersion migration stub (DB v4). *Accept:* accurate per-child status + one useful action — met: `ParentDashboard` (PIN-gated) shows growth stats + traffic-light status + a single next-step action from stuck-on tags, backed by `readiness.ts`/`aggregate.ts` and full-DB export/import; `npm test` drives the flow. *Note:* readiness coverage is over the decode+spell foundation only until M3 adds PSLE Paper-2 skills, so the AL band is a labelled early estimate.

**M3 — PSLE Paper-2 strands. ◑ CORE MET (2026-07-20).** Grammar, Vocabulary, Cloze, Comprehension (MCQ + word-bank acceptable-list scoring) shipped as a starter track (level P3, gated behind the decode ladder). `grammar_mcq`/`vocab_mcq`/`passage_question`/`visual_text` via `McqItem` (+passage), `grammar_cloze` via new `ClozeItem`. **Sentence Manipulation — MCQ-adapted forms now built** (T17, 2026-07-20): editing (spot-and-correct) + synthesis & transformation (pick the correct rewrite), strand `sentence`, deterministic/no-keyboard via `McqItem`. **Still open:** free-text synthesis + comprehension-cloze — free-text/no-keyboard, deferred per §2/§6. *Accept (met for the tractable strands):* all playable, validly sequenced behind decoding.

**M4 — Polish. ✅ CORE MET (2026-07-20).** Self-hosted dyslexia font (Lexend default + OpenDyslexic opt-in, `data-font`); parent Settings (font, voice **speed + voice picker**, session length) persisted + applied; gamification (`gamify.ts` — XP/level badge on picker, "+N XP" on summary, **six achievement badges** on the dashboard); background cache pre-warm on `visibilitychange`; a11y (labelled steppers, aria-live, focus-to-screen on view change). **Genuinely deferred:** threshold tuning against real use (needs the family's live data), daily-quest gamification, and `ui.ts` shared modal/toast primitives (no functional gap — dialogs are already non-native).

**M5 — Learn/Test dual-mode (structured learning path). ✅ MET (2026-07-22, v0.2.63) — all four §19.9 phases landed.** Split the app into two per-child modes: **Learn** (a linear, explicit, low-pressure teaching walk through the phonics/spelling scope & sequence) and **Test** (the current adaptive assessment engine, now gated to only assess what's been learned). The two are linked bidirectionally. **Full spec in §19.** *Accept:* a child works a pattern in Learn (read+spell taught together) → it unlocks in Test → Test confirms mastery or flags it back to Learn for re-teaching; placement still sets the Learn frontier; teaching lives in Learn (Test stops teaching). Build in the §19.9 phase order.

**M5.1 — Phoneme sound-intros + Sound wall. ✅ MET (2026-07-22) — all four §19.13 phases done; T20/T21 owner-approved.** All **44 phonemes** get an explicit sound introduction (🔊 clip + keyword + articulation cue), rolled out across the roadmap as the first step of each Learn unit at the pattern where the sound is first taught; each sound introduced once, with "same sound, new spelling" cards later; plus a browsable **Sound wall** reference. Adds `ng/nk` (T20) + `ear/air/ure` (T21) units for the orphan sounds; `schwa`/`zh` deferred. **Full spec in §19.13.**

**M6 — "Cosmic Explorer" gamification & interactivity overhaul. ✅ MET (2026-07-23) — all five §20.9 phases done.** Wrap the (frozen) pedagogy engine in an outer-space adventure: a child-chosen customisable **buddy**, a **unified galaxy-map hub** (planets = patterns; tap → Learn if new, Test/mission if learned), **Star Coins** economy + customisation **shop**, **missions + daily goal + reward chest**, **chunky/cartoony** SVG/CSS art, runtime-synthesised **SFX + gentle music** (ducks under TTS), and an **opt-in Fluency Arcade** — all dyslexia-safe (no core timing, `prefers-reduced-motion` + Calm Mode). **Full spec in §20;** build in the §20.9 phase order (M6.1→M6.5). Pedagogy/behaviour untouched.

**Phase 2/3** per §13 once the family is using it and wants durability / audio / oral.

---

## 16. Open Decisions (confirm with owner)

1. **Exact P-levels of each child** — sets default placement bands.
2. **Number of children** — assume ≤ 6 (affects picker only). Devices: **iPad/iPhone (resolved)**.
3. **Printing** — printable certificates and PSLE-style practice sheets? If yes: Phase-1 print stylesheet + Phase-2 PDF export.
4. **Offline strictness** — how much must work with no connectivity (drives cache sizing)?
5. **Writing (Paper 1)** — in scope later, or leave out entirely?
6. **Oral/Listening (Papers 3–4)** — Phase 3 audio/speech, or out of scope?
7. **Mother-tongue scaffolding** — should vocabulary support optionally gloss in a chosen Mother Tongue, or English-only?

---

## 17. Appendix — worked examples (en-SG)

**A. `grammar_mcq` (subject–verb agreement):**
```json
{ "id":"itm_g12", "stem":"Neither the teacher nor the students ____ aware of the change.",
  "choices":[{"id":"a","label":"was"},{"id":"b","label":"were"},{"id":"c","label":"is"},{"id":"d","label":"are"}],
  "correctChoiceId":"b",
  "rationale":"In 'neither…nor…', the verb agrees with the nearer noun ('students', plural) → 'were'." }
```

**B. `grammar_cloze` (word bank):**
```json
{ "id":"itm_c04", "passage":"Wei Ming went ___ the hawker centre ___ he was hungry.",
  "wordBank":["to","because","although","at"],
  "blanks":[{"id":"1","acceptable":["to"]},{"id":"2","acceptable":["because"]}],
  "rationale":"'to' marks direction; 'because' gives the reason." }
```

**C. `comprehension_cloze` (no word bank):** passage with blanks; child types a word; scored against authored `acceptable[]` lists (author all genuinely valid alternatives, e.g. despite/although family).

**D. Session shape (10–20 min):** ~12–18 items = current-skill items at adaptive difficulty + ~15% interleaved review + (if triggered) one Lesson and guided practice; end on a win + progress summary.

---


---

## 18. Engineering Process (adapted from proven reference template) — LOCKED

**18.0 Owner working conventions (2026-07-20) — LOCKED.** (1) Keep replies **terse, technical, non-verbose** — conserve tokens. (2) **Give suggestions** where useful. (3) **Ask one relevant question at a time** (not batched) unless told to stop. (4) **Always merge to `main`** (feature-branch dev is fine, but every change lands on `main` = deploy). (5) **Always update this CLAUDE.md in the same change** (per §18.1).

**18.1 Living spec.** This CLAUDE.md is canonical. **Every code change updates it in the same change** — features, data model, file/module tables, roadmap checkboxes, ledger ticks, changelog. A code change with a stale CLAUDE.md is incomplete. All work merges to `main` (= deploy).

**18.2 Changelog table.** Every change appends a dated row: what · why · root cause (for fixes) · verification performed · CACHE_VERSION.

| Date | Change | Why | Verification | Cache |
|---|---|---|---|---|
| 2026-07-23 | **Fix — re-entering a finished planet and quitting reverted it (reported "wipes out my progress").** After finishing a planet in Learn (learned), tapping it again opened a Test **mission**; a short/abandoned round could flip the planet back to **needs-review** — reappearing as un-learned and re-routing to Learn. Root cause: Test's `struggling`→`flagReview` demotes a pattern to needs-review after as few as **2 same-concept misses** (the early-intervention branch), so a just-learned planet with almost no history was knocked back by a couple of wrong taps in a round the child didn't even finish; and `patternStatus` ranked **needs-review above mastered**, so even a certified planet could revert. **Fix (three parts, assessment engine untouched — only the M5 needs-review UX):** (1) `Session.advance` now only flags needs-review once the child has ≥`FLAG_MIN_ITEMS`=5 assessment items on the skill (matching `struggling`'s sustained-accuracy branch), so a brief/abandoned round on a just-learned planet no longer demotes it — genuine sustained struggle still flags it for re-teaching (the down-shift to an easier prereq is unaffected); (2) `patternStatus` now ranks **mastered above needs-review** (§7 "mastery = retention" — only a failed SRS review demotes a certified skill), so a mastered planet never reverts on the galaxy; (3) `GalaxyMap` picks the re-learn target from the SHOWN status, so a mastered-but-flagged pattern is never chosen as a re-learn target. `learned` was already sticky (never cleared on Learn entry), so no data was ever lost — only the displayed status/route reverted. Smoke: the full-wrong struggle path still reaches ≥5 items → still flags needs-review → Learn resurfaces it (loop intact). | Reported: re-opening a finished planet and quitting wiped its progress | `npm test` PASS; `npm run build` green | v0.2.91 |
| 2026-07-23 | **Galaxy wallpaper — full-screen space background (reported).** The galaxy hub's starfield showed as an **inset rounded box** with cream (`var(--paper)`) margins, not a full-screen space scene: `.galaxy-stars` was `position:absolute; inset:0; border-radius:16px; z-index:0` inside the padded, `max-width:560px` `.screen`, so it filled only that inner box. **Fix:** made it a **fixed full-viewport layer behind all content** — `position:fixed; inset:0; width:100vw; height:100vh; z-index:-1` (radius dropped) — so the space gradient + `startStarfield` canvas fill the whole screen edge-to-edge (under the safe-area padding), while `.galaxy-inner` (z-index:1) keeps the content above it. Galaxy-only: `GalaxyMap` unmounts on navigation, so no other view shows it; `.galaxy-inner` text is already light-on-dark. No JS change (canvas still sizes to `clientWidth/Height`, now the viewport). | Reported: "Can the wallpaper fill up the whole screen?" | `npm test` PASS; `npm run build` green | v0.2.90 |
| 2026-07-23 | **Fix — Test/missions unreachable → no stars → daily goal dead (reported).** Root cause (M6.2 regression): the unified galaxy hub made Test/missions reachable ONLY by tapping a **learned** `.planet-test`. A struggling reader placed low has **zero learned patterns → zero mission planets → Test was unreachable**, and since Star Coins are earned in missions and the daily goal counts those coins, the whole reward loop dead-ended ("can't earn stars… what happened to test mode… daily goal doesn't work"). **Fix (two parts):** (1) `GalaxyMap` now shows **explicit primary CTAs** — a **"🚀 Start a mission"** button (Test) whenever any pattern is learned, and a **"📘 Learn/Practise the next planet"** button for the frontier — so Test is always discoverable and reachable (planets remain tappable as a secondary path); (2) **completing a Learn unit now awards `COIN_LEARN`=15 Star Coins** and counts toward the daily goal (`LearnRunner.finish`), so a learn-heavy struggling reader earns stars even before reaching a mission — the reward loop runs in both modes. Smoke: the galaxy must show a `.galaxy-cta` button; the struggle path (learn-only) now earns coins. | Reported: can't earn stars / Test mode gone / daily goal broken | `npm run lint:packs` OK (74 packs, 2424 items); `npm test` PASS; `npm run build` green | v0.2.89 |
| 2026-07-23 | **M6.5 — polish + Fluency Arcade + a11y → M6 COMPLETE (§20.9 Phase 5).** Final gamification phase; engine still frozen. **Fluency Arcade** (`FluencyArcade.tsx`, §20.6) — a **parent-enabled, default-OFF** timed speed game over ALREADY-**mastered** decode patterns only (automaticity is valid there, so timing lives here and ONLY here — never in core lessons); 30s countdown, tap to "zap" words, awards a few cosmetic coins on finish; **records no attempts** (pure game — assessment engine untouched); gated entry (⚡) shows on the galaxy only when the Settings toggle is on; empty-state when nothing's mastered yet. **Buddy reactions** — the child's buddy now appears in the mission screen and reacts to the last answer (idle→cheer on correct, sad on wrong). **Juice** — soft `tap` SFX on every choice/tile tap (`McqItem`/`TileItem`); ambient music (from M6.1) available via the Music toggle. **a11y / reduced-motion audit** — verified every animation (starfield, planet glow, coin pop, buddy bounce, chest wiggle, confetti, goal bar) is disabled under `prefers-reduced-motion` **and** Calm Mode; `Celebration`/`starfield` check the media query in JS and render static/nothing. Settings gains the Fluency-Arcade toggle. Smoke: arcade OFF by default (no galaxy entry); a Calm-Mode CSS-guard check (active-planet animation → none). **Roadmap M6 ✅ MET** — all five §20.9 phases done. | M6 §20.9 Phase 5 — the opt-in speed game + full juice + a11y, closing M6 | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.88 |
| 2026-07-23 | **M6.4 — missions + daily goal + reward chest (§20.9 Phase 4).** The engagement loop that brings kids back. **Missions:** a Test session is now a **mission** — the header shows a **goal bar** that fills as they answer, and the summary is "Mission complete" with a **`RewardChest`** (tap to open → `chest`/coin SFX + `CHEST_BONUS`=25 Star Coins + confetti). **Daily goal + streak:** earn **`DAILY_TARGET`=40** coins in a day to complete the daily goal; completing it awards a **streak bonus** (`streakBonus`=10×streak, cap 50) and increments the **streak** (+1 if yesterday's goal was met, else resets to 1) — all pure in `economy.ts` (`progressGoal`/`rollGoal`/`isNextDay`). The galaxy hub shows a **today's-goal bar + 🔥 streak**; the session summary highlights goal completion. **`Celebration.tsx`** — a brief, skippable confetti burst (re-fires via a numeric trigger) on chest/goal/certificate, **rendering nothing under reduced-motion / Calm Mode**. **Store DB v8→v9:** additive `dailygoal` store (`{childId, day, progress, target, streak, lastGoalDay}`) + `getDailyGoal`/`putDailyGoal`; export/import + `clearChildData` wired. Coins remain cosmetic — the goal/streak/chest never affect difficulty or mastery. Smoke: DB schemaVersion→9; `progressGoal` streak/once-per-day/gap-reset math; the reward chest awards coins on open (polled). | M6 §20.9 Phase 4 — the daily habit loop + mission reward | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.87 |
| 2026-07-23 | **M6.3 — customisable buddy + shop (§20.9 Phase 3).** The child now has a **buddy** they choose + name at add-time and dress up with Star Coins. **`Buddy.tsx`** — a chunky parametric SVG creature (3 characters: robo/alien/blob) with body colour + optional hat + state-driven eyes/mouth (idle/cheer/think/sad/celebrate; bounce animation disabled under reduced-motion / Calm Mode). **`cosmetics.json`** + **`cosmetics.ts`** — static catalogue (3 buddies + 6 colours + 3 hats) with pure lookups (`bodyColour`/`hatValue`); cosmetics are **purely cosmetic — never touch pedagogy**. **Store DB v7→v8:** additive `inventory` store (`{childId, owned[], equipped{colour?,hat?}}`) + `getInventory`/`putInventory`/`buyCosmetic` (deducts coins, adds to owned, idempotent — no double-charge); wired into export/import + `clearChildData`. `Child.buddy = {character,name}` (folded onto Child, no extra store). **`AddStudent`** gains a buddy picker (tap a character + optional buddy name). **`Shop.tsx`** (new `shop` route, reached from the galaxy header 🛍️ / "✨ Customise") — a live Buddy preview + buy (⭐ cost) → own → equip/remove, `coin` SFX on buy. **`GalaxyMap`** shows the child's equipped buddy in the hub. Smoke: DB export schemaVersion→8; the buddy renders in the galaxy; a store-level buy→own(→idempotent)→equip round-trip; add-student now has two inputs so name-fill targets the first. Engine untouched. | M6 §20.9 Phase 3 — ownership + a coin sink that never affects learning | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.86 |
| 2026-07-23 | **M6.2 — unified galaxy-map hub (§20.9 Phase 2).** The child home is now a space **galaxy** (`GalaxyMap.tsx` + animated `starfield.ts` canvas); each of the 20 dual-gated **patterns is a planet**, coloured by `patternStatus` (🪐 new / 🚀 learned=mission / 🏆 mastered / 🛠️ needs-review / 🔒 locked). Tapping a planet **routes by status**: not-started/learning/needs-review → **Learn** (buddy teaches that pattern); learned/mastered → **Test** (mission). The active planet glows; planets past the frontier are locked (the §0.2.81 rule). Reads only pedagogy state (`learn` store + progress) — never writes it (engine frozen). **Navigation restructure:** `ChildPicker` per-child buttons collapse to one **🚀 Play** → the child's galaxy; `App` adds a `galaxy` view + `learnPattern` state; placement now lands in the galaxy; Learn/Test/Trophies/Sound-wall all route back to the galaxy. **`LearnRunner` no longer owns a map** — it takes an `initialPattern` (the tapped planet) and opens that unit directly (falls back to needs-review→frontier); done screen → "Next planet"/"Back to galaxy"; **`LearnMap.tsx` deleted**. The galaxy header carries the `CoinCounter` + 🏆 Trophies + 🔊 Sound wall. Starfield/active-glow respect `prefers-reduced-motion` + Calm Mode. Smoke reworked: placement → galaxy (asserts planets render, active is a learn-planet, later locked); `learnActivePlanet`/`startMission`/`returnToGalaxy` drive both paths; the 430px + M2 blocks return to the picker via the galaxy Back. | M6 §20.9 Phase 2 — the game hub; behaviour identical, only framing/navigation | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.85 |
| 2026-07-23 | **M6.1 — economy + audio + first juice (§20.9 Phase 1).** First code of the gamification layer; pedagogy engine untouched. **Store DB v6→v7:** additive `wallet` object store (`{childId, coins, lifetimeCoins}`), oldVersion-guarded; `getWallet`/`addCoins`; wired into `ALL_STORES` (export/import) + `clearChildData`. **`src/lib/economy.ts` (pure):** `coinsForAnswer(correct, review)` — a correct **assessment** answer = 5 Star Coins, a correct **rep** (`Attempt.review`) = 1, wrong = 0; `COIN_CERT=50` mastery bonus (coins are cosmetic-only, never touch difficulty/mastery). **`src/lib/audio-sfx.ts`:** Web-Audio-**synthesised** SFX (tap/correct/wrong/coin/chest/levelup/rocket — no asset files) + a gentle ambient arpeggio loop; `setSfxEnabled`/`setMusicEnabled`/`setCalm`; `duckMusic`/`unduckMusic` wired into `audio.speak()` so music never competes with TTS; all `try/catch` (silent where no Web Audio). **Settings** gains `sfx`(default on)/`music`(default off)/`calmMode`(default off), applied on boot (`App`) + a `data-calm` root attr; **ParentDashboard** gets the three toggles. **`Session`** now plays a correct/wrong chime + coin sound and awards Star Coins per answer (assessment vs rep) + the cert bonus, showing a **`CoinCounter`** (pops on increase) in the item header. **Dyslexia-safe:** the coin pop is disabled under `prefers-reduced-motion` and Calm Mode; no timing added. Smoke: DB export schemaVersion→7; correct answers accrue wallet coins; wallet in readDb. | M6 §20.9 Phase 1 — visible engagement lift at low risk, engine frozen | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.84 |
| 2026-07-23 | **Spec added — §20 M6 "Cosmic Explorer" gamification & interactivity overhaul, owner-approved design.** Feedback: the app is pedagogically sound but **boring/unengaging** for kids. Full self-contained spec for a presentation/reward/navigation layer that **freezes the pedagogy engine** (no change to scoring/engine/placement/SRS/content/Learn-Test behaviour) and wraps it in an outer-space adventure. Owner Q&A settled every pillar: **buddy + adventure world**, **outer space/planets**, **choose+customise buddy over time**, **effects + gentle music** (runtime Web-Audio synth, ducks under TTS, music off by default), **chunky/cartoony** in-code SVG/CSS art, **missions + daily goal + reward chest**, **no core timers** (opt-in Fluency Arcade only), **individual-only** (no comparison), **unified galaxy-map hub** (planet = pattern; tap → Learn if new, Test/mission if learned). Spec covers the world/economy/buddy/juice model, DB v7 additive stores (`wallet`/`inventory`/`buddy`/`dailygoal` + Settings sfx/music/calm), module add/change list, a 5-phase build order (M6.1 economy+audio+juice → M6.2 galaxy hub → M6.3 buddy+shop → M6.4 missions+chest → M6.5 polish+arcade+a11y), and the invariants-to-preserve (offline, deterministic, dyslexia-safe, cosmetics never affect pedagogy). Roadmap M6 added (§15). **Design-only; no code.** | Owner: "design a plan to gamify … pedagogy stays" (Q&A one-at-a-time) | doc-only; `npm run build` green | v0.2.83 (no code change) |
| 2026-07-23 | **Exhaustive audit — a11y role regression (#1) + attempts storage cap (#2).** Deep pass over the whole runtime (scoring/engine/placement/srs/Session/Learn/store/content) confirmed pedagogical soundness; two actionable fixes. **#1 (a11y regression from v0.2.81):** the tappable Learn-map rows were `<button role="listitem">` — the ARIA override stripped the implicit **button** semantics, so screen readers announced them as list items, hiding the tap affordance. Fixed: open rows are native `<button>`s (no role override, with a descriptive `aria-label`), the container is `role="group"`, locked rows are inert `aria-label`led `<div>`s. **#2 (storage cap, §11/§13):** the `attempts` store was never rolled off (unbounded growth → quota/eviction risk over years). `addAttempt` now caps at `ATTEMPTS_CAP=4000`/child — on crossing, the oldest (by `ts`) overflow rows are deleted in one transaction. Safe: mastery/SRS use rolling windows and the never-truncated `aggregates`/`daily` stores back the trends, so no signal is affected. **#3 (noted, not a defect):** needs-review locking later-learned rows is the requested "lock after active" behaviour. | Owner: "audit … fix everything" — close the a11y regression + bound raw-attempt storage | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.82 |
| 2026-07-23 | **Learn map — tappable lesson rows; removed "Start learning"; Sound wall moved to header (requested).** The Learn map is now the entry point itself: every pattern is a ROW the child taps to open that lesson — the **active** lesson (needs-review pattern, else the frontier) **and every earlier one** are tappable buttons (learn / redo / practise), while lessons **after** the active one are **locked** (🔒, dimmed) until reached. `LearnMap` props change `hasTarget`/`onStart` → `targetId`/`onSelect(patternId)`; rows at index ≤ the target's index are `.lm-row.lm-open` buttons (needs-review row outlined red), later rows are non-interactive `.lm-locked` divs. The bottom **"Start learning"/"Done" buttons are gone**; the **🔊 Sound wall** button moves into the header (top-right, replacing the static "📘 Learn" badge). `LearnRunner.startUnit(patternId)` now begins whichever pattern was tapped (redo supported — `finish` re-`setLearned`s idempotently) instead of only the auto-target. Smoke: `walkLearn` taps `.lm-row.lm-open` (last = active) and asserts no Start button, ≥1 open row, and the header Sound wall button. | Owner: make the map rows clickable (active + previous), lock later ones, drop Start, Sound wall on top | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.81 |
| 2026-07-22 | **Audit round — deliver the sight-word (heart-word) explicit lesson (was authored but never shown).** Traced explicit-instruction delivery: `getLesson` is called only in `LearnRunner`, for a walked PATTERN's decode+encode skills. The threaded/orphan skills — `HF-words`, `HF-spell`, `PH-letter-sounds` — have no `encodePairId`, so they're never in the Learn walk, and M5 removed all teaching from Test → **the heart-word method lessons (read + spell) were authored but never delivered** (only the per-item 💛 heart hint reached the child). Fix (§3 explicit-first): `Session` now shows a threaded skill's lesson **once, at first encounter** (no prior attempts on it, guarded by `introRef`) before serving the item, via a re-added `LessonView` + a `'lesson'` phase. Scoped to threaded skills only — pattern teaching still lives in Learn, so this doesn't reintroduce the M5 Test-teaching that was removed (sight words / letter-sounds live ONLY in Test, so their explicit instruction has no other home). `PH-letter-sounds`' lesson also surfaces once, harmlessly reinforcing the sound-intro cards. Smoke: any Test lesson must be immediately followed by a threaded-skill item (a lesson before a *pattern* item = leaked pattern-teaching → fail); the old `lessons===0` assertion is replaced by this per-lesson check. §19.7 amended. | Owner: "audit … fix everything" — the sight-word explicit lesson was dead content | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.80 |
| 2026-07-22 | **Audit round — connected-text reading in Learn (#1) + sight-word SPELLING (#2).** Closes the two OG-alignment gaps the prior round flagged. **#1 — Learn now ends by reading decodable TEXT, not just words** (the OG lesson arc: sounds → read words → spell words → **read a sentence**). `LearnRunner` gains a `'text'` phase after spelling: for a pattern with a T19 reading pack (`READING_FOR` maps the 8 reading-bearing patterns → their `RD-*` skill), it serves one `passage_question` decodable sentence (via `McqItem`, with the 🔊 sentence-audio support) before marking the pattern learned; patterns without a reading pack (e.g. CVC sub-units 1–3) skip straight to done. The reading packs already existed + are wired. **#2 — sight words are now SPELLED, not just recognised.** New threaded `HF-spell` skill + `spelling-L12-hf` pack (50 items, `gen-hf.mjs`): build each ≤5-letter heart word from letter tiles with the irregular part flagged (`TileItem` now renders the `heart` hint, matching `McqItem`). It threads alongside the HF-words recognition + letter-sounds (3 threaded skills rotating every 4th item), so sight words get an encode channel + ongoing distributed practice. **Deliberately NOT added:** per-word SRS/mastery for sight words — the threaded distributed exposure already provides spaced practice, and per-word scheduling is disproportionate for a home app (documented, not a gap). Smoke: asserts a reading-bearing Learn unit reads a sentence (keyed to the walked pattern), and that `HF-spell` is threaded (8th-item cadence) not eligible. | Owner: "audit … fix everything" — the two OG gaps: word→text transfer in Learn + encode of sight words | `npm run lint:packs` OK (74 packs, 2424 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.79 |
| 2026-07-22 | **Audit round — CRITICAL pack-wiring bug (15 skills unreachable) + inverted dictation sequence + PA extended to digraphs.** **A (critical, §18.11) — packs authored, linted, "signed off", but never imported → unreachable at runtime.** `packs.ts` static-imports each pack; the lint reads the pack *directory* directly, so a pack can pass lint yet be missing from the runtime `PACKS` array — `getPack` returns undefined → `pickItem` returns nothing → `Session` aborts to summary. A new DEV **wiring guard** (`window.__unwiredSkills` = enabled scope skills for which `pickItem` yields nothing; asserted in smoke) found **15 such skills**: dictation L04–L10 (7), **T20 `ng`**, **T21 `ear/air/ure`**, **prefixes**, **c-le** (8) — all authored+signed-off but silently dead. Fixed by importing all 15 packs into `PACKS`; the guard now fails the build if any enabled skill's pack is unwired. **B (pedagogy) — dictation sequence was inverted:** every dictation rung was gated behind `SP-two-syllable`, so CVC sentence-dictation only unlocked after mastering *all* single-syllable spelling. Re-anchored each rung to its own pattern's encode skill (+ chain to the prior rung): `SP-cvc-dictation ⇐ SP-cvc-4`, `SP-digraph-dictation ⇐ [SP-digraphs, SP-cvc-dictation]`, … so dictation reinforces each pattern in step (OG). **C (enhancement) — phonemic awareness now covers digraphs:** new `pa-digraphs` pack (24 items) + `PA-digraphs` (enabled:false) teaching "two letters = ONE sound" (ship = /sh/ /i/ /p/ = 3, not 4); `pa.ts` maps `PH-digraphs → pa-digraphs`, so the digraph Learn unit opens with a segmentation warm-up. | Owner: "audit … fix everything" — the wiring guard surfaced a large latent-content-dead bug beyond the reported dictation case | `npm run lint:packs` OK (73 packs, 2374 items, 0 warnings); `npm test` PASS (adds the runtime wiring guard); `npm run build` green | v0.2.78 |
| 2026-07-22 | **Audit fixes — explicit phonemic awareness (#1) + first-unit variety (#2) + struggle-loop cap (#3).** Whole-system OG/dyslexia audit found the decode+spell pipeline sound; three gaps fixed. **#1 (real gap) — no isolated phonemic-awareness (oral blend/segment) activity.** Every task was grapheme-anchored; PA is the upstream skill for weak/dyslexic readers and an OG strand of its own. Added two audio-only, deterministic, MCQ item types — **`pa_blend`** (hear separated phonemes in sequence → pick the blended word) and **`pa_count`** (hear a word → how many sounds) — rendered via `McqItem` (new `phonemeSeq` field + `audio.phonemeSeq()` sequential clip player). New `pa` strand + `pa-cvc` pack (30 items, satpin CVC, `scripts/gen-pa.mjs`) + `src/lib/pa.ts` (`paItemsFor`). **Learn-only** (§3): `LearnRunner` opens each CVC sub-unit with `PA_N=2` PA items (new `'pa'` phase) before the sound-intro; PA is **not** a Test skill (scope carries `PA-cvc` `enabled:false` only to satisfy lint's per-item/lesson/pool checks — runtime drops it, so it never gates or gets assessed). **#2 — first unit under the variety floor:** `PH-/SP-cvc-1` topped 15→**22** (added decodable satpin plurals naps/pins/taps/tips/pans/tans/sips) → clears the pool<20 warning. **#3 — Test struggle could dominate a session:** once a skill trips `struggling` it's added to `struggledRef` and dropped from the focus set for the rest of the session (kept only if nothing else is eligible), so a stuck child no longer fills the session with prereq down-shifts — re-teaching happens in Learn via the needs-review flag. Lint gains `pa_blend`/`pa_count` key validation + `phonemeSeq` clip resolution. Smoke: `walkLearn` asserts the CVC unit shows a PA warm-up (`sawPA`). | Owner: "audit … fix everything" — close the phonemic-awareness gap + the two minor findings | `npm run lint:packs` OK (72 packs, 2350 items, **0 warnings**); `npm test` PASS; `npm run build` green | v0.2.77 |
| 2026-07-22 | **Pedagogy audit round — Learn cumulative review + flag-tuned session/placement + placement over-credit.** Fresh OG/dyslexia audit (interaction + Learn + the new placement); confirmed the error-correction renderers, dual-gate, grapheme errors, fluency, sound-intros, and satpin CVC split all sound. Four fixes. **#1 placement over-credited untested rungs on an all-pass:** the checkpoint+refine only tested rungs between checkpoints when a checkpoint FAILED, so a near-perfect coarse run placed at the top and credited rungs 17–18 (suffixes/prefixes) as decode-mastered though never directly probed. `coarseState` no longer has an all-pass `top` shortcut — when every checkpoint passes it now refines the **top band** [2nd-last-checkpoint+1, top] rung-by-rung, so the top rungs are DIRECTLY confirmed before crediting (lower-band between-checkpoint crediting is intrinsic to checkpointing + caught by the Test struggle→needs-review→Learn net). **#2 Learn had no cumulative review** (OG "constant review"): a `LearnRunner` unit now OPENS with a `REVIEW_N=1` quick read item from a randomly-chosen previously-learned pattern (new `'review'` phase) before the sound-intro, so prior patterns are revisited during Learn, not only via Test's interleave (skipped when nothing is learned yet). **#3 session length is now flag-tuned** (§14 short sessions): `support.sessionLength` (default 16, dyslexia/decoding **12**); `Session` caps `lenRef` at `min(settings.sessionLength, sup.sessionLength)` so a flagged child gets shorter sittings than the global setting while unflagged children are unchanged. **#4 coarse placement PER is flag-tuned:** `support.placementCoarsePer` (default 2, dyslexia/decoding **3**) so a weaker reader can't fluke a 2/2 checkpoint and jump a band (1/9→~1/27); threaded through `nextPlacement(results, perSkill, perCoarse)` + `Placement`. Smoke adds support-field guards (#3/#4); the good-path `walkLearn` now also exercises the new Learn review item. | Owner: "audit … fix everything" — close the OG cumulative-review gap, flag-tune load/placement, and the placement over-credit the checkpoint rewrite introduced | `npm run build` green; `npm run lint:packs` OK (71 packs, 2306 items, 2 warnings); `npm test` PASS ×3 | v0.2.76 |
| 2026-07-22 | **Audit fixes — placement ceiling (critical) + in-session re-teach route + Learn exposure.** Post-fat-pattern-split audit found three gaps. **#1 CRITICAL — placement could no longer reach a stronger reader.** The decode ladder is now **20 dual-gated rungs** (4 CVC sub-units + 3 fat-pattern splits + ng/r-vowel-teams/suffixes/prefixes/c-le), but `placement.nextPlacement` was a **linear every-rung walk** (PER_SKILL=3 correct at each rung, `MAX_ITEMS=15`) → it topped out at ~rung 5, so a fluent reader was mis-placed near CVC. Rewrote it as **checkpoint + refine** (pure, reconstructable from `results`): a **coarse** pass probes rungs `STRIDE=4` apart (`PER_COARSE=2` each) to find the BAND the child sits in, then a **fine** pass walks that band rung-by-rung from the low end (`PER_SKILL`, the conservative decision-maker) to pin the entry — spanning the full ladder in ~12 items for a strong reader while staying start-low for a weak one (`MAX_ITEMS` 15→22; checkpoints/middle rungs kept disjoint so tallies never overlap). **Fan-out fix (surfaced by #1):** a top placement unlocks many skills at once (M3 + dictation chain + the two held-back spelling rungs); `Session.advance` round-robined **all** eligible skills, which would stall mastery of any one. It now concentrates on the lowest **`FOCUS_WIDTH=3`** eligible skills (no-op at a low placement where few are eligible; matches §7 massed-practice). **#2 — struggle in Test had no in-session route to re-teaching:** on struggle Test flags needs-review + down-shifts, but the child stayed in Test; the **session summary** now surfaces a **"📘 Learn it again"** CTA for the pattern struggled this session (`flaggedPatternRef`) → routes to Learn (which already targets needs-review first). **#3 — Learn "learned" rested on only 3+3 reps:** `LearnRunner` `READ_N`/`SPELL_N` **3→4** for a little more guided exposure before a pattern is marked learned. Smoke's `playSession` extended to drive **dictation + grammar_cloze** items (a high placement now serves them) and its fresh-screen gate broadened to `word-tile`. | Post-split audit "fix everything" — the ladder outgrew the placement algorithm; plus the two moderate gaps | `npm run build` green; `npm test` PASS ×3 (good path now places at the top rung, learns the suffix frontier, Test masters the held-back encode via the focused frontier → retention cert; struggle path unchanged) | v0.2.75 |
| 2026-07-22 | **Split CVC into four OG "satpin" sub-units (owner: "23 sounds, too many at one go").** The CVC floor introduced all **23 single-letter sounds** in one Learn unit. Split into the classic teaching order — **cvc-1 s/a/t/p/i/n → cvc-2 c/k/e/h/r/m/d → cvc-3 g/o/u/l/f/b → cvc-4 j/v/w/x/y/z/qu** — so each Learn unit teaches ~6 sounds and immediately reads/spells real CVC words from the letters taught so far. New **`scripts/gen-cvc.mjs`** authors a curated per-set word list (15/20/24/24) — the old 60-word pack didn't cover the letter-limited early sets — and emits 8 packs (`phonics/spelling-L02a…d`), deleting the old single packs; cumulative `PH-/SP-cvc-1…4` decodability envelopes (old `PH-/SP-cvc-short-vowels` kept for `dictation-L02-cvc`); scope replaces the 2 skills with 8 (floor `PH-cvc-1` prereq `[]`, chain cvc-1→…→cvc-4→digraphs), with `PH-digraphs` + `RD-cvc-sentences` prereqs repointed to `PH-cvc-4`; `sounds.json` repoints the 23 sounds' `firstPattern`/spellings across the four sets (6/6/6/5); `packs.ts` + `M3Demo` (used the deleted pack) + smoke updated (CVC-floor ids → `PH-cvc-1`/`SP-cvc-1`; A2/nextToLearn "next skill" digraphs→`cvc-2`; T19/M5-gate reading unlock → `cvc-4`). `learn.PATTERNS` + `placement.decodeLadder` pick up the sub-units automatically. All four fattest patterns (CVC, vowel-teams, r-controlled, diphthongs) now split. | Owner: the 23-sound CVC Learn unit is far too much at once | `npm run lint:packs` OK (71 packs, 2306 items, 2 warnings = cvc-1 pool 15<20, letter-limited); `npm test` PASS ×3; `npm run build` green | v0.2.74 |
| 2026-07-22 | **Split r-controlled + diphthongs into Learn sub-units (finish the fat-pattern split).** Same recipe as the vowel-teams split, applied to the two next-fattest patterns so each Learn unit teaches ~1–2 new sounds. **r-controlled → `-a` (ar/or) + `-b` (er/ir/ur, all /er/)**; **diphthongs → `-a` (oi/oy/ou/ow — /oy/+/ow/) + `-b` (aw/au + short oo)**. `gen-r-controlled.mjs`/`gen-diphthongs.mjs` reworked to emit 4 packs each (rc 34+34/40+40, di 42+42/33+33), partitioned by team, deleting the old single packs; new `-a`/`-b` decodability envelopes (`-a` = prior + that group's teams, `-b` = cumulative full set; old `PH-r-controlled`/`PH-diphthongs` envelopes kept for the reading packs); scope replaces the 4 skills with 8 (chain vt-b→rc-a→rc-b→di-a→di-b→two-syllable), with `PH-r-vowel-teams`/`PH-two-syllable`/`RD-rcontrolled-sentences`/`RD-diphthong-sentences` prereqs repointed to `rc-b`/`di-b`; `sounds.json` spellings/firstPattern repointed (ar/or→rc-a, er/ir/ur→rc-b, oi/oy/ou/ow→di-a, aw/au/oo→di-b); `packs.ts` updated. `learn.PATTERNS` + `placement.decodeLadder` pick up the sub-units automatically. All three fat patterns (vowel-teams, r-controlled, diphthongs) are now split. | Owner: split the remaining fat patterns too | `npm run lint:packs` OK (65 packs, 2260 items, 0 warnings); `npm test` PASS ×3; `npm run build` green | v0.2.73 |
| 2026-07-22 | **Split vowel-teams into two Learn sub-units (owner: "too many sounds at one go").** The vowel-teams pattern introduced ~5 new sound-intro cards in one Learn unit (the worst offender). Split into **`PH-/SP-vowel-teams-a`** (ai/ay/ee/ea — long a + long e; ~2 sound cards) and **`PH-/SP-vowel-teams-b`** (oa/ow/oo/ew/ue/igh; ~3 cards), each its own dual-gated Test rung + Learn unit, so a session teaches only a few new sounds. `gen-vowel-teams.mjs` now emits 4 packs (32+32 / 40+40) partitioned by team, deletes the old single packs; new `PH-/SP-vowel-teams-a` (silent-e + ai/ay/ee/ea) and `-b` (cumulative full set) decodability envelopes (old `PH-vowel-teams` envelope kept for the reading pack); scope replaces the 2 skills with 4 (chain silent-e→vt-a→vt-b→r-controlled), `PH-r-controlled` + `RD-vowelteam-sentences` prereqs repointed to `vt-b`; `sounds.json` spellings/firstPattern repointed (ai/ay/ee/ea→-a, oa/ow/oo/ew/ue/igh→-b); `packs.ts` + smoke updated. `learn.PATTERNS` + `placement.decodeLadder` pick up the sub-units automatically. **Diphthongs (~4 cards) + r-controlled (~3) not yet split** — milder; can follow the same recipe. | Owner: vowel-teams Learn unit dumps too many new sounds at once | `npm run lint:packs` OK (61 packs, 2262 items, 0 warnings); `npm test` PASS ×3; `npm run build` green | v0.2.72 |
| 2026-07-22 | **Default speech rate 0.9→0.4× (owner request — too fast for learners).** Lowered the app-wide default `ttsRate` to the slow floor (0.4×, the dashboard clamp min) so new/unset profiles hear slower speech by default: `audio.ts` initial `rate`, `store.getSettings` fallback, and every `ParentDashboard` `?? 0.9` display/stepper/settings fallback. Saved per-profile settings are untouched; the Teacher-area stepper still adjusts 0.4–1.6×. | Owner: speech too fast — default to 0.4× | `npm test` PASS; `npm run build` green | v0.2.71 |
| 2026-07-22 | **Netlify deploy support — env-driven base fixes the blank screen (reported).** Owner linked the GitHub repo to Netlify and got a **blank screen**. Root cause: `vite.config.ts` hard-coded `base:'/sg-en-reader-tiles/'` (+ manifest `start_url`/`scope`) for GitHub Pages' repo-name subpath, but Netlify serves from **root `/`**, so every asset URL resolved to `/sg-en-reader-tiles/assets/…` → 404 → nothing renders. **Fix:** `base` is now `process.env.BASE_PATH ?? (process.env.NETLIFY ? '/' : '/sg-en-reader-tiles/')` (start_url/scope follow it) — Netlify sets `NETLIFY=true` in its build, so the same repo auto-builds root-based there while Pages keeps the subpath (override with `BASE_PATH`). Added **`netlify.toml`** (`command="npm run build"`, `publish="dist"`, `NODE_VERSION=20`, SPA `/*→/index.html 200` fallback). Also added a local `declare const process` in the config (no `@types/node` dep) — `tsc -b` type-checks `vite.config.ts` and was erroring on `process`, which had been silently failing the build (dist not regenerated). Verified: default build emits `/sg-en-reader-tiles/assets/…`; `NETLIFY=true` build emits `/assets/…` + `start_url:"/"`. | Reported: Netlify deploy shows a blank screen | default + `NETLIFY=true` builds both green with correct base; `npm test` PASS; `npm run build` green | v0.2.70 |
| 2026-07-22 | **Student management moved off the home screen → Teacher area (requested).** The child-facing `ChildPicker` no longer has a **Manage** toggle (it exposed per-child Reset/Remove on the home screen); it now only launches 📘 Learn / 🎮 Test / 🏆 Trophies + Add student. **Remove student** is added to the PIN-gated **Teacher dashboard** (which already had Reset): a per-child **Remove {name}** button with an inline two-tap confirm ("…and all their progress? This cannot be undone.") → `App.removeStudent` (`removeChild` + `clearChildData`), and the card drops locally so the dashboard updates immediately. `ChildPicker` loses its `onRemove`/`onReset`/`editing`/`confirm` props+state; `ParentDashboard` gains an `onRemove` prop + `confirmRemove` state. Smoke: the old home-screen Manage→Remove block is replaced — the dashboard block now asserts **no "Manage" button on the picker** and drives **Remove Dash → confirm → card gone** inside the Teacher area. | Owner: remove the Manage feature on the home screen; put Remove in the Teacher area | `npm run lint:packs` OK (59 packs, 2262 items); `npm test` PASS; `npm run build` green | v0.2.69 |
| 2026-07-22 | **Accuracy-tracking audit — headline "recent accuracy" was inflated by easy reps + showed 100% on no data (reported "not very accurate").** Full end-to-end audit of the accuracy pipeline. **Verified clean:** all four item renderers record the FIRST attempt only (the OG error-correction reproduce step never double-counts); per-skill `rollingAccuracy`/mastery gates scope to one skill correctly; placement warm-up + Learn-mode practice items are NOT written to the attempts store. **Root cause (the real defect):** the dashboard's cross-skill **"recent accuracy"** (and readiness status/AL band/fluency/stuck-tags) counted the deliberately-easy **non-assessment reps** — interleaved reviews, HF thread, fluency speed-reps, due SRS reviews, post-lesson guided items, and struggle down-shifts to a prerequisite — as if they were assessment. Once a child has any mastery, up to ~half a session is such reps (all difficulty-1 on mastered/prereq skills → almost always correct), so a struggling child's headline accuracy read high. Plus `recentAccuracy` returned **1 (100%)** on zero attempts, flattering a freshly-placed child. **Fix:** new optional `Attempt.review` flag (additive, no migration; legacy rows = assessment) — `Session.loadItem` tags every rep branch (`true`) vs the primary eligible current-skill item (`false`); `readiness.ts` now measures `recentAccuracy`/`fluency`/`stuckTags` over **assessment attempts only**, returns `recentAccuracy:null` + `assessedN` when there's no assessment signal (dashboard shows **"—"**, never 100%), and never flags risk on null. Per-skill mastery, XP, and the activity-volume trend chart are unchanged (the chart is deliberately total activity; its tooltip shows honest raw correct/items). Smoke adds guards: empty → null accuracy; 6 wrong assessment + 30 correct reps stays **High-Risk** with `recentAccuracy:0`, `assessedN:6` (the inflation regression). | Reported: accuracy tracking not accurate — audit + fix | `npm run lint:packs` OK (59 packs, 2262 items); `npm test` PASS (adds rep-exclusion + null-accuracy guards); `npm run build` green | v0.2.68 |
| 2026-07-22 | **Owner sign-off — T20 `ng/nk` + T21 `ear/air/ure` ticked → M5.1 COMPLETE.** Owner: "Sign off." Both orphan-sound units (authored v0.2.66: dual-gated decode+encode packs, envelopes, grapheme-concepts, lessons, scope skills, lint-clean + smoke-green) approved per §12.5 → ledger boxes ticked, §19.13.8 Phase 4 done, roadmap M5.1 marked ✅ MET (all four phases complete). The `ng` + `ear/air/ure` sound rows are now signed-off live content in the Learn sound-intro + Sound wall. No code change. | Owner sign-off of the reviewed T20/T21 content | doc-only; `npm run build` green | v0.2.67 |
| 2026-07-22 | **M5.1 Phase 4 (content) — T20 `ng/nk` + T21 `ear/air/ure` decode+encode units authored (§19.13.5).** The two orphan-sound units that activate the last placed sound rows. **T20** `scripts/gen-ng.mjs` → `phonics/spelling-L14-ng` (**47 decode + 47 encode**): final nasals /ng/ (ring, song, bring, strong) + /ngk/ (bank, think, drink, trunk), `ng`/`nk` as single grapheme tiles, short-vowel-closed ± a taught initial blend/digraph → decodable within the blends envelope + ng/nk; `PH-ng`/`SP-ng` scope skills gated behind `PH-blends` (inserted right after blends in scope order), dual-gated, + read/spell lessons. **T21** `scripts/gen-r-vowel-teams.mjs` → `phonics/spelling-L15-r-vowel-teams` (**23 decode + 23 encode**): r-influenced trigraphs ear (hear/near/clear), air (hair/chair/stair), ure (cure/pure/sure) as single tiles, decodable within the r-controlled envelope + ear/air/ure; `PH-/SP-r-vowel-teams` gated behind `PH-r-controlled` (after r-controlled), dual-gated, + lessons. New `PH-/SP-ng` + `PH-/SP-r-vowel-teams` decodability envelopes; `gen-grapheme-concepts.mjs` extended (`nasal-ng/nk`, `r-vowel-team-ear/air/ure` → 68 graphemes) so encode error-analysis names these confusions. Both units insert into the decode ladder at their pedagogical points (placement/frontier pick them up generically). This makes the `ng` + `ear/air/ure` sound rows live in the Learn sound-intro + Sound wall. Smoke asserts `newSoundsFor('PH-ng')`=ng, `newSoundsFor('PH-r-vowel-teams')`=ear/air/ure, and the scope skills resolve. **Pending owner §12.5 sign-off before ticking T20/T21.** | M5.1 §19.13 Phase 4 — author the orphan-sound content units | `npm run lint:packs` OK (**59 packs, 2262 items, 0 warnings**); `npm test` PASS; `npm run build` green (63 precache) | v0.2.66 |
| 2026-07-22 | **M5.1 Phases 2–3 — sound-intro step in Learn + browsable Sound wall (§19.13 P2–P3).** **Phase 2 (Learn sound-intro):** `LearnRunner` now teaches a pattern's SOUNDS before its read+spell flow — new `'sound'` phase (`soundsRef`/`soundIdx`) built from `newSoundsFor(target)` (each new phoneme, `kind:'new'`) + `newSpellingsFor(target)` (already-known sounds gaining a new grapheme here, `kind:'spelling'`); the card shows the grapheme(s), auto-plays + a 🔊 "Hear the sound" replay, keyword (TTS) + articulation cue for new sounds / "another way to spell it" for new spellings, and "say it with me" — participation only, no scoring; **Next sound → Let's read** into the existing intro→read→spellIntro→spell→done unit. The CVC unit is the foundational sound intro (~23 single-letter sounds). **Phase 3 (Sound wall):** new read-only child-facing `src/features/SoundWall.tsx` — `introducedSounds(learnedSet)` grid of tappable tiles (grapheme + keyword), tap → play clip + panel with keyword + every spelling learned so far (no un-introduced sounds → no spoilers); empty-state nudge. Reachable from the **Learn map** (`LearnMap` gains a "🔊 Sound wall" button) and the **Trophies** room (header button); `App` adds a `soundwall` view + `swReturn` return-target so it returns to whichever screen opened it. New `.sound-card`/`.sound-grapheme`/`.sound-wall`/`.sw-tile` styles. Smoke: `walkLearn` now steps through the sound cards and asserts the CVC unit shows them before reading (`sawSoundCard`). | M5.1 §19.13 Phases 2–3 — surface the sound metadata as explicit teaching + a reference wall | `npm test` PASS (adds sound-card walk + CVC-shows-sounds guard); `npm run build` green (59 precache) | v0.2.65 |
| 2026-07-22 | **M5.1 Phase 1 — phoneme sound metadata (§19.13.8 P1).** New `scripts/gen-sounds.mjs` → `src/data/sounds.json` (44 phoneme rows: id·keyword·articulation·firstPattern·spellings[{grapheme,pattern}]); ids cross-checked 1:1 against the `phonemes.json` clip manifest (42 placed, `schwa`/`zh` deferred with `firstPattern:null`). New pure `src/lib/sounds.ts` — `newSoundsFor(pattern)` (sounds first taught here), `newSpellingsFor(pattern)` ("same sound, new spelling" links), `introducedSounds(learned)` (Sound-wall set, spellings filtered to learned patterns — no spoilers). DEV `window.__sounds`; smoke adds M5.1 invariants (44 rows, digraphs→sh/ch/th, blends→none, vowel-teams = new /ai/ spelling, introduced-follows-learned). No UI yet. | M5.1 §19.13 Phase 1 — the sound dataset + pure lookups | `npm run lint:packs` OK (44 packs, 1869 items); `npm test` PASS; `npm run build` green | v0.2.64 |
| 2026-07-22 | **Spec added — §19.13 M5.1 phoneme sound-intros + Sound wall, owner-approved design.** Self-contained spec for introducing all **44 phonemes** explicitly: each sound taught once as the first step of its Learn unit (🔊 clip + keyword + articulation cue), rolled out at the pattern where it's first taught, with "same sound, new spelling" cards later; plus a browsable child-facing **Sound wall** of sounds learned so far (derived from the `learn` store). Includes the full 44→pattern insertion map, the `sounds.json` data model + `sounds.ts` pure lookups, a 4-phase build order, and files list. Adds ledger **T20 `ng/nk`** + **T21 `ear/air/ure`** units for the orphan sounds (needs authoring + sign-off); `schwa`/`zh` deferred. Roadmap M5.1 added (§15). **Design-only; no code.** | Owner: "spec it into CLAUDE.md" — extend M5 with per-phoneme sound instruction | doc-only; `npm run build` green | v0.2.63 (no code change) |
| 2026-07-22 | **M5 Phase 4 — a11y pass + doc cross-refs → M5 COMPLETE (§19.9 P4).** **A11y (§18.12):** `LearnRunner` moves focus to `main.screen` on each sub-phase change (map→rule→read→spell→done) so screen readers announce the new step (mirrors App's per-view focus); the unit-complete `done` screen is wrapped as an `aria-live="polite"` region; `LearnMap`'s list gets an `aria-label` and its progress line `role="status"`. **Docs:** §5/§7/§14 gained short **M5 cross-ref** notes (Learn walks the pattern order; the engine now powers Test-only with a learned-gate + struggle→flag; the child picker's 📘 Learn / 🎮 Test / map flow), and the module table already lists `LearnRunner`/`LearnMap`. No functional change to Learn/Test behaviour. This closes the last M5 phase — **all four §19.9 phases are done**; roadmap M5 marked ✅ MET. | Finish M5 — a11y on the new screens + spec cross-refs/checkboxes | `npm run lint:packs` OK (44 packs, 1869 items); `npm test` PASS ×3; `npm run build` green | v0.2.63 |
| 2026-07-22 | **M5 Phase 3 — Learn map + needs-review loop UI + dashboard counts (§19.9 P3).** New **`src/features/LearnMap.tsx`** (read-only per-pattern status: not-started/learning/learned/mastered/needs-review) is now the Learn **landing**. `LearnRunner` gains a `map` phase: on entry (and after each unit's "Learn the next one") it recomputes every pattern's `patternStatus` from the `learn` store + `progress`-derived mastery and the next target (needs-review first, else `nextToLearn`); "Start learning" begins the read+spell unit. **`ParentDashboard`** per-child card now shows **📘 Learn N/M patterns learned · 🏆 K mastered** (`getLearn` + `PATTERNS`; mastered = decode+encode both mastered). The Phase-2 needs-review resurfacing is now visible + closeable: struggle in Test flags the pattern → it shows as 🔁 Review on the map and is the next target → re-learning clears the flag. Smoke drives the whole loop (struggle Test → assert needs-review flagged → Learn re-teach → assert cleared + still learned); walkLearn now steps through the map. | M5 §19.9 Phase 3 — make the bidirectional loop visible + add the status map | `npm run lint:packs` OK (44 packs, 1869 items); `npm test` PASS ×3; `npm run build` green | v0.2.62 |
| 2026-07-22 | **M5 Phase 2 — Learn mode live; Test becomes pure assessment (§19.6/§19.7).** The dual-mode split goes live in one atomic deploy (no teaching-less window). **New `src/features/LearnRunner.tsx`** — the §19.6 unit: decode lesson → 3 read items → encode lesson → 3 spell items → `setLearned`+`clearReview` → "learned!" (offer next / done). Participation-based (no accuracy gate — the per-item error-correction guarantees a correct final production); no difficulty ramp/timing/SRS; reuses `McqItem`/`TileItem`/`LessonView`; targets the first needs-review pattern else `nextToLearn`. **`ChildPicker`** now shows **📘 Learn / 🎮 Test / 🏆 Trophies** per child (was "Play"). **`App`** gains the `learn` route. **`Session` is now Test:** loads the learned set and passes it to `eligibleSkills` (only learned patterns are assessed); **all teaching removed** — the first-exposure intro lesson, the struggle→lesson re-teach, and `LessonView`/`introRef`/`lessonCountRef` are gone; on struggle it `flagReview(pattern)` (needs-review, for Learn to resurface) then down-shifts to an easier prereq; a `learnfirst` empty state routes to Learn when nothing is eligible. Placement already marks placement-credited patterns learned (Phase 1). Smoke reworked: the e2e now drives **placement → Learn the frontier → Test** on both paths (good: Learn floss → Test masters the held-back `SP-floss` → retention cert; struggle: Learn CVC → Test wrong → asserts the pattern is flagged needs-review), and asserts **Test fires zero lessons**. | M5 §19.9 Phase 2 — relocate teaching to Learn + gate Test, atomically | `npm run lint:packs` OK (44 packs, 1869 items); `npm test` PASS ×3; `npm run build` green | v0.2.61 |
| 2026-07-22 | **M5 Phase-1 foundation — Learn/Test status + gating layer (additive, non-breaking).** First code for §19. **Store (DB v6):** new `learn` object store (`childId::patternId` → `{patternId, learned, needsReview, learnedAt?, flaggedAt?}`), oldVersion-guarded migration; helpers `getLearn`/`putLearn`/`setLearned`/`flagReview`/`clearReview`; wired into `ALL_STORES` (export/import) + `clearChildData`. **`src/lib/learn.ts` (pure):** `PATTERNS` (the dual-gated decode ladder), `nextToLearn(learned)` frontier (first pattern whose gating patterns are learned), `patternStatus` (not-started/learning/learned/mastered/needs-review), `learnedSet`/`needsReviewSet`. **Engine:** `eligibleSkills` gains an OPTIONAL `learnedPatterns` arg — when passed (Test mode), a dual-gated PATTERN skill is eligible only if its pattern is learned; non-pattern (reading/M3/dictation/threaded) skills are never learned-gated. Omitting it preserves pre-M5 behaviour, so **live behaviour is unchanged**. **Placement:** now marks fully-credited patterns (decode+encode both credited) as `learned`; the held-back top-2 encode rungs stay unlearned (become the first Learn units). `types.LearnState`/`PatternStatus`. Smoke adds the learn-store round-trip, `nextToLearn` frontier, and learned-gate invariants (schemaVersion→6). **Deliberately deferred to land atomically with Phase 2 (`LearnRunner`):** the live gate-flip in `Session`, removal of Test's intro/struggle lessons, and `struggle→flagReview` — bundling them with the Learn UI avoids a deploy window where teaching is gone but Learn doesn't exist yet. | Start M5 (§19.9 Phase 1) — lay the status/gating foundation without a teaching-less deploy | `npm run lint:packs` OK (44 packs, 1869 items); `npm test` PASS ×3; `npm run build` green; DB v6 migration additive | v0.2.60 |
| 2026-07-22 | **Spec added — §19 Learn/Test dual-mode (M5), owner-approved design.** Full, self-contained spec for a new dual-mode structure: **Learn** (linear, explicit, low-pressure teaching walk through the phonics/spelling scope & sequence — one pattern/unit, read+spell taught together, participation-based "learned") + **Test** (the existing adaptive engine, now gated to only assess *learned* patterns, with teaching removed). Bidirectional linkage/status model (not-started→learning→learned→mastered/needs-review), DB v6 `learn` store, engine `eligibleSkills` learned-gate + `nextToLearn` frontier, placement sets the frontier, `LearnRunner`/`LearnMap` screens, 4-phase build order, invariants-to-preserve, and file list — written so any agent can pick up M5. Roadmap M5 added (§15). **Design-only; no code yet.** | Owner: "add the whole roadmap to CLAUDE.md, comprehensive for any agent to pick up" | doc-only; `npm run build` green | v0.2.59 (no code change) |
| 2026-07-22 | **Explicit-first teaching — lessons now precede practice (audit).** Audit finding: §3 says "lessons state the rule BEFORE practice," but the runtime only fired a lesson on *struggle* — new patterns were met cold and the rule was taught only after failing (discovery-first, the opposite of OG explicit instruction). Fix (`Session.advance`): on a skill's FIRST exposure (`itemsAnswered===0`, not placement-credited, has a lesson) the lesson fires up front, then the existing lesson→guided(we-do)→normal(you-do) flow gives gradual release. New per-skill `introRef` (fires once/child-session, never consumes the struggle re-teach budget); reactive struggle→lesson still fires afterwards. Placement-credited + already-started skills skip it. | Audit: implementation contradicted §3's explicit-first principle | `npm test` PASS ×3 (adds a guard that the all-correct mastery path still sees a lesson — the proactive intro — despite never struggling); `npm run build` green | v0.2.59 |
| 2026-07-22 | **Dashboard trend — Daily/Weekly/Monthly/Yearly toggle + student dropdown.** Requested. **Granularity toggle:** new pure `aggregate.summarise(aggregates, daily, gran, cap)` buckets activity by day/week/month/year (oldest→newest, capped); the dashboard trend chart gains a segmented Daily/Weekly/Monthly/Yearly control (`gran` state, default weekly). Weekly/monthly/yearly roll up the existing weekly aggregates (full history); **daily** reads a NEW per-day store. **Schema DB v4→v5:** added a `daily` object store (`childId::YYYY-MM-DD` per-day totals), oldVersion-guarded additive migration; `store.bumpDaily`/`getDaily`, `aggregate.isoDay`, exported in `exportAll`/`importAll`, wiped by `clearChildData`; `Session` writes it per answer alongside the weekly rollup. **Student dropdown:** when >1 child, a `<select>` filters the dashboard to one student (or "All"). Bar labels come from `summarise` (day/week → date, month → "Jul 26", year → "2026"). | Owner: "Daily also… and select students from a dropdown" | `npm test` PASS ×3 (adds `summarise` day/week/month unit checks + a dashboard toggle walk; export schemaVersion→5); `npm run build` green | v0.2.58 |
| 2026-07-22 | **Dashboard chart label — friendly week date (reported "what is W30?").** The weekly-activity bars were labelled with the raw ISO-week suffix (`weekKey.slice(-3)` → `W30`), which is cryptic for a teacher. Added `aggregate.weekMonday(key)` + `aggregate.weekLabel(key)` (the Monday of that ISO week, formatted en-GB → e.g. `21 Jul`); `ParentDashboard` now renders `weekLabel(w.week)`. CSS: `.bar-lbl` `white-space:nowrap`, `.bar-wrap` min-width 28→36px so the date fits. | Reported: the "W30" chart label is unclear | `npm test` PASS; `npm run build` green | v0.2.57 |
| 2026-07-22 | **Owner sign-off — T19 ticked (complete).** Owner: "T19 all done so can sign off?" Verified all 8 reading rungs (`reading-L01-cvc`…`L08-two-syllable`) authored, each **≥20 items** (§6d floor met, 0 warnings), 8 `RD-*` scope skills gated behind their phonics patterns. T19 connected-text reading bridge is complete across the full decode ladder → box ticked. No code change. | Owner sign-off of the completed T19 tier | doc-only; `npm run build` green | v0.2.56 |
| 2026-07-22 | **Owner sign-off — T11 ticked; T18 prefixes+C-le approved.** Owner: "sign off." **T11 dictation chain complete → box ticked** (all encode packs + the full CVC→two-syllable dictation chain authored/lint-clean/smoke-verified). **T18** prefixes + consonant-le slices **approved** (ledger annotated); **box stays OPEN** because open syllables + schwa are genuinely unauthored (need sound-aware decodability, per the T18 note). No code change. | Owner sign-off of the reviewed authored slices | doc-only; `npm run build` green | v0.2.55 |
| 2026-07-22 | **T18 morphology extended — prefixes + consonant-le.** Two new dual-gated decode rungs above suffixes. `scripts/gen-prefixes.mjs` → `PH-/SP-prefixes` (26 decode + 26 encode; un/re/dis/mis/non/pre on decodable bases — prefix letters reuse existing tiles, envelope = two-syllable). `scripts/gen-cle.mjs` → `PH-/SP-c-le` (30 decode + 30 encode; consonant-`le` words kept to a CLOSED short-vowel first syllable so they stay fully decodable, adds a single **`le`** OG syllable tile + `PH-/SP-c-le` envelopes). 4 scope skills chained two-syllable→suffixes→**prefixes→c-le**, each dual-gated (read+spell) + per-level lessons. Segmentation reuses the lint's canonical greedy chunking (generators throw on any undecodable word). Lint-clean (55 packs, 2122 items, 0 warnings). **Deferred (needs a design decision, not more packs):** open syllables + schwa require *sound-aware* decodability — the grapheme-envelope model can't tell long/short/reduced readings of the same letter apart. **Pending owner §12.5 sign-off before ticking T18.** | Extend the decode ladder — highest-PSLE-yield morphology first (owner: "fix everything") | `npm run lint:packs` OK (0 warnings); `npm test` PASS ×3; `npm run build` green | v0.2.54 |
| 2026-07-22 | **T11 dictation chain completed (blends→two-syllable).** New `scripts/gen-dictation.mjs` emits the 7 remaining sentence-dictation packs `dictation-L04-blends`…`L10-two-syllable` (20 decodable sentences each = 140 items), mirroring the CVC/digraph packs: each word is segmented into its canonical greedy grapheme chunking (reusing the lint's `segment` against `decodability.json`, so tiles match the encode packs) with auto-generated confusable distractor tiles + per-level lessons. 7 chained scope skills `SP-blend-dictation`→`SP-twosyllable-dictation`, each gated behind the previous dictation rung (deep after `SP-two-syllable`) — no engine/logic change. Lint-clean (51 packs, 2010 items, 0 warnings). **Pending owner §12.5 sign-off before the T11 box is ticked.** | Complete the dictation chain (owner: "fix everything") | `npm run lint:packs` OK (0 warnings); `npm test` PASS; `npm run build` green | v0.2.53 |
| 2026-07-22 | **T19 reading packs topped to the §6d floor (L03–L08 → 20 each).** Added 2–4 decodable-sentence rows per rung to `gen-reading.mjs` (blends +2, silent-e +3, vowel-teams/r-controlled/diphthongs/two-syllable +4) so every reading pack now meets the ≥20 variety floor — clears the 6 non-fatal pool<20 warnings. New rows stay within each rung's cumulative `PH-*` envelope (lint-verified); no scope/logic change. | Clear the §6d reading-pool warnings — finish the T19 breadth | `npm run lint:packs` OK (44 packs, 1869 items, **0 warnings**); `npm test` PASS; `npm run build` green | v0.2.52 |
| 2026-07-22 | **T19 reading bridge completed across the whole decode ladder.** Added the 6 remaining reading rungs — **blends, silent-e, vowel-teams, r-controlled, diphthongs, two-syllable** — via a new `scripts/gen-reading.mjs` (row = `[passage, question, correct, d1, d2, difficulty]`; emits `reading-L03…L08`, 16–18 decodable-sentence + literal-MCQ items each + a per-level lesson). `decodableWithin` reuses the existing cumulative `PH-*` envelopes (no new decodability entries). 6 new `RD-*-sentences` scope skills, each gated behind its phonics pattern; `placement.priorSkillIds`' generic reading-credit loop + the `lint-packs` reading branch already cover them (no logic change). The word→connected-text bridge now exists at **every rung the child reaches** (CVC → two-syllable, 8 rungs total), so a decoding child always READS connected decodable text at their level, not only isolated words. Packs are 16–18 items (below the §6d ≥20 variety floor → 6 non-fatal warnings) — starter breadth, max pending. | Finish the connected-text bridge across the full decode ladder (top pedagogical gap) | `npm run lint:packs` OK (44 packs, 1848 items, **0 errors**, 6 pool<20 warnings); `npm test` PASS ×3; `npm run build` green | v0.2.51 |
| 2026-07-22 | **T19 connected-text reading extended — CVC rung to floor + digraph rung.** Brought `reading-L01-cvc` to **21 items** (clears the §6d <20 warning) and added a second reading rung **`RD-digraph-sentences`** (`reading-L02-digraphs`, 20 decodable sh/ch/th/ck sentences + literal-comprehension MCQ + lesson; blends decompose into single consonants so only sh/ch/th/ck are new tiles). New `RD-digraphs` decodability envelope; scope skill gated behind the **digraph pattern** (`prereq PH-digraphs`), placed at end of P1. `placement.priorSkillIds`' generic reading-credit loop + the `lint-packs` reading branch already cover it — no logic change. Smoke gains a digraph-reading gating invariant. The word→connected-text bridge now spans the two lowest envelopes (CVC + digraphs). | Continue T19 breadth — make the reading bridge substantive across the early ladder | `npm run lint:packs` OK (38 packs, 1749 items, **0 warnings**); `npm test` PASS ×3; `npm run build` green | v0.2.50 |
| 2026-07-22 | **Owner sign-off — authored audit slices approved.** Owner: "sign off everything." Approved the currently-authored content pending review: the **T19 `RD-cvc-sentences`** connected-text reading slice (16 items + lesson) and the **T18 `PH-/SP-suffixes`** morphology slice (40 decode + 57 encode + lessons). Ledger notes annotated "owner-approved 2026-07-22". **T18/T19 boxes stay OPEN** — the remainder of each tier is genuinely unauthored (T19: ≥20 pool + higher-envelope reading rungs; T18: open syllables, C-le, prefixes, schwa), so ticking would misrepresent coverage; each future slice needs authoring + a fresh §12.5 sign-off (same precedent as T11). No unticked box is fully authored, so nothing is ticked this pass. No code change. | Owner sign-off of the reviewable authored slices | doc-only; `npm run build` green | v0.2.49 |
| 2026-07-21 | **Audit round 3 — connected-text reading (T19) + decode purity + review cap + suffix max.** Fixes the remaining pedagogical gaps. **#1 word→connected-text bridge (T19):** new `reading` strand + `RD-cvc-sentences` skill (itemType `passage_question`, pool `reading-L01-cvc` — 16 decodable CVC sentences + a literal comprehension MCQ + a 🔊 sentence-audio support in `McqItem`, + lesson), gated behind the **CVC pattern** (prereq `PH-cvc-short-vowels`) so a child READS connected decodable text right after mastering CVC read+spell — no longer only isolated words. New `RD-cvc` decodability envelope (CVC graphemes + a few function words); `lint-packs` gains a `reading`-strand branch that decodability-checks the **passage + answer choices** (the question stem is read-aloud-supported, not linted). `placement.priorSkillIds` now also credits reading rungs whose gating pattern is already placed (a higher-placed child can already read lower sentences → no re-teach, no eligible-rotation dilution). **#2 review-density cap (`Session`):** cumulative interleave + fluency now share a per-session cap of ⌈len/3⌉ so new-skill acquisition always keeps the majority of items even for a max-support (dyslexia+fluency) child (due SRS reviews + HF thread exempt). **#3 decode phonic-purity (`gen-suffixes`):** the suffix DECODE pack now carries only the phonically-transparent rules (just-add, doubling — blendable straight through); drop-e (`hoping`) + change-y (`cried`) hide the base-vowel value and would mislead a sound-it-out reader, so they're ENCODE-only (taught as spelling rules). **#4 suffix pack maxed** toward the §6d ceiling (decode 40, encode 57; was 41/41). **#5 heart-hint polish** (dropped the weak `eye` hint; fixed `buy`). Smoke gains T19 gating invariants. | Round-3 audit "fix everything" — close the connected-text gap + the calibration/purity items | `npm run lint:packs` OK (37 packs, 1724 items, 1 warn = reading pool 16<20, starter); `npm test` PASS ×3; `npm run build` green | v0.2.48 |
| 2026-07-21 | **Audit follow-up — interleave/thread cadence-collision fix.** Re-audit caught a regression in the #1 flag tuning: the dyslexia/decoding `interleaveEvery` was set to **4**, but interleave shares session slots with the HF thread (`THREAD_EVERY=4`, checked first in `Session.advance`) — so the every-4 interleave was fully shadowed, yielding **0** cumulative reviews (the OPPOSITE of the "more review" intent; a no-flag child got 3). Fixed to **3** (coprime-ish with 4 → 4 effective reviews/16 ≥ default), and the `fluency` clamp `min(…,4)`→`min(…,3)`. New smoke guard computes *effective* interleave count (thread wins its slots) and asserts flag-tuned ≥ default, so a future collision fails the build. Uncovered by simulating session composition (thread/interleave/fluency stacking). | Re-audit found the flag tuning was silently defeated by the thread cadence | `npm test` PASS (adds effective-interleave regression guard); `npm run build` green | v0.2.47 |
| 2026-07-21 | **Whole-system pedagogy audit — remaining gaps fixed (#1–#6).** **#1 difficulty flags now DRIVE adaptation** (were display-only): new pure `src/lib/support.ts` maps `child.difficultyFlags` → tuning (`interleaveEvery`/`guidedItems`/`placementPerSkill`/`promoteStreak`/`fluencyMaxMs`); dyslexia/decoding = more review + gentler difficulty climb + more conservative placement + longer post-lesson scaffold; fluency flag = earlier automaticity practice. No-flags = identical defaults (engine constants unchanged). Threaded into `Placement` (`nextPlacement(results, perSkill)`) and `Session` (`nextDifficulty(…, promoteStreak)`, `interleavedReviewSkill(…, every)`, guided count). **#2 fluency LOOP** (was measured-only): `engine.fluencySkill`/`medianLatency` — every 6th item serves a difficulty-1 speed rep of the mastered decode pattern read accurately-but-slowest over threshold (non-punitive, no visible timer). **#3 decode scope extended past two-syllable**: new `PH-suffixes`/`SP-suffixes` rung (inflectional -s/-ing/-ed with add/double/drop-e/change-y rules) via `scripts/gen-suffixes.mjs` (41 decode + 41 encode, en-SG, lint-clean; suffix letters decompose into taught tiles so the envelope = two-syllable). Gated behind `PH-two-syllable`; M3 prereq left at two-syllable (no regression). **#4 heart-word method**: HF items carry a `heart` field (irregular part), rendered as a "💛 Heart word — the tricky part is …" scaffold; HF lesson rewritten (decode the regular letters, memorise only the heart). **#5 b/d (p/q) reversal cue**: `TileItem` shows a directional mnemonic when the first wrong tile is the mirror letter. **#6 M3 retention gate — verified already consistent** (M3 single-gate skills already schedule a +2d review and withhold the cert until the review pass, same path as decode; no change). Smoke gains #1 support + #2 fluency invariants. | Whole-system audit "fix everything" — close the captured-but-unused gaps + morphology scope | `npm run lint:packs` OK (36 packs, 1693 items, 0 warnings); `npm test` PASS ×3; `npm run build` green | v0.2.46 |
| 2026-07-21 | **Owner sign-off — T17 ticked.** T17 sentence-manipulation (editing + synthesis MCQ-adapted forms, 20+20 items + lessons, already built/lint-clean/smoke-verified) approved and ticked. **Not signed off (nothing to approve — unauthored):** T18 (advanced decode ladder), T19 (decodable connected-text reading), and the T11 dictation remainder (blends→two-syllable) are not yet authored, so sign-off is premature — they need building first (then §12.5 review). **#6 EAL glossing** still blocked on Open Decision #7 (which mother tongue, or English-only). No code change. | Owner: "sign off everything" — only T17 was authored & pending | doc-only; `npm run build` green | v0.2.45 |
| 2026-07-21 | **Whole-system audit — immediate error correction (OG) + add-time difficulty flags.** Audit beyond the engine (interaction + content layers). **#1/#4 immediate voiced error-correction (all four item renderers, §8):** a WRONG answer is still scored (assessment unchanged) but no longer ends the item — the correct answer is **revealed + voiced** and the child must **reproduce it correctly** before continuing, so the LAST thing they produce is correct, not the error (core OG error-correction routine; prevents consolidating the wrong form — critical for dyslexia). `McqItem` (tap the highlighted+spoken correct choice), `TileItem`/`DictationItem` (the word is revealed as a copy-model and rebuilt from tiles; dictation fixes the first wrong word), `ClozeItem` (wrong blanks cleared, correct word shown greyed, re-placed). `onAnswer` is deferred until the fix is done, carrying the FIRST attempt — score and (correct-only) fluency latency are unaffected; placement warm-up (`quiet`) stays assessment-only (no correction). **#3 add-time difficulty flags (`AddStudent`/`types`/dashboard, §14):** optional decoding/fluency/vocab/comprehension/dyslexia flags captured at add-time, stored on `Child.difficultyFlags`, shown as chips on the teacher dashboard (closes the §14 TODO). **Deferred (content/owner-gated):** **#2 decodable connected-text reading** (word→text bridge) added as ledger **T19** — needs authored decodable-sentence reading packs + owner sign-off (§18.3); **#6 EAL mother-tongue glossing** remains Open Decision #7 (needs owner call). **#5 handwriting / #7 blend-tap segmentation** are platform-inherent (tiles/no-keyboard §13) — noted, not defects. | Whole-system OG audit (owner: "fix everything") | `npm run lint:packs` OK (34 packs, 1611 items, 0 warnings); `npm test` PASS ×3 (adds MCQ correction on the struggle path + a tile wrong→model→rebuild drive in `#m3demo`); `npm run build` green | v0.2.45 |
| 2026-07-21 | **Progression-audit round 2 — retention-integrity fixes #1–#7.** Deeper OG audit of the retention machinery (the anchor of the dyslexia design). **#1 retention-failure now re-teaches (`Session.tsx`, §7):** a failed +2d review used to only re-book another review in 2 days; it now also fires an immediate **guided re-practice block** (3 difficulty-1 items on the forgotten skill) — real "demote to re-practice," not a deferred re-test. **#2 retention verifies BOTH channels (`Session.tsx`, §7):** acquisition schedules reviews for **decode AND encode** (was decode only); the certificate is withheld until **both** have retained (each review stage ≥1 = ≥2 review items across the pattern), so spelling retention — the more fragile channel in dyslexia — is actually checked, not assumed. **#3 placement holds back TWO encode rungs (`placement.ts`, §7):** a decode-only placement credited spelling for every rung below the top; it now holds back the top **two** rungs' encode (decode ≫ encode is the classic profile), so more spelling is earned in-session rather than assumed. **#4 massed practice first (`Session.tsx`, §7):** threading (sight words / letter-sounds) + interleave are suppressed until the child acquires their first pattern — a raw beginner gets an undiluted focus on the current skill (OG: massed for acquisition, distributed for retention); placement-credited children are already past this. **#5 down-shift (`Session.tsx`, §8):** when re-teaching is exhausted (2 lessons) and the child is still failing, drop to an easier **prerequisite** for supported practice instead of hammering the too-hard skill. **#6 fluency band excludes encode (`readiness.ts`):** reading-speed now measured over decode items only (SP-* excluded) so slow tile-building doesn't mislabel a child "effortful." **#7 encode-unlock needs evidence (`engine.ts`):** encode opens at ≥70% decode over **≥6 real items** (`ENCODE_UNLOCK_MIN`), not a 3/3 fluke; placement-confirmed decoders still unlock immediately. | Round-2 OG audit (owner: "fix everything") | `npm run lint:packs` OK (34 packs, 1611 items, 0 warnings); `npm test` PASS ×3 (dual-channel confirmation flow re-verified, adds #7 unlock-threshold checks; placement/holdback paths re-verified); `npm run build` green | v0.2.44 |
| 2026-07-21 | **Progression-audit fixes #1–#5 (grapheme-level errors, fluency signal, safer placement, guided practice).** From a full Structured-Literacy/OG audit of the engine for the decode+spell dyslexia profile. **#1 grapheme-level error analysis (`scoring.ts` + `graphemeConcepts.json`, §7):** encode scorers (`scoreTiles`/`scoreDictation`) now name the concept from the EXPECTED grapheme at the first SUBSTITUTION (same-length mismatch) — `ship`→`chip` = `digraph-sh`, `cat`→`cot` = `vowel-short-a`, `bat`→`dat` = `consonant-b` (b/d reversal); omissions/insertions (length mismatch, e.g. a dropped silent-e) fall back to the item's authored tag. Delivers the "which grapheme missed → missedConcept" the spec promised, so struggle detection + the dashboard "stuck on" are now precise (and **#5** targeted confusable-pair remediation falls out of this — a repeated grapheme confusion now fires its own struggle→lesson). New `scripts/gen-grapheme-concepts.mjs` → `src/data/graphemeConcepts.json` (63 graphemes). **#2 fluency signal (`readiness.ts` + dashboard, §7):** median latency of recent CORRECT answers → a NON-gating teacher-only band (⚡ quick / 🕒 building / 🐢 effortful); "effortful but accurate" (≥85% + slow) is the classic dyslexia signature and drives a "practise for automaticity" action. No child-facing timer (respects §14). **#3 safer placement (`placement.ts`, §7):** `PER_SKILL` 2→3 — two lucky 3-choice guesses (1/9) could fluke a level and over-place a struggling reader; 3/3 drops that to ~1/27 and, capped at `MAX_ITEMS=15`, keeps placement conservative (start low). **#4 post-lesson guided practice (`Session.tsx`, §8):** after a lesson the runtime now forces a short block of **3 difficulty-1 items** on the just-taught skill (guided practice) before resuming normal rotation, so a child who just failed isn't dropped back into a harder item. **#6 deferred (content + owner sign-off):** extending the decode ladder past two-syllable (open/C-le syllables, morphology, prefixes/suffixes, schwa) is a content workstream that needs authored packs + owner review (§18.3) — added as ledger T18, NOT auto-shipped. | Full OG progression audit (owner: "fix everything") | `npm run lint:packs` OK (34 packs, 1611 items, 0 warnings); `npm test` PASS ×3 (adds #1 substitution/omission scorer checks, #4 post-lesson difficulty-1 guard; placement paths re-verified at 3/level); `npm run build` green | v0.2.43 |
| 2026-07-21 | **Fix — Play/Trophies buttons spill past the child card (reported).** On a wider phone (≈430px → 3 narrow columns) the per-card **Play + 🏆 Trophies** buttons overflowed the card. Root cause: the action `.stack` inside the `.avatar` card was centred (avatar `align-items:center`) and shrink-to-fit to the widest button's content, so "🏆 Trophies" made the stack (and its buttons) wider than the narrow card. Fix (`styles.css`): action stacks now `align-self:stretch; width:100%` and `.avatar .btn.small { width:100% }` so buttons fill the card exactly (never exceed it), plus the picker grid min widened `120px→140px` (fewer, roomier columns on phones). Regression guard (smoke): a **new 430px two-child block** asserts every action button stays within its card's **content box** (derived from padding+border — a border-box/document-overflow check misses this spill because the button stays inside the padding cushion + viewport). Negative-tested: reverting the fix fails the guard (button 102px vs 83px content box); with the fix it passes. | Reported: buttons spilling out of the card | `npm run lint:packs` OK (34 packs); `npm test` PASS ×3 (adds the content-box spill guard, negative-tested); `npm run build` green | v0.2.42 |
| 2026-07-21 | **Letter-sounds (T01) unblocked + scaffolded — no longer a wall.** Reported: a struggling reader was "stuck at the same two words, man and ant" — i.e. walled at the audio-only `PH-letter-sounds` floor whose lesson had just 2 fixed examples, unable to reach CVC. Two fixes (owner: "both"). **(1) Threaded, not gating:** `PH-letter-sounds` is now `threaded:true` (like HF sight words) and **removed as CVC's prereq** (CVC is the ladder floor). A shaky child now starts on real CVC word-reading (which has visual choices) while letter-sounds is woven in every ~8th item (two threaded skills — letter-sounds + HF — rotate every 4th), instead of being blocked behind isolated-phoneme discrimination. **(2) Scaffolded + varied:** each letter-sounds choice now shows a **keyword anchor** under the letter (`Choice.keyword`, e.g. `a`/"ant" — OG sound→keyword→letter, generator-sourced, rendered by `McqItem`); the lesson gained **6 keyword examples** (was 2) and `LessonView` now shows a **rotating random subset (≤3)** so it stops repeating the same two words. `gen-letter-sounds.mjs` regenerated (46 items, keyworded choices, 6-example lesson); new CSS for the anchor. Smoke: T01/threaded invariants rewritten (letter-sounds threaded + not eligible + not a CVC prereq; CVC is the floor; two threaded skills rotate, 4 fires/16 with both appearing), and the decode-choice tile click switched to **index-based** (keyword sublabels break exact-text matching). | Reported: struggling reader stuck at letter-sounds ("man and ant") | `npm run lint:packs` OK (34 packs, 1611 items, 0 warnings); `npm test` PASS ×3; `npm run build` green | v0.2.41 |
| 2026-07-21 | **Child-facing trophies (two surfaces) + wider voice-speed range.** Reported: students had no way to see achievements (badges/certs were teacher-dashboard-only), and voice speed "doesn't work" on iPhone. **Trophies (§14):** new child-facing `src/features/Trophies.tsx` — celebratory, growth-only (level + XP progress bar via `gamify.toNextLevel`, the 6 achievement badges earned/locked, and earned certificates' "I can…" statements; no readiness/AL band — that stays parent-only). Reachable **two ways**: (a) a **"🏆 Trophies"** button per child on `ChildPicker` (+ `onTrophies` prop, `App` `trophies` view), and (b) the **session summary now highlights awards earned THIS session** — `Session` snapshots earned-badge ids at mount + collects certs minted during the run, and the summary shows a "New this session!" block (new certs + newly-earned badges) plus a "🏆 My trophies" button into the trophy room. **Voice speed (§14, #1 follow-up):** the range/step was too narrow to perceive; widened 0.5–1.3×/0.1 → **0.4–1.6×/0.2**, with a note that iOS built-in voices only partly honour `rate` (a Web-Speech platform limit, not a wiring bug — real speed control needs pre-recorded audio, Phase 2). New CSS for the trophy room + award highlights. Smoke: session-entry selector fixed to `/Play with Test/` (the new Trophies button also matched `/Test/`), + asserts the summary highlight after the confirmation cert and drives the trophy room (badges + certificate sections, no overflow). | Reported: students can't see achievements; voice speed ineffective on iPhone | `npm test` PASS ×3 (adds summary-highlight + trophy-room checks); `npm run build` green (precache 59) | v0.2.40 |
| 2026-07-21 | **OG-audit pedagogy fixes #1–#4 (mastery = retention; overlearning; spelling not free-placed; earlier + repeatable re-teach).** From a Structured-Literacy/OG review of the engine for the dyslexia profile. **#1 retention gate (`Session.tsx`, §7):** the certificate is now WITHHELD at acquisition and minted only on the **first +2d spaced-review pass** — mastery = retention, not same-session accuracy. Acquisition still advances the ladder (PROVISIONAL mastery, keeps momentum) + schedules the +2d review; the review-pass branch awards the cert once (guarded by a loaded `certsRef`). **#2 overlearning:** every P1 phonics/spelling `minItems` **8→12** (fixes the spec/excerpt inconsistency; 85%×12 ≈ 11/12 — real overlearning for struggling readers). **#3 spelling not free-placed (`placement.ts`/`engine.ts`, §7):** the reading placement only tests decoding, so `priorSkillIds` no longer auto-credits the **top** rung's encode partner — the child must EARN that spelling in-session before the decode entry unlocks (`encodeUnlocked` made placement-aware so the held-back encode is immediately eligible). Spelling lags decoding in dyslexia; a high placement no longer hands it out for free. **#4 earlier + repeatable re-teach (`engine.ts`/`Session.tsx`, §8):** struggle trigger `<0.6 over ≥5` items or **2** same-concept misses (was `<0.5/≥6` and 3 — too late); the lesson may now re-fire **once more** (cap 2/skill/session) after ≥3 further failed attempts if still struggling. **Root-cause fix (surfaced by the new due-review test):** `Session` mount effect was **unguarded against StrictMode double-invoke** (raced two `advance()`s, dropping the due review) — added a `startedRef` guard (as `Placement` already had). **Deferred (agreed):** #5 fluency/automaticity gating (conflicts with §14 no-timing-pressure default) and #6 morphology/affix/other-syllable-type decode scope (content roadmap, not an engine fix). | OG audit: mastery must mean retention + overlearning; spelling over-placed; late one-shot intervention (owner: "do all") | `npm run lint:packs` OK (34 packs, 1611 items, 0 warnings); `npm test` PASS ×3 (adds #1 confirmation-review flow driving cert on a backdated +2d review, #2 minItems-12 gate, #3 held-back-encode-served-first + placement-aware `encodeUnlocked`, #4 earlier-struggle + StrictMode guard); `npm run build` green | v0.2.39 |
| 2026-07-21 | **Phoneme clips re-recorded (all 44) + version footer de-drifted.** Owner replaced every `public/phonemes/*.m4a` with new, longer recordings (each ~2–5× the byte size; filenames unchanged so `phonemes.json` + `audio.phoneme()` resolve identically). Uploaded direct-to-`main` via GitHub web (deploy `e0d6f6b` green); workbox content-hashes the clips so the SW surfaces the update toast and re-fetches only the changed files — no manifest edit needed. Separately fixed the stale visible version: `App.APP_VERSION` was hand-set to `0.2.29` (drifted 8 releases behind `package.json`); now injected from `package.json` via a vite `define` (`__APP_VERSION__`, declared in `vite-env.d.ts`), so §13's footer can never drift again. | Owner re-recorded the phoneme set; footer had drifted (§13/§18.6) | `npm run lint:packs` OK (34 packs, 1611 items, 0 warnings); `npm test` PASS; `npm run build` green (footer renders 0.2.38) | v0.2.38 |
| 2026-07-21 | **Voice — Samantha only.** `listVoices()` now filters to voices named Samantha (was all English), so the Settings picker offers just Samantha; `speak()` default preference is chosen-voice → Samantha → first en-GB → platform default (so the app uses Samantha even before any pick). Picker's ◀▶ arrows hidden when ≤1 voice. On non-Apple devices with no Samantha the row hides and TTS falls back to en-GB. | Owner: "just stick to Samantha and remove the rest" | `npm test` PASS; `npm run build` green | v0.2.37 |
| 2026-07-21 | **Rename user-facing "Parent" → "Teacher".** Visible strings only: ChildPicker "🔒 Teacher area" (+aria-label), dashboard header `Teacher area`, PIN titles "Enter/Create a teacher PIN". Code identifiers (`ParentDashboard`, `onParent`, file) kept to avoid churn — behaviour identical. Smoke assertions updated to the new copy. | Owner: "rename from parent to teacher" | `npm test` PASS; `npm run build` green | v0.2.36 |
| 2026-07-21 | **Diagnosis — TTS silent = iOS ring/silent switch (not a code bug).** Owner's Test reported `▶ speaking…` (engine starts) but no sound. Root cause: on iOS `speechSynthesis` output follows the **ringer/silent hardware switch + ring volume**, while HTML5 `<audio>` (the phoneme `.m4a` clips) plays on the media channel and ignores it — so clips are audible but TTS is muted when silent mode is on. Not fixable in-app (Web Speech gives no channel control). Added an inline hint under the Voice row (shown once Test reports speaking/finished) telling the parent to switch silent mode off + raise the ringer volume. | Owner: "says speaking but no sound" | `npm test` PASS; `npm run build` green | v0.2.35 |
| 2026-07-21 | **TTS Test button — self-diagnosing (still-silent report).** Prior cancel-guard fix didn't resolve it on the owner's device, so the Test button now builds its own utterance with `onstart`/`onend`/`onerror` handlers and shows the engine's own status inline (`▶ speaking…` / `✓ finished` / `✗ not-allowed|interrupted|synthesis-failed` / `no speechSynthesis` / `sent (N voices)…`). This distinguishes a device-mute/volume problem (fires `✓ finished` but no sound) from a permission/gesture problem (`✗ not-allowed`) from speak() never running — so the next report pinpoints the cause instead of guessing. | Reported: Test still doesn't work — need the device's own error | `npm test` PASS; `npm run build` green | v0.2.34 |
| 2026-07-21 | **Fix — TTS Test button silent (iOS).** Root cause: `speak()` called `synth.cancel()` unconditionally; iOS Safari drops a `speak()` that immediately follows `cancel()` when nothing is queued — the dashboard Test was the first TTS call, so its utterance was swallowed. Now cancels only to interrupt an in-flight utterance (`synth.speaking || synth.pending`) and `resume()`s if paused. | Reported: Test button doesn't work | `npm test` PASS; `npm run build` green | v0.2.33 |
| 2026-07-21 | **TTS voice picker in parent Settings.** `speak()` previously hard-picked the first en-GB voice; now `audio.ts` gains `setVoice(voiceURI)` / `listVoices()` (English voices only) / `onVoicesReady(cb)` (handles the async `voiceschanged`), and `speak()` prefers the chosen voice (falling back to first en-GB, then platform default). New `Settings.voiceURI?` persisted; applied app-wide on boot (`App`) and per session (`Session`). Dashboard Settings adds a **◀ name ▶ stepper + 🔊 Test** row (non-native, §18.12; hidden when the device exposes no voices). | Owner: "can change the TTS?" → chose a Settings voice picker | `npm test` PASS (existing settings walk green; voice row is hidden in headless — no installed voices — so verified manually that selection persists + Test speaks); `npm run build` green | v0.2.32 |
| 2026-07-21 | **T01 letter-sounds ACTIVATED — 44 phoneme clips shipped (owner-recorded).** Owner dropped all 44 `public/phonemes/*.m4a` clips (23 single-letter for T01 + 21 digraph/vowel-team for later levels); validated every manifest id resolves to a non-empty file, no extras. Flipped `PH-letter-sounds` live: removed `enabled:false` in `scopeAndSequence.json`, so it re-enters the runtime graph as the **decode floor** (prereqs `[]`) and CVC's `["PH-letter-sounds"]` prereq now resolves instead of being stripped. Placement ladder **unchanged** (letter-sounds has no `encodePairId`, so it isn't a dual-gated rung) — but a low placement now serves letter-sounds first in-session (foundation before CVC), and `placement.priorSkillIds` already marks it mastered for higher placements. `audio.phoneme()` resolves `import.meta.env.BASE_URL + 'phonemes/' + file` → correct on Pages; clips SW-precached (precache **15→59 entries**). Smoke's T01 guard flipped inert→active (skill present, floor with no prereqs, CVC depends on it). **T01 ticked.** | Owner: "ok done... all 44" — phoneme recordings delivered | manifest check: 44/44 resolve, none empty, no extras; `npm run lint:packs` OK (34 packs, 1611 items, 0 warnings); `npm test` PASS; `npm run build` green (59 precache entries) | v0.2.31 |
| 2026-07-21 | **Owner sign-off — 6 breadth packs approved.** T14 tenses, T15 antonyms+context, T16 inference+vocab-cloze, T11 digraph-dictation all owner-approved; ledger notes updated (approved). No code change. | Owner: "Sign off." | doc-only; `npm run build` green | v0.2.30 |
| 2026-07-21 | **Content breadth — 6 new packs (T11/T14/T15/T16 follow-ups).** All buildable ledger breadth authored in one pass, each 20–24 items + lesson, en-SG, lint-clean, gated behind existing chains so the mature session/placement flow is unperturbed. **T14** `grammar-L03-tenses` (`GR-tenses-basic`, prereq `GR-sva-simple`; 24 grammar_mcq — past/present/continuous/future incl. irregular went/sang/won/lost). **T15** `vocab-L02-antonyms` (`VOC-antonyms`, prereq `VOC-synonyms`; 24) + `vocab-L03-context` (`VOC-context`, prereq `VOC-antonyms`; 24 context-clue). **T16** `comp-L02` (`CM-inference`, prereq `CM-comprehension`; 20 passage_question, inference/evaluative, longer passages) + `cloze-L02-vocab` (`CL-vocab-cloze`, prereq `CL-grammar-cloze`; 20 grammar_cloze, content-word word-bank). **T11** `dictation-L03-digraphs` (`SP-digraph-dictation`, prereq `SP-cvc-dictation`; 20 sh/ch/th/ck sentence-dictation, decodable within the SP-digraphs envelope, single-tile digraphs). No new item types/renderers — all reuse `McqItem`/`ClozeItem`/`DictationItem`. Scope gains 6 skills (1 in P1 dictation chain, 5 in P3 parallel branches); no smoke changes needed (existing M3 gating + T12/T17 invariants still hold — new skills all gated behind two-syllable). **Owner-approved 2026-07-21.** | Owner: "Do all now" — author every buildable content-breadth item (audio-blocked T01 + free-text §2/§6 excepted) | `npm run lint:packs` OK (**34 packs, 1611 items, 0 warnings**); `npm test` PASS; `npm run build` green | v0.2.30 |
| 2026-07-20 | **T17 sentence manipulation — MCQ-adapted (editing + synthesis & transformation).** New strand `sentence` + item types `editing_mcq`/`synthesis_mcq` (both render via `McqItem`, deterministic exact-match, **no keyboard** §13). Scope P3: `SM-editing` (prereq `CL-grammar-cloze`) → `SM-synthesis` (prereq `SM-editing`), gated deep behind the grammar/cloze chain. Packs `sm-L01-editing` (20 spot-and-correct: SVA, tense, plural, article, pronoun, possessive, capitalisation, spelling) + `sm-L02-synthesis` (20 pick-the-rewrite: because/although/so/but, both/neither-nor/either-or, who/which relative clauses, when/if/unless/while, reported speech, too…to, so…that, despite, not only…but also) + lessons, en-SG. Lint: added both types to `mcqTypes` (key + unique-label validation). Wired into the DEV `#m3demo` harness; smoke drives an editing item and asserts SM is gated (not eligible up front). **What stays deferred:** typed/free-text synthesis (needs keyboard/AI scorer, ruled out §2/§6). | Make Sentence Manipulation playable deterministically, MCQ-adapted like open-ended comprehension | `npm run lint:packs` OK (28 packs, **1479 items, 0 warnings**); `npm test` PASS (adds editing drive + T17 gating invariant); `npm run build` green | v0.2.29 |
| 2026-07-20 | **T12 HF threading** (closes the last open piece of T12). `HF-words` becomes a `threaded` skill (new `SkillDef.threaded` flag; prereq removed): `engine.eligibleSkills` filters threaded skills out of the normal rotation, and `engine.threadedSkill(count)` returns the HF skill **every 4th session item** (`THREAD_EVERY=4`, ~19–25% of a session), rotating if several threaded skills exist. `Session.advance` serves the threaded item right after due-reviews and before the interleave/eligible branches. Effect: tricky sight words (the/was/said…) are woven through **every** session from the very first level, not gated after two-syllable. Smoke adds a T12 engine invariant (HF absent from eligible; threads on the 4th-item cadence, 4× per 16). | Thread HF sight words throughout as §5/§6d intends, instead of end-gating them | `npm test` PASS (T12 cadence + eligible-exclusion invariants, plus all prior flows); `npm run lint:packs` OK (26 packs, 1439 items, 0 warnings); `npm run build` green | v0.2.28 |
| 2026-07-20 | **T11 dictation built** (new `dictation` item type end-to-end). Added `dictation` to `ItemType` + a `words[]` field to `PackItem`; `scoreDictation` (per-word tile match); `DictationItem` renderer (hear a short decodable sentence → build it one word at a time from grapheme tiles, "Next word"/"Check"); `dictation-L02-cvc` pack (20 CVC-only sentences using a/it/is/in/on/up as decodable function words — no HF like "the"; each word canonical-segmented, decodable within the CVC envelope) + lesson; scope skill `SP-cvc-dictation` (strand spelling, single-gate, **prereq SP-two-syllable** so it's gated after the ladder and never perturbs placement/early-session timing). Lint gained a dictation branch (per-word graphemes present + canonical + decodable; skips the whole-sentence audioText from the word-level decodability check). Wired into Session render + the DEV `#m3demo` harness; smoke drives the renderer word-by-word and asserts `scoreDictation` math. Remaining TXX status: **T01 blocked on owner phoneme recordings; T12 HF threading is engine follow-up; T17 deferred (free-text).** | Continue the ledger — dictation is the one buildable capstone (T01 audio-blocked, T17 deferred) | `npm run lint:packs` OK (26 packs, **1439 items, 0 warnings**); `npm test` PASS (adds dictation renderer drive + scorer math; M3 strands + all prior invariants green); `npm run build` green | v0.2.27 |
| 2026-07-20 | **T14/T15/T16 owner-approved → ticked** (grammar articles+SVA, vocab synonyms, comprehension+grammar-cloze; 24 items each, lint-clean). Ledger notes list future adds (tenses-basic, antonyms/context-clue, more passages) as follow-ups, not blockers. | Owner sign-off on the expanded M3 Paper-2 starter track | doc-only; `npm run build` green | v0.2.26 |
| 2026-07-20 | **M3 starter packs expanded 10→24 items each** (clears the 5 §6d sub-floor warnings): `grammar-L01-articles` (a/an/the incl. silent-h & 'one'/'university' sound rules), `grammar-L02-sva` (singular/plural, collective, neither-nor, 'each'/'every', Maths-as-singular), `vocab-L01-synonyms` (14 more sentence-synonym items, correct-choice position varied a/b/c), `comp-L01` (14 more en-SG passages, literal + inference), `cloze-L01-grammar` (14 more word-bank items, connector/preposition). All en-SG, Singapore context, unique choice labels; T14/T15/T16 ledger notes updated (still pending owner sign-off). | Clear the 5 sub-floor lint warnings + make Paper-2 substantive | `npm run lint:packs` OK (25 packs, **1419 items, 0 warnings**); `npm test` PASS; `npm run build` green | v0.2.25 |
| 2026-07-21 | **Warm-up UX — remove progress bar + gentle end card (reported).** The 15-dot progress bar made a ~6-item warm-up look like it had quit early (6/15 filled), and finishing snapped straight back to the picker (felt abrupt). Removed the dots entirely; `Placement` now ends on a friendly card ("Nice warm-up, {name}! 🌟 … Let's read") that the child taps to continue, instead of auto-jumping. Smoke's three placement drives updated to click through the new done card (via a DOM click — the button is confirmed actionable in isolation; the harness DOM-click avoids a suite-only actionability-stability flake). | Reported: progress bar implies premature exit; abrupt ending | `npm test` PASS ×3; `npm run build` green | v0.2.24 |
| 2026-07-21 | **Fix — warm-up placement ended after 2 items (reported).** A new account's warm-up stopped after two taps whenever the child missed either of the first two items: the staircase requires 2/2 correct to climb, and CVC is the enabled floor, so a partial miss decided the entry immediately and finished. Made worse by the task being audio-only ("Tap the word you hear", cat/cot/cut) — on iOS TTS is often silent until the child taps 🔊, so the first pair is easily missed. Fix (`placement.ts` + `Placement.tsx`, §7 "warm-up game… end on an achievable item"): once the entry level is decided, top up with **achievable** items (difficulty-1 from a level already cleared, or the floor) to a **`MIN_WARMUP=6`** minimum, ending gently; padding items are *not* scored so they don't move the placement. Also **guarded the mount effect against React StrictMode double-invoke** (was advancing twice, discarding the first item and skewing the count). Warm-up copy now nudges tapping 🔊. Smoke counts placement items and asserts **≥6** on the struggle path (was ending at 2). | Reported: intro warm-up ends after two steps | `npm test` PASS ×3 (adds ≥6-item warm-up guard); `npm run build` green | v0.2.23 |
| 2026-07-20 | **§18.11 content & pedagogy audit (pre-"done" gate).** Re-verified every shipped pack + the engine against spec; **no content defects found**. Two new automated guards added to `lint-packs` (both negative-tested — confirmed they fail on a planted violation, then reverted): **(8) canonical grapheme segmentation** — every encode item's `graphemes` must equal the greedy longest-match chunking of its `displayWord` within its envelope, so digraphs/vowel-teams/medial-doubles stay single OG tiles (`ship`→`sh·i·p`, not `s·h·i·p`); **(9) distractor uniqueness** — MCQ choice labels must be distinct (no accidental second-correct). All 25 packs pass both. Manual scans clean: no US spellings beyond the blacklist (`-ize`/`-or`/color/center…), no Singlish target answers, no empty `stem`/`rationale`/`iCanStatement`/`explanation`. Engine invariants (dual gate A2, placement-mastery A1, difficulty A3, interleave A5, SRS +2/+7/+21d, struggle→lesson-once, cert idempotency) already guarded by the committed smoke — re-confirmed green ×3. **Reviewed acceptance (not a defect):** M3 comprehension/vocab/cloze passages are not decodability-linted by design — they're gated behind the *full* decode ladder, are meaning-focused MCQ, and §3 permits EAL vocabulary glossing. | §18.11 "before done" audit — verify the shipped base before the family relies on it | `npm run lint:packs` OK (25 packs, 1349 items) with 2 new gates; negative-tests confirmed both bite; `npm test` PASS ×3; `npm run build` green | v0.2.22 (build-gate only — no client bundle change) |
| 2026-07-20 | **Fix — button overflow with OpenDyslexic (reported).** The wider OpenDyslexic glyphs pushed two layouts off-screen: the Manage **Reset/Remove** buttons inside the narrow 2-column avatar cards, and the **Settings** font/stepper rows. Root cause: `.btn.small` had fixed wide padding (`0 24px`) and `flex:0` (no shrink), and the rows didn't wrap. Fixes: `.btn.small` → `flex:0 1 auto; min-width:0; padding:0 14px`; ChildPicker manage actions + confirm now **stack vertically** (full-width, fits any font); `.set-row` and its control group `flex-wrap:wrap`. Rejected a `.app{overflow-x:hidden}` band-aid — it would have *clipped* the buttons and silently defeated the overflow test. Smoke now forces `data-font=dyslexic` and asserts no horizontal overflow on the **Manage** (row + confirm) and **Settings** screens (the exact spots reported). | Reported: buttons spill off-screen in OpenDyslexic | `npm test` PASS ×3 (adds dyslexic-font overflow guards on manage + settings); `npm run build` green | v0.2.22 |
| 2026-07-20 | **M4 completion — achievements + a11y focus.** `gamify.achievements(attempts,certs,usage)` (pure): six growth-only badges (getting-started, first-cert, 50/100 correct, five-skills, 3-week-streak), shown per child on the dashboard as earned/locked with a "N/6 badges" count. A11y: the `.screen` `<main>` is focusable (`tabIndex=-1`) and takes focus on every view change so screen readers announce the new screen (§18.12); labelled settings steppers already landed in v0.2.20. Smoke adds `achievements` invariants (none earned at zero; getting-started+first-cert after 1 correct/1 cert) and a dashboard badge-row check. **M4 core complete.** *Left as genuinely-deferred:* threshold tuning (needs the family's real usage data, §15) and `ui.ts` shared modal/toast primitives (no functional gap — all dialogs are already non-native per §18.12). | Finish M4's gamification depth + a11y pass; the remainder needs live data or is non-functional | `npm test` PASS ×3 (adds achievement guards + badge row); `npm run build` green | v0.2.21 |
| 2026-07-20 | **M4 — polish (dyslexia font, settings, gamification, a11y/cache).** **Fonts now self-hosted + offline** (§14): `@fontsource/lexend` + `@fontsource/opendyslexic` (400/700) imported in `main.tsx` → woff2 bundled + SW-precached (previously the CSS named "Lexend" but shipped nothing, so offline users fell back to a system font). Default **Lexend**; **OpenDyslexic opt-in** via `data-font` on the root, applied on boot from `settings.font` and toggled live. **Settings** surfaced in the parent dashboard (PIN-gated): font choice, voice speed (ttsRate 0.5–1.3×), session length (8–24) — persisted to the `settings` store; `Session` now reads `sessionLength`/`ttsRate` (via `audio.setRate`) on mount instead of a hard-coded 16. **Gamification** `src/lib/gamify.ts` (pure, derived — no stored score to drift): XP = 10/correct + 50/certificate, quadratic `level()`; the child picker shows a "⭐ Lvl N" badge and the session summary shows "+N XP". **Cache pre-warm** (§13): `pwa.ts` runs `registration.update()` on `visibilitychange` (foreground) — never auto-reloads; the waiting-SW toast still governs. Smoke adds: boot sets `data-font=lexend`, dashboard toggle → `dyslexic` persists across reload, picker level badge renders, and `gamify.xp`/`level` invariants. | M4 milestone — the dyslexia-first font was referenced but never bundled (offline gap); plus learner motivation + adjustable pacing (§14) | `npm test` PASS ×3 (adds font toggle/persist, XP badge, gamify guards); `npm run build` green; precache 15 entries incl. woff2 | v0.2.20 |
| 2026-07-20 | **M3 — PSLE Paper-2 foundations (grammar/vocab/comprehension/cloze; MCQ + word-bank).** New scope level **P3** with 5 skills gated behind the full decode ladder (`PH-two-syllable` pattern, §5 — unreachable until decoding clears) + intra-strand chains: `GR-articles`→`GR-sva-simple`, `VOC-synonyms`, `CM-comprehension` (prereq VOC), `CL-grammar-cloze` (prereq SVA). Types: added item types `vocab_mcq`/`vocab_cloze_mcq`/`passage_question`/`visual_text`/`grammar_cloze`, strands `comprehension`/`cloze`, and `PackItem.passage`/`wordBank`/`blanks`. Renderers: `McqItem` now shows an optional `passage` (reused for every MCQ type — grammar/vocab/comprehension/visual-text); **new `ClozeItem`** (tap a word-bank word → fills the next blank, tap a blank to clear, Check; each word used once; no keyboard §13). Scoring: `scoreCloze` (per-blank acceptable-list, deterministic). Packs (10 items each + lesson, en-SG): `grammar-L01-articles`, `grammar-L02-sva`, `vocab-L01-synonyms`, `comp-L01`, `cloze-L01-grammar`. §6b lint extended: new MCQ types validated; `grammar_cloze` checked (word bank ≥2, blanks present, every acceptable ∈ bank). Session serves the cloze renderer. **Deferred (§2/§6):** editing, comprehension-cloze (no word bank), synthesis & transformation — free-text/brittle, no keyboard. DEV-only `#m3demo` route + `M3Demo.tsx` render the cloze + a passage MCQ so the smoke exercises the renderers without mastering the whole ladder; smoke adds M3 gating (grammar gated → unlocks after two-syllable pattern), `scoreCloze`/`scoreMcq` invariants, and the demo render (correct answers score, no overflow/errors). | M3 milestone — the PSLE Paper-2 strands, validly sequenced behind decoding | `npm run lint:packs` OK (25 packs, 1349 items, 5 pool<20 warnings); `npm test` PASS ×3; `npm run build` green | v0.2.19 |
| 2026-07-20 | **M2 — parent dashboard + readiness + analytics + backup.** Data layer: store **DB v4** adds `aggregates` (per child·ISO-week·skill `{items,correct,minutes}`, never rolled off), `usage` (weekly session count + streak, target 4), `settings` (PIN, ttsRate, variant, sessionLength); oldVersion-guarded migration. `src/lib/aggregate.ts` (ISO-week key + consecutive-week test), `src/lib/readiness.ts` (pure §10 status On-Target/Some-Risk/High-Risk + coarse AL band framed as *early estimate* + growth stats + action plan from recent stuck-on `missedConcept` tags), `store.exportAll()/importAll()` full-DB JSON backup (§11 safety net). Session now counts the session in `usage` on mount (streak rolls on new ISO week) and writes a weekly aggregate per answer; progress status uses placement-aware `isMastered` (already) so interleaved reviews don't corrupt rollups. UI: `ParentDashboard.tsx` (PIN-gated via on-screen `PinPad.tsx`, §18.12 no native dialogs) — per-child growth card (skills mastered, recent accuracy, weeks active), reading-level vs P-level, traffic-light status, "N of M sessions + streak", weekly bar chart from aggregates, action plan + stuck-on chips, recent certificates, per-child reset, and global **Export/Import backup**; band shown small + parent-only. `ChildPicker` gains a "🔒 Parent area" entry; `App` adds the `dashboard` route. Smoke adds: aggregates+usage written by a session, readiness invariants (empty→On-Target, 6-wrong→High-Risk), non-destructive export/import round-trip (row-count stable), and a dashboard UI walk (add child → create PIN → growth card renders → export downloads) — zero console errors, no overflow at 390px. | M2 milestone — parent visibility + the export/import backup §13 flags as the eviction safety net | `npm test` PASS ×3 (adds M2 dashboard + readiness + export/import guards); `npm run build` green; DB v4 migration additive | v0.2.18 |
| 2026-07-20 | **T01 — letter-sounds scaffolding (inert until phoneme clips ship).** Foundational single-letter→sound level below CVC, wired end-to-end but **disabled** pending the recorded audio. Added: `src/data/phonemes.json` (44-clip manifest, id→`public/phonemes/*.m4a`); real `audio.phoneme()` (cached `HTMLAudioElement`, silent no-op when a clip is absent — no console error); `PackItem.phonemeId` + `McqItem` branch (decode_choice prompt plays a phoneme clip, "🔊 Hear the sound", instead of TTS); `scripts/gen-letter-sounds.mjs` → `phonics-L01-letter-sounds.json` (**46 items** = 23 single-letter sounds ×2, distractors curated to never share the played sound — the /k/ item never offers 'c'; + lesson); `PH-letter-sounds` envelope in `decodability.json`; SW precache glob gains `m4a`. **Inert mechanism:** scope skill carries `enabled:false`; `packs.SKILLS` drops disabled skills **and strips prereqs that point at them**, so CVC's new `["PH-letter-sounds"]` prereq collapses to `[]` and live behaviour is unchanged. `placement.priorSkillIds` now also marks non-ladder foundation prereqs mastered (turnkey so a high placement won't leave letter-sounds blocking once enabled). **To activate:** drop the 23 `public/phonemes/*.m4a` clips + set `enabled:true` + re-verify + owner sign-off. Smoke guards T01 stays inert (skill absent, CVC prereq stripped); temporarily flipping `enabled:true` confirmed lint+build clean and the graph valid, then reverted. | T01 was blocked only on audio assets; scaffold everything else so clips are the last step | `npm run lint:packs` OK (20 packs, 1299 items, 0 warnings); `npm test` PASS (adds T01-inert guard); `npm run build` green; enable-flip dry-run clean | v0.2.17 |
| 2026-07-20 | **T13 — lesson coverage hard-gated (§8).** All 19 runtime skills (9 decode + 9 encode + HF) already shipped explicit en-SG lessons (rule + worked examples), but the build only *warned* on gaps and never checked shape — a future pack could ship a lesson-less skill and silently break the struggle→lesson branch (`getLesson` → undefined → no re-teach). §6b lint gains check (7): every scope skill must have a lesson in its pool pack with a non-empty `iCanStatement` + `explanation` (≥10 chars) + ≥1 worked example (each `text`+`note`), all en-SG-scanned — hard build error otherwise. Negative-test (emptied a `workedExamples`) confirmed the gate fails red; reverted. Closes T13. | Struggle→lesson silently no-ops for any lesson-less skill — make coverage guaranteed, not incidental | `npm run lint:packs` OK (19 packs, 1253 items, 0 warnings); negative-lint caught the empty lesson; `npm test` PASS; `npm run build` green | v0.2.16 (build-gate only — no client bundle change) |
| 2026-07-20 | **A5 — cumulative interleave (§7, §17D) + related fixes.** New `engine.interleavedReviewSkill(attempts,count,pre?)`: every 5th session item (~18% of 16) returns a mastered skill to slip in as a quick review, rotating through the mastered set; `Session.advance` serves it as a **normal attempt** (not an SRS review — no demotion). Because an interleaved review of a *placement*-mastered skill has few/no attempts, `Session` now writes progress status via placement-aware `isMastered(...,masteredRef)` so such a review never downgrades a 'mastered' row to 'active' (A1 persistence). Since interleave lowers per-pattern throughput, a full dual pattern can span >1 session — the smoke's mastery path now **re-enters sessions until a certificate lands** (≤6), and its grapheme-tile clicks target an **enabled** tile (repeated-grapheme words like p·o·p reuse a label; `.first()` alone re-hit the disabled first tile). Engine invariants extended with A5 cadence/selection guards. Closes the last open §18.11 item. | §17D interleaving was unimplemented — only time-due SRS reviews existed | `npm test` PASS ×3 (adds A5 guards + multi-session mastery path); `npm run build` green | v0.2.16 |
| 2026-07-20 | **Engine audit fixes (§18.11 A1–A4).** Root cause: `Session` derived mastery from **attempts only** — placement wrote `progress.status='mastered'` + `entrySkillId` the session never read, so a placed child restarted at CVC (**A1**); and eligibility/advancement/certificate used single-skill `skillMastered`, leaving `patternMastered` dead code — decode-only mastery advanced the ladder and earned a certificate **without spelling** (**A2**, breaks the core dual gate). Fixes: `engine.isMastered(attempts,skill,pre?)` folds in placement-mastered skills; `eligibleSkills`+`patternDecodeSkill` moved into `engine.ts` (single source, §18.7) and now gate decode advancement on the **prior pattern** (decode AND encode) and award **one pattern certificate** on dual-gate completion; `Session` seeds `masteredRef` from `progress`. **A3** `nextDifficulty` counts the trailing streak (resets after each promotion → 1→2→3 over 6 corrects, not 4). **A4** corrected a misleading `scoreTiles` comment. Harness: `window.__engine`/`__getSkill` exposed (DEV); smoke gains engine invariants (A2 dual-gate + advancement, A1 placement-mastery, A3 streak) + an e2e A1 check (session skips placement-mastered CVC). Deferred **A5** (§17D 15–20% mastered-skill interleave — only time-due SRS reviews exist today). | §18.11 audit — engine sequencing deviated from §7 while the data layer was clean | `npm test` PASS incl. new A1/A2/A3 guards (negative-test confirmed the A2 guard red on the old gate); `npm run build` green | v0.2.15 |
| 2026-07-20 | Spec instantiated | Project start | — | — |
| 2026-07-20 | M0 scaffold: Vite+React+TS+PWA, Actions→Pages, IndexedDB store, add-student, ChildPicker, one grammar_mcq loop, update-toast, dyslexia-first styles | First Session Playable milestone start | Offline env — build verified via Actions on push | v0.1.0 |
| 2026-07-20 | M1 first increment: adaptive engine (`engine.ts` — rolling-window mastery, difficulty 1–3, dual decode+encode gate, encode-unlock @70%, struggle→lesson), scope&sequence loader (`packs.ts`, `scopeAndSequence.json`), tile encode renderer (`build_word`) + `decode_choice` MCQ renderer, `scoring.scoreTiles` grapheme-sequence match, `audio.ts` TTS `speak()` (en-GB) + stubbed `phoneme()`, real Session runner (interleaved skills, SESSION_LEN=16, certificates), store schema v2 (attempts keyed by uuid + childId index, progress/certificates stores). CVC short-vowel decode+spelling packs (12 items each) + lessons. Committed `npm test` Playwright harness. Root-cause: tsconfig.node `noEmit` broke `tsc -b` (TS6310) → emitDeclarationOnly+outDir. | M1 MVP core (decoding+spelling engine) start; reaches First Lesson Playable 🏁 (§18.10) | `npm test` PASS headless @390px: mastery→2 certs, dual-gate lockout (encode stays locked when decode <70%), struggle→lesson fires once, zero console errors, no horizontal overflow; `npm run build` green | v0.2.0 |
| 2026-07-20 | §6b build-lint (`scripts/lint-packs.mjs` + `src/data/decodability.json`): validates answer-key resolution, one-correct-per-item, en-SG (US-spelling blacklist), §6a decodability (greedy grapheme segmentation per envelope), duplicate item ids, pool floor (=mastery.minItems, warn <20); wired into `build` as a hard gate + `lint:packs` script. Removed orphaned M0 `grammar-L01-articles` pack (skill not in runtime scope; grammar is M3/T14) — lint caught the dangling skill ref. **App bug fixed** (lint/harness surfaced it): when `pickItem` recycled the pool and re-served the same item id, the item renderer kept its `key={item.id}` so React reused the mounted `McqItem`/`TileItem` with stale internal `picked`/`built` state → item appeared pre-answered with no Continue (a child would soft-lock too). Fix: key renderer by a per-serve counter (`serve`) so every serve remounts fresh. Hardened smoke harness (spawn Vite JS entry + reliable kill; DOM-gated "fresh interactive screen" wait to avoid `window.__item` races; isolated browser context per run). Root cause of soft-lock: renderer identity tied to content id, not serve instance. | §6b lint automation (unblocks ledger sign-off) + milestone bug hardening | `npm test` PASS ×3 consecutive (stable) headless @390px; `npm run build` green with lint gate; negative-lint test confirmed catch of bad key/US-spelling/dup-id/pool | v0.2.1 |
| 2026-07-20 | Content: expanded CVC decode + spelling packs 12→**20 items each** (§6d variety floor). New words: fox, bus, hen, jam, mop, rug, web, zip (pure CVC, en-SG, real-word minimal-pair distractors; adds x/z coverage). All missedConcepts tagged to the target word's short vowel. Difficulty spread per pack: 6×d1, 8×d2, 6×d3. Pending owner sign-off before ledger tick. | Reach §6d pool floor (20/skill) for variety | `npm run lint:packs` OK (40 items, 0 warnings); `npm test` PASS | v0.2.1 |
| 2026-07-20 | **Manage students: Reset + Remove**. ChildPicker gains a "Manage" toggle; each child shows **Reset** (wipe all progress/attempts/certificates/reviews, clear `entrySkillId`, re-run the warm-up placement) and **Remove** (delete profile + all data), each behind an inline two-tap confirm (§18.12 — no native dialogs). Store: `removeChild`, `resetChild`, and a `clearChildData` helper (deletes attempts by childId index + progress/certs/reviews by key prefix in one multi-store transaction). App routes Reset → placement. ChildPicker avatars are now cards with an explicit **Play** button (was: whole-avatar button). Smoke adds a remove-student check (add → placement → Manage → Remove → confirm → gone). | Owner asked for remove/reset student management | `npm test` PASS ×2 (adds remove-student check); `npm run build` green | v0.2.14 |
| 2026-07-20 | **`srs.ts` — spaced repetition (§7)**. New `src/lib/srs.ts` (pure): `scheduleFirst` (+2d, stage 0 on new mastery), `onReviewPass` (advance +7d → +21d → graduate), `onReviewFail` (demote to +2d/stage 0), `dueReviews` (scheduled & past-due, soonest-first, cap 4). Store bumped to **DB v3** with a `reviews` object store (oldVersion-guarded migration keeps existing attempts/progress intact). Session runner: loads reviews on mount, serves **due reviews first** (easier difficulty-1 items on mastered skills) before the current-skill loop, records review pass/fail → advance/demote, and **schedules a review when a skill is newly mastered**. `window.__srs` exposed in DEV for tests. Smoke asserts the scheduling math (+2/+7/+21, pass-advance, fail-demote, due filter+cap) and that mastering a skill schedules a review. (Fresh-session behaviour unchanged — reviews aren't due yet — so existing flows stay green.) | Spaced repetition (§7) — the last M1-core engine piece | `npm test` PASS (placement→session, mastery+cert+review-scheduled, struggle→lesson, dual-gate lockout, SRS math, 0 errors, no overflow); `npm run build` green | v0.2.13 |
| 2026-07-20 | **`placement.ts` — warm-up reading placement (closes First Lesson Playable 🏁 §18.10)** + **T12 ticked**. New `src/lib/placement.ts` (staircase up the 9 dual-gated decode skills: 2 items/level, advance on both correct, stop at first not-passed → that's the entry level; ≤15 items) + `src/features/Placement.tsx` (game-framed warm-up, no right/wrong feedback, auto-advance, progress dots). `McqItem` gains a `quiet` prop (suppresses feedback/styling). App flow: add-student → **placement** → child picker; on finish, sets `child.entrySkillId` and marks all lower decode+encode skills `mastered` so the session starts at the placed level (reading level decoupled from P-level). Smoke updated to drive placement (fresh-screen gated) for both mastery (places high) and struggle (places at CVC) paths. **T12 HF first set owner-approved → ticked.** | Close the milestone: entry is now placement-driven, not first-skill | `npm test` PASS ×3 (placement→session, mastery+cert, struggle→lesson, dual-gate lockout, 0 errors, no overflow); `npm run build` green | v0.2.12 |
| 2026-07-20 | **T12 high-frequency words (first set)** + **T10 ticked**: new skill `HF-words` (recognition, `decode_choice`) in scope. Generator `scripts/gen-hf.mjs` → `phonics-L12-hf`, **55 sight-word items** + lesson (the/was/said/you/one/come/could/who/because/people/friend/school… with confusable distractors). These are non-decodable, so they use the §6a **`highFrequency` escape hatch** — a new `HF-words` envelope in `decodability.json` lists every word+distractor (graphemes empty). Concept `sight-word`. Gated after T10 (prereq PH-two-syllable) so it doesn't perturb session/placement timing; **true threading through every session is deferred to SRS/session-composition work**. **T10 owner-approved → ticked** (T02–T10 decoding ladder complete). | Add HF sight-word set (T12); tick T10 | `npm run lint:packs` OK (19 packs, 1253 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.11 |
| 2026-07-20 | **T10 two-syllable** (compound, closed VCCV, medial doubles): new skills `PH-two-syllable` (decode) + `SP-two-syllable` (encode) in scope (P1, prereq diphthongs; dual-gated). Generator `scripts/gen-two-syllable.mjs` → `phonics-L10-two-syllable` + `spelling-L10-two-syllable`, **65 items each** (maxed). Three types: compound (sun+set), closed VCCV split between the two middle consonants (nap|kin), and medial doubles that mark the split (rab|bit). Tiles stay a **flat grapheme sequence**; medial doubles bb/tt/nn/pp/dd/mm/cc/gg/rr are single tiles. Concept `syllable-{compound,vccv,double}`. Envelope adds the 9 medial-double graphemes. Lessons teach syllable division (split compounds between words; split VCCV between consonants; doubles keep the first vowel short). **T09 owner-approved → ticked.** Completes the single-syllable→two-syllable decoding ladder (T02–T10 all authored). | Start T10 (final phonics tier), maxed per §6d directive | `npm run lint:packs` OK (18 packs, 1198 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.10 |
| 2026-07-20 | **T09 diphthongs + short-oo** (oi/oy, ou/ow, aw/au, oo): new skills `PH-diphthongs` (decode) + `SP-diphthongs` (encode) in scope (P1, prereq r-controlled; dual-gated). Generator `scripts/gen-diphthongs.mjs` → `phonics-L09-diphthongs` + `spelling-L09-diphthongs`, **76 items each** (maxed). Gliding vowels as single grapheme tiles; `ow`/`oo` tiles are **reused** from T07 with their diphthong/short senses here (ow=cow not snow, oo=book not moon) — the item pool disambiguates by word. Concept `diphthong-{oi,oy,ou,ow,aw,au}` and `short-oo`. Envelope adds oi/oy/ou/aw/au. Encode lesson gives the positional spelling rules (oi mid / oy end; ou mid / ow end). **T08 owner-approved → ticked.** | Start T09, maxed per §6d directive | `npm run lint:packs` OK (16 packs, 1068 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.9 |
| 2026-07-20 | **T08 r-controlled** (ar/or/er/ir/ur): new skills `PH-r-controlled` (decode) + `SP-r-controlled` (encode) in scope (P1, prereq vowel-teams; dual-gated). Generator `scripts/gen-r-controlled.mjs` → `phonics-L08-r-controlled` + `spelling-L08-r-controlled`, **74 items each** (maxed). Each bossy-r vowel is a **single grapheme tile** (car→c·ar, bird→b·ir·d, church→ch·ur·ch). Note er/ir/ur all say /er/, so the encode lesson foregrounds the spelling-choice difficulty. Concept `r-controlled-{ar,or,er,ir,ur}`. Envelope adds the 5 r-teams. **T07 owner-approved → ticked.** | Start T08, maxed per §6d directive | `npm run lint:packs` OK (14 packs, 916 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.8 |
| 2026-07-20 | **T07 vowel teams** (ai/ay, ee/ea, oa/ow, oo, ew/ue, igh): new skills `PH-vowel-teams` (decode) + `SP-vowel-teams` (encode) in scope (P1, prereq silent-e; dual-gated). Generator `scripts/gen-vowel-teams.mjs` → `phonics-L07-vowel-teams` + `spelling-L07-vowel-teams`, **72 items each** (maxed; ~8 per team across 10 teams). Each team is a **single grapheme tile** (rain→r·ai·n, feet→f·ee·t, light→l·igh·t; `igh` is a 3-char tile). Only the **long-vowel senses** are used (ow=snow not cow; ea=leaf not bread; oo=moon not book) — the diphthong/short senses land in T08/T09. Concept `vowel-team-{ai,ay,ee,ea,oa,ow,oo,ew,ue,igh}`. Envelope adds the 10 team graphemes (greedy longest-match segments `igh` before 2-char teams before singles). Encode distractor tiles auto-include confusable teams (ai/ay, ee/ea, oa/ow). **T03/T05/T06 owner-approved → ticked.** | Start T07, maxed per §6d directive | `npm run lint:packs` OK (12 packs, 768 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.7 |
| 2026-07-20 | **T06 silent-e / magic-e** (a_e i_e o_e u_e): new skills `PH-silent-e` (decode) + `SP-silent-e` (encode) in scope (P1, prereq FLOSS; dual-gated). Generator `scripts/gen-silent-e.mjs` → `phonics-L06-silent-e` + `spelling-L06-silent-e`, **64 items each** (maxed; 16 per long vowel). First **long-vowel** pattern. Tiles are individual letters incl. the trailing silent `e` (cake→c·a·k·e); best decode distractors are the **short-vowel counterparts** (cap/cape, kit/kite, hop/hope, cub/cube) — directly teaches the magic-e effect. Concept `silent-e-{a,i,o,u}` derived from the long vowel. Envelope = FLOSS envelope (no new graphemes). Lessons explain the bossy-e rule. Note: `June`→`jute` (proper-noun capital broke tile/displayWord match; lint caught it). | Start T06, maxed per §6d directive | `npm run lint:packs` OK (10 packs, 624 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.6 |
| 2026-07-20 | **T05 FLOSS/doubling** (ff/ll/ss/zz): new skills `PH-floss` (decode) + `SP-floss` (encode) in scope (P1, prereq blends; dual-gated). Generator `scripts/gen-floss.mjs` → `phonics-L05-floss` + `spelling-L05-floss`, **58 items each** (maxed the quality FLOSS well; zz is naturally small). Doubled consonant = a **single grapheme tile** (ff/ll/ss/zz), reinforcing the FLOSS chunk (bell→b·e·ll, miss→m·i·ss). Concepts `floss-ff/ll/ss/zz` (derived from the doubled grapheme). Envelopes `PH-/SP-floss` = blends envelope + ff/ll/ss/zz. Encode lesson states the doubling rule (double f/l/s/z after a short vowel at word end). **T04 owner-approved → ticked.** | Start T05, maxed per §6d directive | `npm run lint:packs` OK (8 packs, 496 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.5 |
| 2026-07-20 | **T04 blends** (initial + final consonant blends): new skills `PH-blends` (decode) + `SP-blends` (encode) in scope (P1, prereq digraphs; dual-gated). Authored via **generator** `scripts/gen-blends.mjs` (§12 pipeline — 70-word table → both packs) → `phonics-L04-blends` + `spelling-L04-blends`, **70 items each** (maxed per §6d directive). A blend = two adjacent single-consonant graphemes (each its own tile), so words are CCVC/CVCC/CCVCC; envelope needs no new graphemes (= digraphs envelope). Covers bl/cl/fl/gl/pl/sl, br/cr/dr/fr/gr/pr/tr, sc/sk/sn/sp/st/sw/tw + final nd/nt/mp/st/sk/lt/lk/ft/nk, plus CCVCC (stand/stamp/trust/blend/frost/twist) and blend+digraph (crush/brush/crash/drink) at d3. Concepts `blend-initial`/`blend-final`. Decode distractors hand-picked (real, decodable); encode distractor tiles auto-generated from a confusable pool. Envelopes `PH-/SP-blends` added to `decodability.json`. Pending owner sign-off. | Start T04 (next phonics pattern after digraphs), maxed per §6d directive | `npm run lint:packs` OK (6 packs, 380 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.4 |
| 2026-07-20 | Content: digraph packs 24→**60 items each** (maxed the quality digraph-CVC well). +36 words/skill across sh/ch/th/ck, including double-digraph words (check/chick/chuck, back/pack/deck/lock…) and high-frequency th function words (this/that/then/them/than). Pure decodable-within-envelope, en-SG, real-word distractors (many sh/ch/th minimal pairs: shin/chin/thin, bath/bash/back), correct-choice position varied. Spread per pack ≈ 19/20/21 across difficulty 1/2/3. | Owner asked to max the digraph bank | `npm run lint:packs` OK (4 packs, 240 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.3 |
| 2026-07-20 | **T03 digraphs** (sh/ch/th/ck): new skills `PH-digraphs` (decode) + `SP-digraphs` (encode) in scope (P1, prereq CVC decode; dual-gated, encode-unlock @70%); packs `phonics-L03-digraphs` + `spelling-L03-digraphs` (24 items each: 6 words × 4 digraphs, difficulty 8/8/8; grapheme tiles treat sh/ch/th/ck as single units; distractors include confusable digraphs; correct-choice pos varied) + decode/encode lessons. Extended `decodability.json` with PH-/SP-digraphs envelopes (CVC graphemes + sh/ch/th/ck; greedy longest-match segments the digraph first). Updated smoke assertions for the larger skill graph (later skills unlock as CVC masters, so a fixed 16-item session can't master every skill → assert entry skill masters+certifies, not all). Pending owner sign-off before ledger tick. | Start T03 (next phonics pattern after CVC) | `npm run lint:packs` OK (4 packs, 168 items, 0 warnings); `npm test` PASS (mastery+cert, struggle→lesson, dual-gate lockout, 0 errors, no overflow); `npm run build` green | v0.2.2 |
| 2026-07-20 | Content: CVC packs 20→**60 items each** (maxed the quality single-consonant CVC well). +40 words/skill spanning all five short vowels (bag/bat/can/cap/fan/ham/man/map, beg/jet/leg/pet/red/set/vet/wet, big/bin/dig/fin/hit/kid/lid/win, box/cot/dot/hop/hot/job/pot/top, bug/cub/cut/gum/hug/mug/nut/run). Pure CVC, en-SG, real-word distractors, **correct-choice position varied a/b/c** (McqItem renders choices in fixed order — avoids a position tell). Spread per pack: 18×d1, 24×d2, 18×d3. | Owner asked to max the CVC bank | `npm run lint:packs` OK (120 items, 0 warnings); `npm test` PASS; `npm run build` green | v0.2.1 |

**18.3 Content Pack Ledger (mandatory).** A **T-numbered checkbox ledger** lists every content pack the app needs, grouped by strand and mapped to roadmap phases. Preamble for any AI resuming the project: work top-to-bottom within the current phase; author the pack; run §6b lint; deliver to owner for review (§12.5); **tick the box only after owner sign-off, in the same change, with a changelog row. An unticked box = content not approved; NEVER build/ship UI against an unticked pack.**

- [x] T01 phonics-L01-letter-sounds (+ phoneme audio manifest) *(**ACTIVE — owner-approved 2026-07-21**. Pack (46 items, keyword-anchored choices) + 6-example lesson + `phonemes.json` manifest + `audio.phoneme()` + `phonemeId` renderer, lint-clean. Owner delivered all **44 `public/phonemes/*.m4a` clips**; SW-precaches them (59 entries). **Threaded, not gating (2026-07-21):** `threaded:true` + removed as CVC's prereq — woven in every ~8th item (rotates with HF) so a struggling reader starts on real CVC words instead of being walled at audio-only isolated sounds; choices carry keyword anchors + `LessonView` rotates examples. Smoke asserts T01 threaded/not-eligible/not-a-CVC-prereq.)*
- [x] T02 phonics-L02-cvc-short-vowels *(owner-approved 2026-07-20; 60 items, lint-clean)*
- [x] T03 phonics-L03-digraphs (sh ch th ck) *(owner-approved 2026-07-20; 60 decode + 60 encode, lint-clean)*
- [x] T04 phonics-L04-blends *(owner-approved 2026-07-20; 70 decode + 70 encode via `scripts/gen-blends.mjs`, lint-clean)*
- [x] T05 phonics-L05-floss-doubling *(owner-approved 2026-07-20; 58 decode + 58 encode via `scripts/gen-floss.mjs`, lint-clean)*
- [x] T06 phonics-L06-silent-e *(owner-approved 2026-07-20; 64 decode + 64 encode via `scripts/gen-silent-e.mjs`, lint-clean)*
- [x] T07 phonics-L07-vowel-teams *(owner-approved 2026-07-20; 72 decode + 72 encode via `scripts/gen-vowel-teams.mjs`, lint-clean)*
- [x] T08 phonics-L08-r-controlled *(owner-approved 2026-07-20; 74 decode + 74 encode via `scripts/gen-r-controlled.mjs`, lint-clean)*
- [x] T09 phonics-L09-diphthongs *(owner-approved 2026-07-20; 76 decode + 76 encode via `scripts/gen-diphthongs.mjs`, lint-clean)*
- [x] T10 phonics-L10-two-syllable *(owner-approved 2026-07-20; 65 decode + 65 encode via `scripts/gen-two-syllable.mjs`, lint-clean)*
- [x] T11 spelling packs mirroring T01–T10 (encode items + dictation sentences) *(**owner-approved 2026-07-22 — complete.** encode packs L02–L10 approved; **dictation built** — `dictation` item type + `DictationItem` + `scoreDictation`; `dictation-L02-cvc` (20, `SP-cvc-dictation`) **+ `dictation-L03-digraphs`** (20 sh/ch/th/ck sentences, `SP-digraph-dictation`, prereq `SP-cvc-dictation`, added 2026-07-21), both lint-clean/smoke-verified, **owner-approved 2026-07-21**. **Chain completed 2026-07-22** — new `scripts/gen-dictation.mjs` emits the 7 remaining rungs `dictation-L04-blends`…`L10-two-syllable` (20 decodable sentences each, canonical greedy segmentation reused from the lint, distractor tiles auto-generated), + 7 chained scope skills `SP-blend-dictation`→`SP-twosyllable-dictation` (each gated behind the previous dictation rung, deep after `SP-two-syllable`), + per-level lessons; lint-clean (0 warnings), **owner-approved 2026-07-22**.)*
- [x] T12 high-frequency word sets (threaded) *(owner-approved 2026-07-20; 55-word set via `scripts/gen-hf.mjs`, lint-clean. **Threading now implemented** — `HF-words` is a `threaded` skill (prereq removed) served **every 4th session item** at any level via `engine.threadedSkill` (kept out of the eligible rotation), so sight words are learnt from the start, not gated at the end. Smoke asserts the cadence. Future: additional HF sets are optional breadth.)*
- [x] T13 lessons for all phonics/spelling skills *(owner-directed 2026-07-20; all 19 runtime skills — 9 decode + 9 encode + HF — ship well-formed explicit en-SG lessons, now hard-gated by `lint-packs` check (7): coverage + shape + en-SG)*
- [x] T14 grammar starter track (articles, SVA-simple) *(owner-approved 2026-07-20; `grammar-L01-articles` + `grammar-L02-sva`, 24 each + lessons. **`grammar-L03-tenses`** (`GR-tenses-basic`, 24, past/present/continuous/future) added + **owner-approved 2026-07-21**.)*
- [x] T15 vocab starter track *(owner-approved 2026-07-20; `vocab-L01-synonyms`, 24 + lesson. **`vocab-L02-antonyms`** (24) + **`vocab-L03-context`** (24 context-clue) added + **owner-approved 2026-07-21**.)*
- [x] T16 PSLE Paper-2 comprehension + cloze *(owner-approved 2026-07-20; `comp-L01` + `cloze-L01-grammar`, 24 each + lessons. **`comp-L02`** (`CM-inference`, 20 inference/evaluative) + **`cloze-L02-vocab`** (`CL-vocab-cloze`, 20 content-word word-bank) added + **owner-approved 2026-07-21**.)*
- [x] T19 decodable connected-text reading (word→text bridge) *(**owner-approved 2026-07-22 — complete.** New `reading` strand: **8 reading rungs** `reading-L01-cvc`…`L08-two-syllable` (`RD-*-sentences`), decodable-sentence passage + literal-comprehension MCQ + 🔊 sentence audio + per-level lesson, generated via `scripts/gen-reading.mjs` (rows), each gated behind its phonics pattern and **all ≥20 items** (§6d floor met, 0 warnings). Decodability-linted on passage + answer choices; `placement.priorSkillIds` credits reading rungs the child can already read; smoke-guarded. A decoding child now READS connected decodable text at every rung reached (CVC → two-syllable), not just isolated words.)*
- [x] T20 `ng`/`nk` decode+encode unit (M5.1, §19.13) *(**owner-approved 2026-07-22.** `scripts/gen-ng.mjs` → `phonics/spelling-L14-ng` (47 decode + 47 encode), `ng`/`nk` single grapheme tiles, `PH-/SP-ng` envelope, dual-gated scope skills gated behind `PH-blends` (inserted after blends), + read/spell lessons; lint-clean, smoke-green. Activates the `ng` sound-intro row.)*
- [x] T21 `ear`/`air`/`ure` r-vowel-team unit (M5.1, §19.13) *(**owner-approved 2026-07-22.** `scripts/gen-r-vowel-teams.mjs` → `phonics/spelling-L15-r-vowel-teams` (23 decode + 23 encode), `ear`/`air`/`ure` trigraph tiles, `PH-/SP-r-vowel-teams` envelope, dual-gated scope skills gated behind `PH-r-controlled` (inserted after r-controlled), + lessons; lint-clean, smoke-green. Activates the three r-vowel-team sound-intro rows.)*
- [ ] T18 advanced decode ladder (open syllables, C-le, prefixes/suffixes/morphology, schwa) *(**inflectional-suffix slice authored 2026-07-21** — `PH-suffixes`/`SP-suffixes` (-s/-ing/-ed) via `scripts/gen-suffixes.mjs`, **40 decode + 57 encode** + lessons, lint-clean, wired + smoke-green, gated behind two-syllable. Decode carries only the phonically-transparent rules (just-add, doubling); drop-e/change-y are ENCODE-only (spelling rules). **Authored suffix slice owner-approved 2026-07-22.** **Prefixes + C-le authored 2026-07-22** — `scripts/gen-prefixes.mjs` → `PH-/SP-prefixes` (`phonics/spelling-L12-prefixes`, 26 decode + 26 encode; un/re/dis/mis/non/pre on decodable bases, prefix letters reuse existing tiles → envelope = two-syllable) and `scripts/gen-cle.mjs` → `PH-/SP-c-le` (`phonics/spelling-L13-c-le`, 30 decode + 30 encode; consonant-`le` words with a CLOSED short-vowel first syllable so they stay decodable, new **`le`** grapheme tile + envelope) + per-level lessons; 4 dual-gated scope skills chained two-syllable→suffixes→**prefixes→c-le**; lint-clean (55 packs, 2122 items, 0 warnings), smoke PASS ×3, **prefixes + C-le owner-approved 2026-07-22**. **Box stays open** only for the genuinely-unauthored remainder. **Still deferred (design decision, not just packs):** **open syllables** (me/go/table — long vowel in an open syllable) and **schwa** (unstressed vowel reduction) need *sound-aware* decodability — the grapheme-envelope model can't distinguish long/short/reduced readings of the same letter, so these are a decodability-engine change, not more content.)*
- [x] T17 sentence manipulation (editing, synthesis & transformation) *(**owner-approved 2026-07-21** — MCQ-adapted forms; new strand `sentence`; `SM-editing` (`editing_mcq`, 20 items: spot-and-correct spelling/grammar/capitalisation) + `SM-synthesis` (`synthesis_mcq`, 20 items: pick the correctly combined/rewritten sentence — connectors, relative clauses, reported speech, too…to/so…that) + lessons, lint-clean, smoke-verified; deterministic exact-match, no keyboard (§13), rendered via `McqItem`, gated behind `CL-grammar-cloze`. **Free-text synthesis stays deferred** (§2/§6).)*

> **Ledger status (2026-07-20).** T02 (`phonics-L02-cvc-short-vowels`, decode), the CVC slice of T11 (`spelling-L02-cvc-short-vowels`, encode), and the CVC lessons of T13 are **authored, wired, and passing the automated §6b build-lint** (`npm run lint:packs`; **60 items each** — the quality CVC well maxed out — decodability-checked against the CVC envelope, en-SG, all missedConcepts tagged to the word's short vowel, correct-choice position varied a/b/c). **Owner-approved 2026-07-20 (§12.5)** — T02 ticked; the CVC slices of T11/T13 approved (their boxes stay open only because those T-numbers span all phonics levels, not yet authored). These are the first packs signed off and shipped to UI. Phoneme audio (T01) deferred: TTS-only this pass, `audio.phoneme()` stubbed.

**18.4 Per-feature spec format (every roadmap item).** **Objective** (pedagogical rule/behaviour, exact numbers) · **Target** (file · module · function) · **Behaviour/UI** · **Schema** (new fields: name/type/default/location, §11 updated) · **Acceptance** (how to confirm in a browser).

**18.5 Verification.** Every feature verified headless (Playwright) before marked complete: end-to-end with **zero console errors**. "Syntax valid" ≠ verified. Committed harness (`npm test`) asserts at minimum: boot/wiring smoke on every screen; mastery-gate invariants (dual decode+encode, thresholds); spaced-repetition scheduling (+2d/+7d/+21d, demotion); tile-scoring invariants (grapheme sequence match, distractor confusables); decodability lint passes for all shipped packs; placement walk-down/up logic; IndexedDB persistence + schemaVersion migration; export/import round-trip; **zero horizontal overflow at 360/390px**; a11y basics (labels, aria-live feedback, focus). Every bug fix adds a check that would catch its return.

**18.6 Cache discipline.** Any shipped-file change bumps `CACHE_VERSION` (drives the update toast, §13).

**18.7 Single source of truth.** Every word, answer key, acceptable-list, grapheme segmentation, lesson, and threshold lives in pack/config files — never hardcoded in `src/` modules. Missing content → add to pack + ledger first.

**18.8 Root-cause fixes.** Debug to actual cause before editing; record cause + fix in changelog. No symptom-patching.

**18.9 Build tooling (resolved): Vite + React + TypeScript.** vite-plugin-pwa generates the SW (precache manifest auto-includes packs/audio); GitHub Actions builds `dist/` and deploys to Pages from `main`; `CACHE_VERSION` semantics map to the generated SW revision + a visible `APP_VERSION` (package.json) for the toast/footer. Template's "no-build" rule is superseded — all other §18 rules stand.

**Module map — responsibilities (adapt as built; keep table current).**

| Module | Responsibility |
|---|---|
| `core.ts` | Constants, DOM/util helpers. No imports. |
| `ui.ts` | Themed modal/toast/confirm (no native alert/confirm/prompt); focus-trap, Escape, aria-modal, visual-viewport sized. |
| `packs.ts` | Pack loading/validation; pure lookups over content packs. |
| `engine.ts` | Mastery model, difficulty, progression, dual gates, struggle detection. |
| `srs.ts` | Spaced-repetition scheduling + session composition. |
| `placement.ts` | Warm-up placement walk. |
| `scoring.ts` | Deterministic scorers per itemType + rule-based error analysis (missedConcept). |
| `store.ts` | IndexedDB persistence, schemaVersion migration, aggregates, export/import. |
| `audio.ts` | speak()/phoneme() service (TTS + bundled clips). |
| `items/` | One renderer per itemType (tiles, MCQ, cloze…). |
| `session.ts` | Session runner state machine (items → lesson branch → summary). |
| `dashboard.ts` | Parent dashboard, readiness, action plan, usage/streak. |
| `router.ts` + `main.ts` | Screens/boot. |

Adding/moving a module: update this table **and** the SW app-shell list, bump `CACHE_VERSION` — same change.

**Actual layout as built (M1–M2, 2026-07-20):** `src/lib/packs.ts`, `src/lib/engine.ts`, `src/lib/scoring.ts`, `src/lib/audio.ts`, `src/lib/placement.ts` (warm-up staircase), `src/lib/srs.ts` (spaced repetition), `src/lib/aggregate.ts` (ISO-week rollups), `src/lib/readiness.ts` (§10 readiness + action plan), `src/lib/gamify.ts` (XP/level, pure), `src/lib/support.ts` (§1 difficulty-flag → pacing/placement/fluency tuning, pure; no-flags = defaults), `src/lib/learn.ts` (M5 §19 — pure: `PATTERNS`, `nextToLearn` frontier, `patternStatus`, `learnedSet`), `src/lib/sounds.ts` (**M5.1 §19.13 — pure phoneme-sound lookups over `sounds.json`: `newSoundsFor`/`newSpellingsFor`/`introducedSounds`**), `src/lib/pa.ts` (**§3 audit — phonemic-awareness lookup `paItemsFor`/`hasPA` over the Learn-only `pa-cvc` pack; PA is NOT a Test skill**), `src/lib/economy.ts` (**M6 §20.4 — pure Star-Coins awards + daily-goal/streak/chest math; cosmetic-only**), `src/lib/audio-sfx.ts` (**M6 §20.1 — Web-Audio-synth SFX + ambient music, mute/calm-aware, ducks under TTS**), `src/lib/starfield.ts` (**M6 §20.2 — animated canvas starfield for the galaxy, reduced-motion/Calm-safe**), `src/lib/cosmetics.ts` (**M6.3 §20.3 — pure cosmetics-catalogue lookups over `cosmetics.json`; cosmetic-only**); `src/store.ts` (persistence, **DB v8** adds `wallet`/`inventory` with `reviews`/`aggregates`/`daily`/`usage`/`settings`/`learn` stores, oldVersion-guarded migrations, `exportAll`/`importAll` backup); `src/features/Session.tsx` (**M5 Test mode** — assessment only, learned-gated, no teaching, struggle→`flagReview`; writes weekly aggregates + per-day `daily` + usage), `src/features/LearnRunner.tsx` (**M5 Learn mode** — the §19.6 read+spell teaching unit; M6: takes an `initialPattern` from the galaxy, no internal map) + `src/features/GalaxyMap.tsx` (**M6 §20.2 — the galaxy hub; planet=pattern, tap→Learn/Test; shows the buddy; the child home**), `src/features/Buddy.tsx` (**M6.3 §20.3 — chunky parametric SVG buddy, colour/hat/5 states**) + `src/features/Shop.tsx` (**M6.3 §20.3 — customisation shop, buy/equip cosmetics**) + `src/features/{RewardChest,Celebration}.tsx` (**M6.4 §20.4 — mission chest + reduced-motion-safe confetti**) + `src/features/FluencyArcade.tsx` (**M6.5 §20.6 — opt-in timed speed game over mastered patterns; records no attempts**), `src/features/Placement.tsx` (warm-up screen; marks placement-credited patterns learned), `src/features/ParentDashboard.tsx` + `src/features/PinPad.tsx` (M2 PIN-gated dashboard); `src/features/items/McqItem.tsx` (all MCQ types — grammar/vocab/comprehension/visual-text — `quiet` prop, `phonemeId` branch, optional `passage`, `Choice.keyword` anchor scaffold for letter-sounds, a 🔊 sentence-audio support for `reading` strand passages (T19), + `pa_blend`/`pa_count` phonemic-awareness prompts via `audio.phonemeSeq()`, §3), `src/features/items/TileItem.tsx` (build_word/spell_tiles; b/d reversal cue; heart-word hint for sight-word spelling, HF-spell), `src/features/items/ClozeItem.tsx` (grammar_cloze word-bank), `src/features/items/DictationItem.tsx` (**dictation — word-by-word sentence build, T11**); `src/features/{ChildPicker,AddStudent,LessonView}.tsx`, `src/features/Trophies.tsx` (child-facing trophy room — level/XP bar, badges, certificates, 🔊 Sound-wall link; §14), `src/features/SoundWall.tsx` (**M5.1 read-only Sound wall — sounds learned so far, tap to hear; §19.13.6**), `src/features/CoinCounter.tsx` (**M6 Star-Coins counter with a reduced-motion/Calm-safe pop**), `src/features/M3Demo.tsx` (DEV `#m3demo` render harness); `src/App.tsx` (router/boot; add→placement→pick, + dashboard + trophies routes); `src/data/scopeAndSequence.json` (skills may carry `enabled:false` = authored-but-inert; `packs.SKILLS` drops them and strips prereqs pointing at them) + `src/data/packs/*.json`; `src/data/decodability.json` (§6a envelopes) + `src/data/phonemes.json` (§6c phoneme-clip manifest, files under `public/phonemes/*.m4a`, played by `audio.phoneme()`) + `src/data/graphemeConcepts.json` (§7 grapheme→error-concept map for grapheme-level scoring, from `gen-grapheme-concepts.mjs`) + `scripts/lint-packs.mjs` (§6b build gate) + `scripts/gen-*.mjs` (pack generators) + `test/smoke.mjs` (§18.5 harness). **Not yet built:** `ui.ts` (shared modal/toast primitives — M4 polish); M3 strand packs (grammar/vocab/cloze/comprehension/synthesis).

**18.10 Milestone — First Lesson Playable 🏁.** Add student → warm-up placement → decode + spell-tiles items → struggle triggers Lesson → resume → certificate — end-to-end **offline**, verified on iPad Safari (or simulated viewport). M2+ features are gated on this milestone. **Status (2026-07-20): MET** ✅ — `npm test` drives the full flow (add student → **warm-up placement walk** → session seeded at the placed level → decode + spell-tiles → struggle→Lesson → certificate), offline, 390px, zero console errors. `placement.ts` + `Placement.tsx` built: staircase up the 9-level decode ladder, ≤15 items, no right/wrong feedback, places at the first un-passed level and marks lower levels mastered.

**18.11 Content & pedagogy audit (before "done").** Re-verify the finished app against spec + packs: every answer key resolves; decodability holds for every shipped item; en-SG spelling throughout; grapheme segmentations correct; mastery/SRS gating and once-per-X behaviours match §7 exactly (audit hardest on *engine behaviour* — reference projects found data layers clean but engine sequencing deviating). Findings = numbered work-list (Rule/Target/Fix/Why); each closed with a regression check; record what was verified clean.

**18.12 UI primitives & a11y.** No native dialogs; shared modal/toast primitives; keyboard + screen-reader usable; labelled icon buttons; aria-live for answer feedback; phone-first, zero horizontal overflow at 360px.

---

## 19. Learn / Test Dual-Mode — Structured Learning Path (SPEC — owner-approved 2026-07-22, NOT yet built)

> **For the AI agent picking this up:** this is a full, self-contained spec for milestone **M5**. Read it top to bottom. It builds *on top of* the existing engine (§7), lessons (§8), placement (§7), scope & sequence (§5), and item renderers (§6) — reuse them, do not rebuild them. Every design choice below was decided with the owner; do not re-litigate them. Build in the §19.9 phase order, each phase a separate commit with `npm test` + `npm run build` green and a CLAUDE.md changelog row (per §18).

### 19.1 Why (rationale)
Today the single adaptive session (`Session.tsx`) both **teaches** (lessons fire on first exposure + on struggle) and **assesses** (mastery, SRS, readiness) in one blended loop. The owner wants these split into two deliberate, child-selectable modes so the *teaching* is explicit, systematic and unhurried (the OG ideal for dyslexia) and the *assessment* is clean:
- **Learn mode** — a structured, linear, low-pressure path that **teaches** each phonics/spelling pattern (read + spell together). No adaptive difficulty, no mastery pressure, no timing. This becomes the app's explicit-instruction spine.
- **Test mode** — the existing adaptive engine, unchanged in its assessment behaviour, but now **gated to only assess patterns that have been learned** and **no longer does any teaching itself**.

This makes the app's implicit blended model into an explicit **teach → assess → re-teach** loop.

### 19.2 The two modes (decided)
| | **Learn** (new) | **Test** (existing engine, trimmed) |
|---|---|---|
| Purpose | Explicit teaching | Adaptive assessment |
| Order | **Linear** — one pattern at a time in fixed scope order | Adaptive rotation over *learned* patterns |
| Pressure | None — no scoring gate, no timing, no difficulty ramp | Full engine (difficulty 1–3, mastery, SRS, readiness) |
| Unit | **One whole pattern, read + spell together** | Individual items |
| Teaching | Owns ALL teaching (intro lesson + guided practice + re-teach) | **None** — teaching removed |
| "Done" signal | **Participation** — completing the unit's steps marks the pattern **learned** | Mastery (existing dual-gate + retention) |
| Gating | A pattern can only be learned once its prereq pattern is learned | Only *learned* patterns are eligible |

### 19.3 Pattern status model + the linkage loop (decided)
A **pattern** = a phonics **decode** skill + its **encode** pair (`encodePairId`); identify a pattern by its **decode skill id** (e.g. `PH-cvc-short-vowels`). Threaded skills (`PH-letter-sounds`, `HF-words`) are NOT patterns — leave them threaded/always-available as today.

Per (child, pattern) status, in order:
1. **not-started** — never taught.
2. **learning** — Learn unit in progress (transient).
3. **learned** — Learn unit completed (or placement-credited). **Unlocks the pattern in Test.**
4. **mastered** — Test confirmed mastery (existing dual-gate + retention → certificate). Shown green in the Learn map.
5. **needs-review** — Test detected struggle on a learned pattern → flagged so **Learn resurfaces it** for re-teaching. Cleared when the child re-completes its Learn unit; Test then re-confirms.

**The loop:** Learn *learned* → eligible in Test · Test *mastery* → mastered badge in Learn · Test *struggle* → needs-review → Learn re-teaches → learned again → Test re-confirms.

### 19.4 Data model (DB v6)
Add ONE store (additive, oldVersion-guarded migration; bump `SCHEMA_VERSION` 5→6; add to `ALL_STORES` for export/import; wipe in `clearChildData`):
```
learn:{childId}  →  rows keyed "childId::patternId"
  { patternId:string; learned:boolean; needsReview:boolean; learnedAt?:number; flaggedAt?:number }
```
- `mastered` is NOT stored here — derive it from the existing `progress` store (`status:'mastered'`) / `certificates`, so there is one source of truth for mastery.
- Store helpers in `store.ts`: `getLearn(childId)`, `putLearn(childId, row)`, `setLearned(childId, patternId)`, `flagReview(childId, patternId)`, `clearReview(childId, patternId)`.

### 19.5 Engine / gating changes (`engine.ts`, `placement.ts`)
- **Gate Test by learned:** `eligibleSkills(attempts, pre, learnedPatterns)` — a decode/encode skill is eligible only if its pattern (`patternDecodeSkill(s).id`) ∈ `learnedPatterns` (threaded skills stay exempt). This is the single most important change: **Test never serves an unlearned pattern.**
- **Learn frontier:** add `nextToLearn(learnedPatterns)` → the first pattern in scope order whose prereq pattern is learned (or has no prereq) and that is not itself learned. Drives Learn's linear order. Reuse `decodeLadder` (placement.ts) + the scope prereqs.
- **Placement marks learned:** `placement.priorSkillIds` already credits sub-frontier patterns as `mastered`; also mark those patterns **learned** (they're known). The held-back top-2 **encode** rungs (§7 #3) → their patterns are **decode-known but spelling-untaught**, so treat them as **not learned** → they become the first real Learn units (Learn teaches the spelling; the read half can be a quick review).

### 19.6 Learn mode UX — `LearnRunner` (new `src/features/LearnRunner.tsx`)
Linear walk. On entry, pick the target pattern = the first **needs-review** pattern (re-teach takes priority), else `nextToLearn`. One **unit** = one pattern, taught read+spell together:
1. **Intro / rule** — the pattern's decode `getLesson(decodeSkillId)` (rule + worked examples; `LessonView`, audio-first). *I-do.*
2. **Read practice** — a few `decode_choice` items at difficulty 1 (reuse `McqItem` + the §8 immediate error-correction). **No score kept, no pass bar** — purely practice; the child just does them. *we-do → you-do.*
3. **Spell rule + practice** — the encode `getLesson(encodeSkillId)` then a few `build_word` items d1 (reuse `TileItem` + correction). *we-do → you-do.*
4. **Mark learned** — `setLearned(child, patternId)` + `clearReview` (both decode & encode now unlock in Test). Celebrate ("You learned **sh, ch, th, ck**! Try it in Test.").
5. **Continue** — offer "Learn the next one" (→ next `nextToLearn`) or "Done".

Rules: participation-based (finishing the steps = learned; no accuracy gate — the per-item error-correction guarantees correct final production). No difficulty ramp, no timing, no SRS. Item counts per step are small/fixed (e.g. 3–4). Reuse existing pools via `pickItem(skillId, 1, seen)`.

### 19.7 Test mode changes (`Session.tsx`)
- **Remove PATTERN teaching:** delete the first-exposure intro-lesson branch and the struggle→`LessonView` branch for dual-gated patterns — that teaching lives in Learn. Keep the post-lesson **guided block** mechanism only insofar as it's reused by the down-shift.
- **EXCEPTION — threaded-skill first-encounter lesson (§3, 2026-07-22):** the threaded/orphan skills `HF-words`, `HF-spell`, `PH-letter-sounds` are **not patterns** (no `encodePairId`), so they never appear in Learn — sight words / letter-sounds live ONLY in Test. Their explicit heart-word-method lesson therefore has no other home, so `Session` shows it **once, at first encounter** (`itemsAnswered===0`, guarded by `introRef`) before the item, via `LessonView` + a `'lesson'` phase. This is not the pattern re-teaching M5 removed — it's the only delivery point for the sight-word method.
- **Struggle → flag, don't teach:** when `struggling(...)` on a learned pattern, call `flagReview(child, patternId)` (sets needs-review) and keep going; optionally end the session early with a gentle "Let's practise **X** in Learn." **Keep the per-item immediate error-correction** (reveal + reproduce correct — that's answer integrity, not a lesson) and the §7 #5 down-shift.
- **Gate eligibility:** load the child's `learned` set on mount; pass it to `eligibleSkills`. If nothing is learned yet, Test shows "Learn a pattern first" and routes to Learn.
- Everything else (difficulty, mastery dual-gate + retention, SRS, interleave, massed-first, fluency, readiness, aggregates/daily) is **unchanged**.

### 19.8 Placement interaction (`Placement.tsx`, `App.tsx`)
- Placement still runs once for a new child (warm-up staircase, §7). On finish it marks sub-frontier patterns **learned** + **mastered** (as today) and holds back the top-2 encode rungs (not learned). The child then lands on the **child picker**; from there they choose **Learn** (starts at their frontier) or **Test** (only their placement-learned patterns are available until they Learn more).

### 19.9 Build phases (each a separate commit; keep `npm test`/`build` green)
1. **Status + gating layer (no new screens).** DB v6 `learn` store + helpers; `eligibleSkills` gated by learned; `nextToLearn`; placement marks learned; **remove Test's intro-lesson + struggle-lesson**, wire struggle→`flagReview`. Smoke: Test serves nothing when nothing learned; serves a pattern once it's marked learned; struggle sets needs-review. **◑ FOUNDATION DONE (2026-07-22, v0.2.60):** DB v6 `learn` store + `getLearn`/`putLearn`/`setLearned`/`flagReview`/`clearReview` (export/import + clearChildData wired); `src/lib/learn.ts` (`PATTERNS`, `nextToLearn`, `patternStatus`, `learnedSet`/`needsReviewSet`); `engine.eligibleSkills(attempts, pre, learnedPatterns?)` learned-gate (OPTIONAL — off unless the arg is passed, so live behaviour is unchanged); placement marks fully-credited patterns learned (held-back top-2 encode stay unlearned). Smoke covers the store round-trip, `nextToLearn` frontier, and the learned-gate. **✅ DONE (foundation v0.2.60; live wiring landed with Phase 2, v0.2.61).** The deferred live wiring (Session gate-flip + teaching removal + `struggle→flagReview`) shipped atomically with Phase 2 — teaching never disappeared between deploys.
2. **Learn mode.** `LearnRunner` (the §19.6 unit flow); `ChildPicker` gains **📘 Learn** + **🎮 Test** per child (rename "Play"→"Test"); `App` route `learn`. Smoke: a Learn unit walks intro→read→spell→learned, and the pattern then becomes Test-eligible. **✅ DONE (2026-07-22, v0.2.61):** `src/features/LearnRunner.tsx` (intro lesson → 3 read → encode lesson → 3 spell → `setLearned`+`clearReview` → done/next; participation-based, no scoring gate, reuses `McqItem`/`TileItem`/`LessonView`); `ChildPicker` now shows 📘 Learn / 🎮 Test / 🏆 Trophies per child; `App` `learn` route. **Session is now Test (§19.7):** loads the learned set → `eligibleSkills(...,learned)`; **all teaching removed** (intro + struggle lessons, `introRef`/`lessonCountRef`/`LessonView` gone); on struggle it `flagReview(pattern)` + down-shifts (no re-teach); a `learnfirst` empty state routes to Learn when nothing is eligible. Smoke reworked: e2e now drives **placement → Learn the frontier pattern → Test** (good path masters the held-back encode → retention cert; struggle path flags needs-review); asserts Test fires **zero** lessons.
3. **Loop + map.** Learn resurfaces needs-review patterns first; a **Learn map** screen (read-only: every pattern's status not-started/learning/learned/mastered/needs-review); dashboard reflects learned vs mastered counts. Smoke: Test struggle → needs-review → Learn targets it → re-learn clears it. **✅ DONE (2026-07-22, v0.2.62):** new `src/features/LearnMap.tsx` (per-pattern status list) is now the Learn **landing** — `LearnRunner` opens a `map` phase (computes each pattern's `patternStatus` from the `learn` store + `progress`-derived mastery, and the next target = needs-review first else `nextToLearn`), "Start learning" begins the unit, and after a unit "Learn the next one" returns to the map. Needs-review resurfacing already shipped in Phase 2; Phase 3 makes it visible and adds the clear-loop UI. `ParentDashboard` per-child card now shows **📘 Learn N/M patterns learned · 🏆 K mastered** (`getLearn` + `PATTERNS`/`learnedSet`, mastered = decode+encode both mastered). Smoke drives the full loop: struggle Test → assert needs-review flagged → Learn (map targets the flagged pattern) → re-teach → assert the flag cleared + pattern still learned.
4. **Docs + polish.** Update §5/§7/§14 cross-refs, module table, and this spec's checkboxes; a11y pass on the new screens; CACHE_VERSION bump. **✅ DONE (2026-07-22, v0.2.63):** §5/§7/§14 gained M5 cross-ref notes + module table updated; a11y — `LearnRunner` moves focus to `main.screen` on each sub-phase (map→rule→read→spell→done) so SR announces it, the `done` celebration is an `aria-live` region, and `LearnMap` has an `aria-label`ed list + a `role="status"` progress line; CACHE bumped via the version footer (0.2.63). **M5 COMPLETE** across all four phases.

### 19.10 Invariants to preserve (don't regress)
- All existing engine behaviour (dual-gate, retention certs, SRS +2/+7/+21, massed-first, interleave/fluency review cap, difficulty 1–3, grapheme-level errors, immediate error-correction, down-shift, readiness, aggregates/daily) stays intact — Learn/Test only changes *what is eligible* and *where teaching lives*.
- Fully offline, deterministic scoring, decodability-linted content, en-SG, dyslexia-first UI, no native dialogs (a `<select>` is fine), no timing pressure.
- One source of truth for mastery (progress/certificates), for "learned" (the new `learn` store), and for scope order (`scopeAndSequence.json`).
- Placement stays short/low-pressure and sets both modes' frontier.

### 19.11 Files (add / change)
- **Add:** `src/features/LearnRunner.tsx` (unit flow), `src/features/LearnMap.tsx` (status map), `src/lib/learn.ts` (pure: `nextToLearn`, status derivation — keep pure + unit-tested).
- **Change:** `src/store.ts` (DB v6 `learn` store + helpers, export/import, clearChildData), `src/lib/engine.ts` (`eligibleSkills` learned-gate; `nextToLearn` may live in `learn.ts`), `src/lib/placement.ts` (mark learned), `src/features/Session.tsx` (remove teaching, struggle→flag, gate by learned), `src/features/ChildPicker.tsx` (Learn/Test buttons), `src/App.tsx` (routes + load learned set), `src/features/ParentDashboard.tsx` (learned vs mastered), `test/smoke.mjs` (phase guards), `src/types.ts` (`LearnState`), `styles.css`.

### 19.12 Open sub-decisions (safe to decide at build time, owner delegated)
- Exact item counts per Learn step (suggest 3–4 read + 3–4 spell).
- Whether a Learn unit for a *decode-known, spelling-held-back* pattern shows a short read review or jumps to spelling (suggest a 2-item read warm-up then spelling).
- M3 strands (grammar/vocab/cloze/comprehension) stay **Test-only** for now — Learn covers phonics + spelling patterns only (owner scope). A later M6 could extend Learn to M3.

---

## 19.13 Phoneme sound-intros + Sound wall (M5.1 — SPEC, owner-approved 2026-07-22, NOT built)

> **For the AI agent picking this up:** an extension of M5 Learn mode (§19). Every one of the **44 phonemes** gets an explicit **sound introduction**, rolled out across the roadmap at the pattern where the sound is first taught, plus a browsable **Sound wall** reference. All 44 clips already exist in `public/phonemes/*.m4a` (manifest `src/data/phonemes.json`); only the 23 single-letter ones are used today. Every design point below was decided with the owner — do not re-litigate. Build in the §19.13.8 order; reuse the Learn machinery and the existing clips.

**19.13.1 Where it lives.** The sound-intro is the **first step of a Learn unit** (§19.6), before read practice: learning a pattern opens by teaching that pattern's NEW sound(s), then read → spell as before. Patterns that add **no new phoneme** (blends, FLOSS, two-syllable, suffixes/prefixes/C-le) skip it and go straight to practice. Purely additive to `LearnRunner`; Test/engine untouched.

**19.13.2 Sound-intro card (decided).** Per new sound: the grapheme shown large (e.g. **sh**), a 🔊 button playing `audio.phoneme(id)`, a keyword anchor ("**sh** — ship"), a short **articulation/mouth cue** ("lips forward, push the air — /sh/"), and a "say it with me" prompt (no scoring — no speech input until Phase 3). Several new sounds in one pattern → a short sequence of cards. Multisensory: hear + see grapheme + keyword + mouth cue.

**19.13.3 One sound, many spellings (decided).** Each of the 44 sounds is introduced **once**, at its first occurrence. When a later pattern adds a NEW grapheme for an already-introduced sound, the card reframes it: *"You know this sound /ai/ — here's another way to spell it: ai, ay."* (Directly supports the encode side — dyslexic spellers must learn a sound's spelling alternatives.) Total = 44 sound-intros + the "new spelling" link-cards.

**19.13.4 The 44 → insertion map** (clip id · example · grapheme(s) @ first pattern · later spellings):
- **letter-sounds (23):** `b`bat `d`dog `f`fan `g`goat `h`hat `j`jam `k`kite `l`leg `m`man `n`net `p`pig `r`rat `s`sun `t`tap `v`van `w`web `y`yak `z`zip · `a`ant `e`egg `i`ink `o`orange `u`umbrella.
- **digraphs (4):** `sh`ship · `ch`chip · `th-unvoiced`thin · `th-voiced`this (both sounds of the one `th` grapheme).
- **ng unit / T20 (1):** `ng`ring (unit also teaches `nk`=/ngk/, bank).
- **silent-e (3 long vowels):** `ai`cake(`a_e`) · `igh`bike(`i_e`) · `oa`home(`o_e`) — later spellings `ai/ay`, `igh`, `oa/ow` at vowel-teams; long-u `u_e`(cube) rides the `oo-long` clip.
- **vowel-teams (2 new):** `ee`feet(`ee`, later `ea`) · `oo-long`moon(`oo`, later `ew/ue`). (This pattern mostly adds NEW SPELLINGS of the silent-e long vowels → "same sound" cards.)
- **r-controlled (3):** `ar`car · `or`fork (later `aw/au` at diphthongs) · `ur`her (three spellings `er/ir/ur`, one sound — the canonical "many spellings" case).
- **diphthongs (3):** `oi`coin(later `oy`) · `ow`cow(later `ou`) · `oo-short`book.
- **ear/air/ure unit / T21 (3):** `ear`hear · `air`hair · `ure`cure.
- **deferred/special (2):** `schwa` about — blocked on sound-aware decodability (see T18 note); `zh` measure — very rare, no clean decodable grapheme, optional/last (may live only on the Sound wall).

**19.13.5 New units for orphan sounds (decided; content → ledger T20/T21, owner sign-off per §18.3).**
- **T20 `PH-ng`/`SP-ng`** after digraphs/blends: `ng` (ring, song) + `nk` (bank, think) tiles + `PH-/SP-ng` envelope + decodable packs + lessons + the `ng` sound row.
- **T21 `PH-r-vowel-teams`/`SP-r-vowel-teams`** after r-controlled: `ear`/`air`/`ure` trigraph tiles + envelope + packs + lessons + the three sound rows.
Both are standard next-step phonics with clips already recorded. `schwa` and `zh` stay out of the ladder.

**19.13.6 Sound wall (decided).** Read-only reference `SoundWall.tsx`, reachable from Learn and the Trophy room (§14). Grid of every sound **introduced so far** (a sound is "introduced" once its first-taught pattern is `learned` — derived from the `learn` store, no new store); tap → play clip + keyword + all spellings taught so far. Never shows un-introduced sounds (no spoilers). Child-facing, audio-first, celebratory.

**19.13.7 Data model.** New static `src/data/sounds.json`, one row per phoneme:
```
{ id, keyword, articulation, firstPattern (decode skill id), spellings: [{ grapheme, pattern }] }
```
`id` matches the `phonemes.json` clip ids. `firstPattern` = the scope skill whose Learn unit introduces the sound. `spellings` = each grapheme + the pattern that teaches that spelling (drives the "new spelling" links + the Sound wall). Pure lookups in `src/lib/sounds.ts` — `newSoundsFor(patternId)`, `newSpellingsFor(patternId)`, `introducedSounds(learnedPatterns)` — unit-tested. No new IndexedDB store.

**19.13.8 Build phases** (each a commit; `npm test` + `npm run build` green):
1. `sounds.json` (all 44 rows) + `sounds.ts` pure lookups + smoke unit checks. No UI. **✅ DONE (v0.2.64).**
2. Sound-intro step in `LearnRunner` (new-sound + new-spelling cards before read practice). Smoke: a digraph Learn unit shows a /sh/ card before read practice. **✅ DONE (v0.2.65)** — `'sound'` phase (new-sound + new-spelling cards, auto-play + replay + keyword/articulation), CVC unit is the foundational sound intro; smoke asserts sound cards precede reading.
3. `SoundWall.tsx` + Learn/Trophies entry + `App` route; fills from `introducedSounds`. Smoke: wall shows introduced sounds only; clip plays. **✅ DONE (v0.2.65)** — read-only wall (grapheme+keyword tiles → clip + spellings panel, no spoilers), reachable from the Learn map + Trophies header, `App` `soundwall` view with a `swReturn` return-target.
4. Content units **T20 `ng/nk`** + **T21 `ear/air/ure`** (packs + scope + envelopes + lessons + sound rows), owner sign-off, tick. Docs (§5/§19 cross-refs, module table), CACHE_VERSION bump. **✅ DONE (v0.2.66; owner-approved 2026-07-22)** — `gen-ng.mjs`/`gen-r-vowel-teams.mjs` → dual-gated packs (47+47 / 23+23), envelopes, grapheme-concepts, lessons + scope skills at the blends/r-controlled insertion points; lint-clean + smoke-green, so the `ng` + `ear/air/ure` sound rows are live. **M5.1 COMPLETE.**

**19.13.9 Invariants.** Reuses existing clips + Learn machinery; no change to Test/engine/mastery/SRS. Fully offline, deterministic, en-SG, dyslexia-first, no timing. A sound is never introduced ahead of its pattern. `schwa`/`zh` explicitly deferred. One source of truth: `sounds.json` for sound metadata, `phonemes.json` for clips, `scopeAndSequence.json` for order, the `learn` store for "introduced".

**19.13.10 Files.** *Add:* `src/data/sounds.json`, `src/lib/sounds.ts`, `src/features/SoundWall.tsx`, `scripts/gen-ng.mjs` + `scripts/gen-r-vowel-teams.mjs` (T20/T21). *Change:* `src/features/LearnRunner.tsx` (sound-intro step), `src/features/{ChildPicker,Trophies}.tsx` (Sound-wall entry), `src/App.tsx` (route), `src/data/scopeAndSequence.json` + `src/data/decodability.json` (ng, ear/air/ure), `test/smoke.mjs`, `styles.css`.

---

## 20. M6 — "Cosmic Explorer" gamification & interactivity overhaul (SPEC — owner-approved design 2026-07-23, NOT built)

> **For the AI agent picking this up:** feedback is that the app, while pedagogically sound, is **boring / not engaging** for children. M6 wraps the EXISTING engine in an outer-space adventure game. **The pedagogy engine is frozen** — do NOT change scoring, engine, placement, SRS, content, decodability, Learn/Test *behaviour*, or the scope & sequence. M6 is a **presentation + reward + navigation layer** on top. Every design point below was decided with the owner (Q&A 2026-07-23); do not re-litigate. Build in the §20.9 phase order, each phase a separate commit with lint+test+build green and a changelog row (§18).

### 20.1 Vision (decided)
- **Core metaphor:** Buddy + adventure world. **Setting: outer space / planets.** A child-chosen, customisable **buddy** (alien/robot pal) travels a **galaxy** with the child.
- **Art:** **chunky / cartoony** — bold outlines, big expressive characters, exaggerated animation — authored as **in-code SVG + CSS/canvas** (fully offline, no external assets, no runtime AI).
- **Sound:** **effects + gentle music.** SFX **synthesised at runtime via the Web Audio API** (no asset files) — tap, correct chime, soft wrong, coin, chest, level-up arpeggio, rocket. Optional soft synth **ambient loop**. All mutable in Settings; **SFX on / music off by default**; audio **ducks/pauses during `speak()`** so it never competes with the TTS the child relies on.
- **Play loop:** **missions + daily goal + reward chest.** A Test session = a **mission** to a learned planet with a goal bar; finishing bursts a **reward chest** of Star Coins. A **daily goal + streak** (growing bonus) brings them back.
- **Timing:** **no timers in core play** (dyslexia-first, preserved). Automaticity gets an **opt-in "Fluency Arcade"** mini-game, OFF by default, never part of core lessons.
- **Social:** **individual only** — each child's own private journey/buddy/rewards; no comparison (the household spans very different reading levels incl. a struggling reader).
- **Navigation:** **unified galaxy-map hub.** The galaxy map IS the home. Tap a planet: if new, the buddy flies there and **teaches it (Learn)**; once learned it's a **mission zone (Test)**. Learn/Test blend into one journey — same behaviour, reskinned.

### 20.2 World model
- **Galaxy → systems → planets.** Each **pattern** (the 20 dual-gated decode ladder rungs, `learn.PATTERNS`) = a **planet**. Group planets into **systems** by scope level (P1 CVC cluster, digraph/blend cluster, …). Planet **status** mirrors `patternStatus` (not-started=undiscovered/locked, learning, learned=landed, mastered=flag planted, needs-review=distress signal). The map is the single source for progress, derived from the existing `learn` store + progress — **no new pedagogy state**.
- The galaxy map **replaces** `LearnMap` as the Learn landing AND becomes the child home surface (the current `ChildPicker` per-child Learn/Test/Trophies buttons collapse into "enter your galaxy"). Tapping a planet routes to `LearnRunner` (unlearned/needs-review → the buddy teaches) or `Session`/mission (learned → assess), exactly as today. Locked planets (later than the frontier) stay locked (the §0.2.81 rule). **The hub also shows explicit primary CTAs (v0.2.89) — "🚀 Start a mission" (Test, whenever any pattern is learned) + "📘 Learn/Practise the next planet" (frontier) — so Test is always discoverable/reachable even when a low-placed reader has no mission planets yet; the planets stay tappable as a secondary path.** Coins are earned in missions AND on Learn-unit completion (`COIN_LEARN`), so the reward loop runs in both modes.

### 20.3 Buddy & customisation (decided)
- At **add-student**, the child **picks a buddy** (a few chunky SVG characters) and **names it**. Stored on `Child` (or a new `buddy` record).
- Buddy is a **parametric SVG component** with animated states (idle, cheer, think, sad-encourage, celebrate) reused across every screen; recolourable/skinnable from equipped cosmetics.
- **Customisation shop:** spend **Star Coins** to unlock **buddy outfits, colours, and rocket upgrades** (cosmetic only — never affects difficulty/rewards/pedagogy). Owned + equipped items live in an `inventory` store.

### 20.4 Economy (decided; additive — existing rewards kept)
- **Star Coins** = the new **spendable** currency. Earned from: correct **assessment** answers (small), **mission completion** (chest), **mastery/certificate** (bonus), **daily goal** + **streak** milestones. Reps (`Attempt.review`) earn less/none, mirroring the accuracy rules. Spent only in the shop.
- **XP / Level → "Explorer Rank"** — kept exactly (`gamify.ts`), never spent; drives a rank badge. **Certificates** ("I can…") → **planet flags / mission medals** (kept, reframed). **Badges** kept. Nothing pedagogical is renamed in data — only the child-facing labels/art change.

### 20.5 Feedback / juice (decided: chunky, and dyslexia-safe)
- Correct = buddy **cheer** + star/coin pop flying to the counter; tile place = **bounce + click**; wrong = **soft** wobble + the existing errorless-correction model (unchanged); pattern mastered = **planet lights up + flag**; level-up = **rocket fanfare**; mission end = **chest burst**. Canvas **starfield** background.
- **Guardrails (hard):** all motion respects **`prefers-reduced-motion`**; a **Calm Mode** toggle (Settings) dials animation + sound down globally; celebrations are **brief and skippable** and never block input; keep OpenDyslexic option, large targets, off-white contrast; **no timing** in core. Juice must never delay the next item or obscure the prompt/TTS.

### 20.6 Fluency Arcade (opt-in, decided)
- A separate, **parent-toggled** (default OFF) speed mini-game over **mastered** patterns only (automaticity is a valid goal there). Never gates progress, never appears in core lessons, no bearing on mastery/coins-for-speed in core. Its own screen.

### 20.7 Data model (DB v7 — additive, oldVersion-guarded; bump `SCHEMA_VERSION` 6→7, add to `ALL_STORES` + `clearChildData`)
```
wallet:{childId}      → { coins:number, lifetimeCoins:number }
inventory:{childId}   → { owned:string[], equipped:{buddy?:string, outfit?:string, colour?:string, rocket?:string} }
buddy:{childId}       → { character:string, name:string }        (or fold onto Child)
dailygoal:{childId}   → { day:'YYYY-MM-DD', progress:number, target:number, streak:number, lastGoalDay:string }
```
Settings gains `sfx:boolean` (default true), `music:boolean` (default false), `calmMode:boolean` (default false). Cosmetic catalogue is a static `src/data/cosmetics.json` (id, kind, cost, SVG params) — no runtime cost logic beyond coins ≥ price. **One source of truth stays:** mastery=progress/certs, learned=`learn` store, order=`scopeAndSequence.json`; coins/inventory are purely additive reward state.

### 20.8 Modules (add / change)
- **Add:** `src/lib/audio-sfx.ts` (Web-Audio synth SFX + ambient, mute-aware, ducks on `speak()`), `src/lib/economy.ts` (pure: coin awards from an attempt/result + daily-goal/streak math), `src/features/GalaxyMap.tsx` (hub), `src/features/Buddy.tsx` (parametric SVG + states), `src/features/Shop.tsx`, `src/features/RewardChest.tsx` + `src/features/Celebration.tsx` (overlays), `src/features/FluencyArcade.tsx`, `src/features/CoinCounter.tsx`, `src/data/cosmetics.json`, `src/lib/starfield.ts` (canvas bg). *Change:* `store.ts` (v7 stores), `App.tsx` (galaxy hub routing), `Session.tsx` (mission framing: goal bar + chest + coin awards — **logic untouched**, only wraps), `LearnRunner.tsx` (buddy-led reskin), `ChildPicker.tsx`/`AddStudent.tsx` (buddy pick/name; galaxy entry), `ParentDashboard.tsx` (sfx/music/calm toggles, arcade toggle, coins shown), `Trophies.tsx` (medals/flags), `styles.css`, `test/smoke.mjs`.

### 20.9 Build phases (each a commit; lint+`npm test`+build green; pedagogy engine untouched)
1. **M6.1 Economy + audio + first juice.** `audio-sfx.ts` (synth SFX, Settings sfx/music/calm, duck-on-TTS); `economy.ts` + `wallet` store (DB v7); `CoinCounter`; award coins + play SFX + light correct/reward animation in the EXISTING screens. Immediate perceived lift, low risk. Smoke: coins accrue on correct assessment (not on reps), SFX/calm settings persist, DB v7 migration. **✅ DONE (v0.2.84):** DB v7 `wallet` + `getWallet`/`addCoins`; `economy.coinsForAnswer` (5 assessment / 1 rep / `COIN_CERT` 50); `audio-sfx.ts` (synth SFX + ambient, mute/calm aware, ducks under `speak()`); `CoinCounter` (reduced-motion/Calm-safe pop) in the Test header; Settings sfx/music/calm toggles + `data-calm`. Engine untouched. Smoke: schemaVersion→7 + coins accrue on correct.
2. **M6.2 Galaxy map hub.** `GalaxyMap` + `starfield`; planets = patterns from `learn.PATTERNS`/`patternStatus`; becomes the Learn landing + child home; unified tap→Learn/Test routing (behaviour identical); locked planets. Smoke: map renders planets by status, tapping the active planet enters the unit, later planets locked. **✅ DONE (v0.2.85):** `GalaxyMap.tsx` (planet per pattern, status-coloured, tap → Learn if new/needs-review else Test) + `starfield.ts` (reduced-motion/Calm-safe); the child home; `ChildPicker`→one 🚀 Play; `App` `galaxy` view + `learnPattern`; `LearnRunner` takes `initialPattern` (no internal map); **`LearnMap.tsx` deleted**. Reads pedagogy state only. Smoke reworked around the hub.
3. **M6.3 Buddy + customisation.** `Buddy` SVG + states across screens; buddy pick/name at add-student; `Shop` + `inventory` store + `cosmetics.json`; equip/recolour. Smoke: buy→own→equip round-trip; buddy renders. **✅ DONE (v0.2.86):** `Buddy.tsx` (parametric SVG, 3 characters, colour+hat+5 states), `cosmetics.json`/`cosmetics.ts` catalogue, DB v8 `inventory` store + `buyCosmetic` (idempotent), `Child.buddy`, `AddStudent` buddy picker, `Shop.tsx` (galaxy 🛍️ route, live preview, buy/equip), buddy shown in the galaxy. Cosmetic-only; engine untouched.
4. **M6.4 Missions + daily goal + chest.** Mission framing on `Session` (goal bar, `RewardChest`, `Celebration`), `dailygoal` store + streak bonus. Smoke: finishing a mission opens the chest + awards coins; daily goal/streak increments. **✅ DONE (v0.2.87):** mission goal bar + "Mission complete" summary + `RewardChest` (tap→coins+confetti); `economy` daily-goal/streak math (`progressGoal`, `DAILY_TARGET`, `streakBonus`, `CHEST_BONUS`); DB v9 `dailygoal` store; `Celebration.tsx` (reduced-motion/Calm-safe); galaxy shows today's-goal bar + 🔥 streak. Cosmetic-only.
5. **M6.5 Polish + Fluency Arcade + a11y.** Full juice pass + ambient music; `FluencyArcade` (opt-in, default off); `prefers-reduced-motion` + Calm Mode audit; buddy reactions everywhere; sign-off. Smoke: reduced-motion path renders without animation; arcade is off by default and never in core. **✅ DONE (v0.2.88):** `FluencyArcade.tsx` (opt-in, default off, mastered-only, records no attempts) + Settings toggle + gated galaxy ⚡ entry; buddy reacts to answers in the mission screen; `tap` SFX on choice/tile taps; a11y audit — every animation guarded by `prefers-reduced-motion` + Calm Mode. **M6 COMPLETE (all 5 phases).**

### 20.10 Invariants to preserve (do NOT regress)
- **Zero change** to `scoring.ts`, `engine.ts`, `placement.ts`, `srs.ts`, `learn.ts`, content packs, `decodability.json`, the scope & sequence, or Learn/Test *behaviour* (mastery, dual gate, retention certs, difficulty, struggle→needs-review, threaded lessons, assessment-only accuracy). M6 only reads engine state and adds reward/presentation state.
- Fully offline, self-contained, deterministic; runtime **wiring guard** + decodability lint stay green; en-SG; **no timing in core**; dyslexia-first (reduced-motion, Calm Mode, OpenDyslexic, large targets, audio never over TTS). Coins/cosmetics are cosmetic-only and never influence difficulty or what's taught.
- One source of truth: pedagogy state (progress/certs/`learn`) is read-only to the game layer; the game adds only `wallet`/`inventory`/`buddy`/`dailygoal`.

### 20.11 Open sub-decisions (safe to decide at build time)
Exact coin values + daily-goal target; number/design of buddies + cosmetics in the launch catalogue; system/cluster grouping of planets on the map; whether `buddy` folds onto `Child` or its own store; ambient-music synthesis vs a tiny bundled loop (prefer synth to stay asset-free).

---

*End of CLAUDE.md. Build M0→M1→M2→M3→M4, then M5 (§19 Learn/Test dual-mode) and M5.1 (§19.13 phoneme sound-intros + Sound wall); then M6 (§20 "Cosmic Explorer" gamification layer — presentation/reward only, pedagogy engine frozen). Keep the P1–P6 scope-and-sequence sacred; all content en-SG, static, decodability-linted, fully offline; deterministic scoring only; make the parent dashboard honest and growth-framed.*
